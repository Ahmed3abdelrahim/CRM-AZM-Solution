╭─── Claude Code v2.1.241 ─────────────────────────────────────────────────────╮
│                                                    │ Tips for getting        │
│                    Welcome back!                   │ started                 │
│                                                    │ Run /init to create a … │
│                       ▐▛███▛█                      │ ─────────────────────── │
│                      ▝▜██████▀                     │ What's new              │
│                        ▝▝ ▝▝                       │ Bug fixes and reliabil… │
│                                                    │ Bug fixes and reliabil… │
│        Sonnet 5 · Claude Team · Techprocess        │ Cost estimates (`/cost… │
│ D:\AZM_Solution\CRM-AZM-Solution\specs\001-biling… │ /release-notes for more │
╰──────────────────────────────────────────────────────────────────────────────╯

   Tackle your toughest work with Opus 5. Switch anytime with /model.

❯ /speckit-constitution Read PLAN.md in the repository root. This is a          
bilingual Arabic/English customer support CRM built on-premise with Docker      
                                                                                
Derive the constitution from PLAN.md section 2 (Non-Negotiable Constraints C1   
through C12). Each constraint becomes a principle with its enforcement point    
stated explicitly.                                                              
                                                                                
Add two principles not in that table:                                           
                                                                                
SCOPE DISCIPLINE — Tier D capabilities defined in PLAN.md section 3 are         
specified with acceptance criteria and accommodated in the data model, but      
generate no implementation tasks. Shortcuts taken for the timeline are recorded 
in docs/DEBT.md with their repayment trigger. Undocumented shortcuts are        
violations.                                                                     
                                                                                
TESTING PROPORTIONALITY — Business rules with branching logic require tests:    
status transition legality, permission checks, SLA computation, tenant scoping. 
CRUD passthroughs do not. AI features are validated against a bilingual golden  
dataset, not unit tests.                                                        
                                                                                
Calibrate MVP. Do not mandate testcontainers, CI gates, or air-gap vendoring —  
those are debt items, not principles.                                           

  Searched for 4 patterns, read 3 files, listed 2 directories, ran 2 shell commands (ctrl+o to expand)

● PLAN.md doesn't exist anywhere in the repo (root, git history, or docs) — only
  docs/architecture/stack.md was created, despite the commit message
  referencing a "plan." I can't derive Section 2 (constraints C1–C12) or Section
  3 (Tier D capabilities) without that content, and I don't want to fabricate
  12 constraints on your behalf.

● User answered Claude's questions:
  ⎿  · PLAN.md isn't in the repo root, git history, or docs/. How do you want to
     proceed? → i have moved now to repo root everything you need at its place

  Searched for 1 pattern, listed 1 directory (ctrl+o to expand)

● PLAN.md is there now. Let me read it.

  Searched for 3 patterns, read 4 files (ctrl+o to expand)

● Neither exists yet — nothing to propagate to there. Now writing the
  constitution.

● Write(D:\AZM_Solution\CRM-AZM-Solution\.specify\memory\constitution.md)
  ⎿  Added 138 lines, removed 35 lines
        1 -# [PROJECT_NAME] Constitution                                 
        2 -<!-- Example: Spec Constitution, TaskFlow Constitution, etc. -
          -->                                                            
        1 +<!--                                                          
        2 +Sync Impact Report                                            
        3 +Version change: [TEMPLATE] → 1.0.0 (initial ratification)     
        4 +Bump rationale: First concrete adoption of the constitution fr
          +om a placeholder template — treated as                        
        5 +  MAJOR-equivalent baseline (1.0.0), not incremental, since no
          + prior ratified principle set existed.                        
        6 +Modified principles: none renamed (initial fill)              
        7 +Added sections:                                               
        8 +  - Core Principles I–XII (derived verbatim from PLAN.md §2, c
          +onstraints C1–C12)                                            
        9 +  - Core Principles XIII (Scope Discipline) and XIV (Testing P
          +roportionality) — new, not in PLAN.md §2                      
       10 +  - Authoritative Sources & Precedence (Section 2)            
       11 +  - Development Workflow (Section 3)                          
       12 +  - Governance                                                
       13 +Removed sections: none (all placeholder tokens replaced)      
       14 +Templates requiring updates:                                  
       15 +  - .specify/templates/plan-template.md — ✅ no change needed;
          + its "Constitution Check" gate already                        
       16 +    reads this file at run time and needs no hardcoded princip
          +le list                                                       
       17 +  - .specify/templates/spec-template.md — ✅ no change needed;
          + generic and principle-agnostic                               
       18 +  - .specify/templates/tasks-template.md — ✅ no change needed
          +; Tier D exclusion is enforced by                             
       19 +    Principle XIII at generation time, not by template structu
          +re                                                            
       20 +  - .github/agents/*.md, .claude/skills/*/SKILL.md — ✅ no out
          +dated agent-specific references found                         
       21 +Follow-up TODOs:                                              
       22 +  - docs/DEBT.md does not exist yet. Principle XIII and PLAN.m
          +d §8 require it. Create it before the                         
       23 +    first Definition-of-Done check (PLAN.md §10) using PLAN.md
          + §8's table as the seed content.                              
       24 +-->                                                           
       25  
       26 +# CRM-AZM-Solution Constitution                               
       27 +                                                              
       28  ## Core Principles
       29  
        6 -### [PRINCIPLE_1_NAME]                                        
        7 -<!-- Example: I. Library-First -->                            
        8 -[PRINCIPLE_1_DESCRIPTION]                                     
        9 -<!-- Example: Every feature starts as a standalone library; Li
          -braries must be self-contained, independently testable, docume
          -nted; Clear purpose required - no organizational-only librarie
          -s -->                                                         
       30 +### I. Bilingual String Externalization                       
       31 +Every user-facing string MUST be externalized to `ar`/`en` res
          +ource files. No hardcoded string                              
       32 +literals in application or template code. **Enforcement point*
          +*: lint rule + code review on every                           
       33 +PR touching user-facing code. *(PLAN.md §2, C1)*              
       34  
       11 -### [PRINCIPLE_2_NAME]                                        
       12 -<!-- Example: II. CLI Interface -->                           
       13 -[PRINCIPLE_2_DESCRIPTION]                                     
       14 -<!-- Example: Every library exposes functionality via CLI; Tex
          -t in/out protocol: stdin/args → stdout, errors → stderr; Suppo
          -rt JSON + human-readable formats -->                          
       35 +### II. Reference-Data Bilingual Completeness                 
       36 +Every reference-data table MUST store both `label_ar` and `lab
          +el_en`, both `NOT NULL`. A migration                          
       37 +introducing a reference table without both columns MUST be rej
          +ected. **Enforcement point**:                                 
       38 +migration-level `NOT NULL` constraint. *(PLAN.md §2, C2)*     
       39  
       16 -### [PRINCIPLE_3_NAME]                                        
       17 -<!-- Example: III. Test-First (NON-NEGOTIABLE) -->            
       18 -[PRINCIPLE_3_DESCRIPTION]                                     
       19 -<!-- Example: TDD mandatory: Tests written → User approved → T
          -ests fail → Then implement; Red-Green-Refactor cycle strictly 
          -enforced -->                                                  
       40 +### III. Structural RTL/LTR Parity                            
       41 +RTL and LTR MUST share one stylesheet via CSS logical properti
          +es (`ms-*`, `me-*`, `ps-*`, `pe-*`,                           
       42 +`start-*`, `end-*`). No mirrored build, no direction-specific 
          +stylesheet. **Enforcement point**:                            
       43 +Tailwind configuration established in batch 4a; a repository-w
          +ide grep for `ml-`, `mr-`, `pl-`,                             
       44 +`pr-`, `left-`, `right-` MUST return nothing (PLAN.md §5 F12).
          + *(PLAN.md §2, C3)*                                           
       45  
       21 -### [PRINCIPLE_4_NAME]                                        
       22 -<!-- Example: IV. Integration Testing -->                     
       23 -[PRINCIPLE_4_DESCRIPTION]                                     
       24 -<!-- Example: Focus areas requiring integration tests: New lib
          -rary contract tests, Contract changes, Inter-service communica
          -tion, Shared schemas -->                                      
       46 +### IV. Universal Tenant Attribution                          
       47 +Every domain table MUST carry `branch_id` and `department_id`.
          + **Enforcement point**: migration                             
       48 +schema convention plus the shared base model every domain enti
          +ty inherits (PLAN.md §4.1).                                   
       49 +*(PLAN.md §2, C4)*                                            
       50  
       26 -### [PRINCIPLE_5_NAME]                                        
       27 -<!-- Example: V. Observability, VI. Versioning & Breaking Chan
          -ges, VII. Simplicity -->                                      
       28 -[PRINCIPLE_5_DESCRIPTION]                                     
       29 -<!-- Example: Text I/O ensures debuggability; Structured loggi
          -ng required; Or: MAJOR.MINOR.BUILD format; Or: Start simple, Y
          -AGNI principles -->                                           
       51 +### V. Repository-Layer Tenant Scoping                        
       52 +Tenant scoping MUST be applied inside the repository layer and
          + MUST NEVER be left to the caller.                            
       53 +**Enforcement point**: the `ScopedRepository` base class is th
          +e only construct permitted to build                           
       54 +tenant-scoped queries — services and route handlers never add 
          +their own branch/department filter.                           
       55 +*(PLAN.md §2, C5)*                                            
       56  
       31 -## [SECTION_2_NAME]                                           
       32 -<!-- Example: Additional Constraints, Security Requirements, P
          -erformance Standards, etc. -->                                
       57 +### VI. Immutable Event & Audit Trails                        
       58 +`ticket_events` and `audit_logs` MUST be insert-only. No `UPDA
          +TE` or `DELETE` path may exist at any                         
       59 +layer. **Enforcement point**: database role permissions revoke
          + `UPDATE`/`DELETE` grants, backed by                          
       60 +a trigger. *(PLAN.md §2, C6)*                                 
       61  
       34 -[SECTION_2_CONTENT]                                           
       35 -<!-- Example: Technology stack requirements, compliance standa
          -rds, deployment policies, etc. -->                            
       62 +### VII. Atomic Audit Writes                                  
       63 +Every audit row MUST be written in the same database transacti
          +on as the mutation it records; a                              
       64 +rolled-back transaction MUST leave no audit row. **Enforcement
          + point**: a service-layer decorator                           
       65 +wraps every mutating service method. *(PLAN.md §2, C7)*       
       66  
       37 -## [SECTION_3_NAME]                                           
       38 -<!-- Example: Development Workflow, Review Process, Quality Ga
          -tes, etc. -->                                                 
       67 +### VIII. Single AI Gateway                                   
       68 +Every LLM or embedding call MUST route through the LiteLLM wra
          +pper. No vendor SDK may be imported                           
       69 +anywhere in application code. **Enforcement point**: import li
          +nt rule scanning for vendor SDK                               
       70 +imports. *(PLAN.md §2, C8)*                                   
       71  
       40 -[SECTION_3_CONTENT]                                           
       41 -<!-- Example: Code review requirements, testing gates, deploym
          -ent approval process, etc. -->                                
       72 +### IX. Deterministic AI Degradation                          
       73 +Every AI feature MUST define a deterministic fallback, and the
          + system MUST remain fully usable with                         
       74 +the model endpoint unreachable. **Enforcement point**: a per-f
          +eature acceptance criterion, verified                         
       75 +by stopping the LLM container (PLAN.md §5 F07). *(PLAN.md §2, 
          +C9)*                                                          
       76  
       77 +### X. Service-Layer Permission Enforcement                   
       78 +Permissions MUST be checked in service code. UI-level hiding o
          +f controls is cosmetic only and                               
       79 +confers no security guarantee. **Enforcement point**: a servic
          +e-layer guard decorator on every                              
       80 +mutating method, declaring its required permission. *(PLAN.md 
          +§2, C10)*                                                     
       81 +                                                              
       82 +### XI. Data-Driven Status Transitions                        
       83 +Status transition legality MUST be encoded as rows in the `sta
          +tus_transitions` table, never as                              
       84 +hardcoded conditionals. A change to legal transitions is a dat
          +a change, not a code change.                                  
       85 +**Enforcement point**: the transition service consults `status
          +_transitions` exclusively.                                    
       86 +*(PLAN.md §2, C11)*                                           
       87 +                                                              
       88 +### XII. Stateless SLA Derivation                             
       89 +SLA state MUST be derived from stored timestamps and MUST be c
          +orrect after a full container                                 
       90 +restart. No in-memory timers may hold SLA state. **Enforcement
          + point**: SLA due dates and breach                            
       91 +state are computed at query time from `created_at`, policy, an
          +d `sla_paused_ms` (PLAN.md §5 F05).                           
       92 +*(PLAN.md §2, C12)*                                           
       93 +                                                              
       94 +### XIII. Scope Discipline                                    
       95 +Tier D capabilities (PLAN.md §3) are specified with acceptance
          + criteria and accommodated in the                             
       96 +data model as reserved fields or tables, but generate no imple
          +mentation tasks this sprint. Any                              
       97 +shortcut taken to fit the 48-hour budget MUST be recorded in `
          +docs/DEBT.md` with an explicit                                
       98 +repayment trigger. An undocumented shortcut is a violation, no
          +t a neutral omission — the distinction                        
       99 +between deliberate debt and scope creep is whether it is writt
          +en down. **Enforcement point**:                               
      100 +`/speckit.tasks` MUST refuse to emit implementation tasks for 
          +Tier D items; `docs/DEBT.md` is                               
      101 +checked against PLAN.md §8 as part of the Definition of Done (
          +PLAN.md §10).                                                 
      102 +                                                              
      103 +### XIV. Testing Proportionality                              
      104 +Business rules with branching logic — status transition legali
          +ty, permission checks, SLA                                    
      105 +computation, tenant scoping — MUST have tests. CRUD passthroug
          +hs MUST NOT be required to carry                              
      106 +tests; writing them anyway is unnecessary weight, not extra sa
          +fety. AI features are validated                               
      107 +against a bilingual golden dataset (20 tickets, scored by scri
          +pt — PLAN.md §5 F07), not unit tests.                         
      108 +**Enforcement point**: code review rejects both untested branc
          +hing logic and superfluous tests                              
      109 +wrapped around simple CRUD passthroughs; the golden-set score 
          +is recorded per its acceptance                                
      110 +criterion.                                                    
      111 +                                                              
      112 +## Authoritative Sources & Precedence                         
      113 +                                                              
      114 +`PLAN.md` is the single source of truth for scope, domain mode
          +l, and business rules.                                        
      115 +`docs/architecture/stack.md` is the single source of truth for
          + technology and version choices. If                           
      116 +either of these and a generated artifact (spec, plan, tasks) d
          +isagree, the source document wins and                         
      117 +the artifact is regenerated — never patched around the disagre
          +ement. A dependency listed under                              
      118 +stack.md's "Explicitly not in this stack" MUST NOT be introduc
          +ed without a justification entry in                           
      119 +`research.md`.                                                
      120 +                                                              
      121 +## Development Workflow                                       
      122 +                                                              
      123 +Delivery proceeds in the nine batches defined in PLAN.md §6 (4
          +a–4i). Each batch has its own gate                            
      124 +condition that MUST be demonstrably true — not merely believed
          + true — before the next batch starts                          
      125 +(e.g., "docker compose restart then re-query: every breach sta
          +te is identical"). Commit and `/clear`                        
      126 +between every batch; context is not carried forward informally
          + across a batch boundary. The                                 
      127 +Definition of Done (PLAN.md §10) is the release checklist for 
          +the sprint, and it is not satisfied                           
      128 +until `docs/DEBT.md` matches PLAN.md §8.                      
      129 +                                                              
      130  ## Governance
       44 -<!-- Example: Constitution supersedes all other practices; Ame
          -ndments require documentation, approval, migration plan -->   
      131  
       46 -[GOVERNANCE_RULES]                                            
       47 -<!-- Example: All PRs/reviews must verify compliance; Complexi
          -ty must be justified; Use [GUIDANCE_FILE] for runtime developm
          -ent guidance -->                                              
      132 +This constitution supersedes ad hoc practice. Principles I–XII
          + are structural per PLAN.md §2:                               
      133 +violating one requires a schema or architecture rewrite, and M
          +UST NOT be worked around with a                               
      134 +patch. Principle XIII is the boundary between deliberate 48-ho
          +ur debt and silent scope creep.                               
      135 +Principle XIV keeps test effort proportional to actual branchi
          +ng risk instead of uniform coverage                           
      136 +theater.                                                      
      137  
       49 -**Version**: [CONSTITUTION_VERSION] | **Ratified**: [RATIFICAT
          -ION_DATE] | **Last Amended**: [LAST_AMENDED_DATE]             
       50 -<!-- Example: Version: 2.1.1 | Ratified: 2025-06-13 | Last Ame
          -nded: 2025-07-16 -->                                          
      138 +**Amendment procedure**: a change to a PLAN.md §2 constraint (
          +C1–C12) MUST be made in PLAN.md                               
      139 +first, then reflected here, then propagated to any spec, plan,
          + or tasks artifact it affects. A new                          
      140 +principle not sourced from PLAN.md §2 (as with XIII and XIV) i
          +s added directly here and justified                           
      141 +in the amendment's Sync Impact Report.                        
      142 +                                                              
      143 +**Versioning policy** (semantic versioning of this document): 
      144 +- **MAJOR** — a principle is removed or redefined in a backwar
          +d-incompatible way.                                           
      145 +- **MINOR** — a principle is added, or existing guidance is ma
          +terially expanded.                                            
      146 +- **PATCH** — wording, clarification, or typo fixes with no se
          +mantic change.                                                
      147 +                                                              
      148 +**Compliance review**: every `/speckit.plan` Constitution Chec
          +k gate and every PR is checked                                
      149 +against these principles before merge. A violation is resolved
          + by either fixing the work, recording                         
      150 +a justification in that plan's Complexity Tracking table, or —
          + for Tier D scope only — an entry in                          
      151 +`docs/DEBT.md` per Principle XIII.                            
      152 +                                                              
      153 +**Version**: 1.0.0 | **Ratified**: 2026-08-25 | **Last Amended
          +**: 2026-08-25                                                

  Searched for 1 pattern (ctrl+o to expand)

● Only the source template itself references the placeholder tokens — no
  downstream template hardcodes a principle count, so nothing else needs
  updating.

  Searched for 1 pattern (ctrl+o to expand)

● No extensions.yml — no hooks to run, before or after.

  Summary

  Constitution written to .specify/memory/constitution.md, version 1.0.0
  (initial ratification, 2026-08-25).

  - Principles I–XII map 1:1 to PLAN.md §2's C1–C12, each with its enforcement
    point stated explicitly (lint rule, migration constraint, ScopedRepository,
    DB triggers, import lint, etc.).
  - Principle XIII (Scope Discipline) and XIV (Testing Proportionality) added
    per your spec, not present in PLAN.md's table.
  - Added Authoritative Sources & Precedence (PLAN.md/stack.md win over
    generated artifacts) and Development Workflow (the 4a–4i batch gates,
    commit-and-/clear discipline) as the two non-principle sections the template
    expects — kept short, sourced directly from PLAN.md §6 rather than invented
    process.
  - Deliberately did not mandate testcontainers, CI gates, or air-gap vendoring
    — those stay in docs/DEBT.md as debt, not constitution.

  Checked plan-template.md, spec-template.md, tasks-template.md, and the
  agent/skill files — all are generic and principle-count-agnostic, so no
  propagation edits were needed.

  Follow-up: docs/DEBT.md doesn't exist yet, but Principle XIII and the
  Definition of Done both require it. It should be created from PLAN.md §8's
  table before your first DoD check — want me to create it now?

  Suggested commit message: docs: ratify project constitution v1.0.0 from 
  PLAN.md constraints

✻ Brewed for 2m 49s

※ recap: Goal: ratify the project constitution from PLAN.md's constraints. Done: wrote v1.0.0 to .specify/memory/constitution.md with all 14 principles. Next: waiting on your go-ahead to create docs/DEBT.md. (disable recaps in /config)

❯ /speckit.specify Read PLAN.md in the repository root and produce a complete   
  product specification from it.                                                
                                                                                
  Treat the twelve feature areas F01 through F12 in PLAN.md section 5 as ONE    
  integrated product, not twelve independent features. Specify all twelve. The  
  tier markers control what gets implemented, not what gets specified.          
                                                                                
  For every feature: carry across its rules, its API surface, and its           
  acceptance criteria as testable user stories. Preserve the tier marker on     
  every requirement.                                                            
                                                                                
  Tier D requirements must appear with full acceptance criteria and must be     
  accommodated by the data model, so implementing them later is an extension    
  rather than a rewrite. They must not generate implementation tasks.           
                                                                                
  Apply PLAN.md section 2 constraints and section 12 platform requirements to   
  every feature — they are cross-cutting, not standalone.                       
                                                                                
  Describe behavior and outcomes only. No database engines, frameworks, or API  
  implementation shapes; those belong in the plan. Flag anything PLAN.md leaves 
  ambiguous as [NEEDS CLARIFICATION] rather than guessing. Also read PLAN.md    
  section 4.1. The scoping pattern assignment there (S1 through S6) is          
  binding on every entity. Do not restate it in the specification — it is a     
  data-model                                                                    
  concern — but do not contradict it either.                                    

● Skill(speckit-specify)
  ⎿  Successfully loaded skill

  Listed 1 directory, ran 1 shell command (ctrl+o to expand)

● Now writing the specification.

● Write(spec.md)
  ⎿  Wrote 794 lines to spec.md
       1 # Feature Specification: Bilingual Support CRM — Core Product
       2 
       3 **Feature Branch**: `001-bilingual-support-crm`
       5 **Created**: 2026-08-25
       6 
       7 **Status**: Draft
       8 
         t and produce a complete product
      10 specification from it. Treat the twelve feature areas F01 throug
         h F12 in PLAN.md section 5 as ONE
     … +1268 lines (ctrl+o to expand)
● Write(D:\AZM_Solution\CRM-AZM-Solution\.specify\feature.json)
  ⎿  Wrote 3 lines to ..\..\.specify\feature.json
      1 {
      2   "feature_directory": "specs/001-bilingual-support-crm"
      3 }

  Searched for 1 pattern (ctrl+o to expand)

● Update(spec.md)
  ⎿  Added 2 lines, removed 2 lines
      704  - **Ticket Status**: A named stage in a ticket's life (e.g. ne
           w, open, in progress, resolved, closed,
      705    reopened), some of which pause SLA accounting and some of wh
           ich are terminal.
      706  - **Status Transition Rule**: A configured, permitted move fro
           m one ticket status to another,
      707 -  optionally requiring a specific permission and/or a recorded
          - reason — this table of rules is what                         
      708 -  makes the workflow configurable rather than fixed in softwar
          -e.                                                            
      707 +  optionally requiring a specific permission and/or a recorded
          + reason — this rule set is what makes                         
      708 +  the workflow configurable rather than fixed in software.    
      709  - **Ticket**: The central support record — its subject, descri
           ption, category, priority, status,
      710    assignment (to an individual and/or a team), originating cha
           nnel, and language — that everything
      711    else in the product attaches to.

● Now creating the quality checklist file and running validation.

● Write(checklists\requirements.md)
  ⎿  Wrote 44 lines to checklists\requirements.md
       1 # Specification Quality Checklist: Bilingual Support CRM — Core 
         Product
       2 
         ore proceeding to planning
       4 **Created**: 2026-08-25
       5 **Feature**: [spec.md](../spec.md)
       6 
       8 
       9 - [x] No implementation details (languages, frameworks, APIs)
      10 - [x] Focused on user value and business needs
     … +44 lines (ctrl+o to expand)

● The spec is written and passes quality validation except for three deliberate
  [NEEDS CLARIFICATION] markers — flagged per your instruction rather than
  guessed. No .specify/extensions.yml hooks exist, so nothing else to dispatch.

  Question 1: Channel-Originated Branch/Department Assignment

  Context: FR-023 / Story 8 — "an inbound message MUST be linked to an existing
  ticket... otherwise, a new customer record MUST be created from the sender's
  details." Every customer and ticket must belong to exactly one branch and
  department (FR-004), but PLAN.md never says how a channel message — which
  arrives with no branch/department context — gets one.

  What we need to know: How is the branch/department determined for a customer
  or ticket auto-created from an inbound channel message (email today; any
  future channel)?

  Option: A
  Answer: Fixed by the receiving channel/mailbox configuration
  Implications: Each inbound mailbox (or channel instance) is pre-configured to
  one branch+department; simplest, but requires one mailbox per department in a
   multi-department org
  ────────────────────────────────────────
  Option: B
  Answer: Falls back to one configured default branch/department, reassignable
  later by staff
  Implications: Simple and uniform, but every inbound ticket needs manual
  re-triage before it's correctly scoped
  ────────────────────────────────────────
  Option: Custom
  Answer: Provide your own rule
  Implications: e.g. routing by recipient address pattern, subject-line hint, or

  requiring a human triage step before scoping

  Your choice: [Wait for user response]

  Question 2: SLA Override Bounds

  Context: FR-039 / Story 5, Scenario 6 — "a Team Lead MUST be able to override
  the SLA policy applied to an individual ticket." PLAN.md's actor table just
  says leads can "override SLA policy on a ticket" with no bound.

  What we need to know: Can a lead only pick among the organization's existing
  SLA policies, or set fully custom targets for that one ticket — and is a
  reason required, the way some status transitions require one?

  ┌────────┬────────────────────────┬───────────────────────────────────────┐
  │ Option │         Answer         │             Implications              │
  ├────────┼────────────────────────┼───────────────────────────────────────┤
  │        │ Existing policies      │ Simplest; keeps SLA targets           │
  │ A      │ only, no reason        │ standardized and auditable as "which  │
  │        │ required               │ policy," not arbitrary numbers        │
  ├────────┼────────────────────────┼───────────────────────────────────────┤
  │        │ Existing policies      │ Matches the pattern already used for  │
  │ B      │ only, reason required  │ some status transitions; adds a small │
  │        │                        │  UI/audit requirement                 │
  ├────────┼────────────────────────┼───────────────────────────────────────┤
  │        │ Fully custom targets   │ Most flexible for edge cases, but     │
  │ C      │ on that one ticket,    │ means SLA targets are no longer fully │
  │        │ reason required        │  derivable from policy alone          │
  ├────────┼────────────────────────┼───────────────────────────────────────┤
  │ Custom │ Provide your own rule  │                                       │
  └────────┴────────────────────────┴───────────────────────────────────────┘

  Your choice: [Wait for user response]

  Question 3: Cross-Branch Reporting Authorization

  Context: FR-060 / Story 10, Scenario 2 — "an administrator MAY explicitly
  request a broader, cross-branch view." PLAN.md's permission examples
  (ticket.assign, ticket.close, customer.delete, admin.config) don't include
  anything reporting-specific.

  What we need to know: Is cross-branch reporting available to every account
  holding the Administrator role, or does it require a separate, distinctly
  grantable permission (so an org could have an admin who configures the system
  but can't see other branches' figures)?

  ┌────────┬────────────────────────┬───────────────────────────────────────┐
  │ Option │         Answer         │             Implications              │
  ├────────┼────────────────────────┼───────────────────────────────────────┤
  │        │ Inherent to the        │ Simplest — matches "Administrator =   │
  │ A      │ Administrator role     │ full configuration + reads audit log" │
  │        │                        │  already implying broad visibility    │
  ├────────┼────────────────────────┼───────────────────────────────────────┤
  │        │ Separate grantable     │ More flexible/least-privilege, but    │
  │ B      │ permission (e.g.       │ adds a permission not currently       │
  │        │ distinct from other    │ listed anywhere in PLAN.md            │
  │        │ admin capabilities)    │                                       │
  ├────────┼────────────────────────┼───────────────────────────────────────┤
  │ Custom │ Provide your own rule  │                                       │
  └────────┴────────────────────────┴───────────────────────────────────────┘

  Your choice: [Wait for user response]

  ---

  Once you answer, I'll fold the resolutions into the spec, clear the markers,
  and re-validate the checklist. If you'd rather leave these open for
  /speckit.clarify to handle later, just say so and I'll report the spec as
  done-with-open-questions instead.

  Feature directory: specs/001-bilingual-support-crm
  Spec file: specs/001-bilingual-support-crm/spec.md

✻ Worked for 6m 49s

※ recap: We ratified the CRM constitution from PLAN.md and just drafted the full product spec covering all 12 features; I'm now waiting on your answers to 3 open clarification questions (channel branch/department assignment, SLA override bounds, cross-branch reporting permission) before finalizing it. (disable recaps in /config)

❯ the answer for question 1 is  Fixed by the receiving channel configuration,   
  with a fallback. Add a channel_configs table                                  
  (S1-scoped): channel, identifier (the mailbox address or WhatsApp number),    
  branch_id,                                                                    
  department_id, default_category_id, is_active. Inbound messages resolve       
  branch and                                                                    
  department by matching the recipient identifier against this table. If no     
  config matches,                                                               
  the ticket is created under a designated system default and flagged           
  needs_triage so it                                                            
  surfaces in the Unassigned queue. question 2 B existing polices only , reason 
  required , Q3 → B. Separate grantable permission                              

  Searched for 1 pattern, read 1 file (ctrl+o to expand)

● Now I'll resolve all three markers with the answers given.

● Update(spec.md)
  ⎿  Added 5 lines, removed 5 lines
      192  5. **Given** an unassigned ticket in a department with availab
           le agents holding the right permission,
      193     **When** auto-assignment runs, **Then** it is assigned to o
           ne of them in a fair rotating order,
      194     skipping anyone marked unavailable.
      195 -6. **Given** a Team Lead, **When** they override the SLA polic
          -y applied to one specific ticket,                             
      196 -   **Then** that ticket's deadlines are recalculated from the 
          -overridden policy going forward.                              
      197 -   [NEEDS CLARIFICATION: is a Team Lead's override limited to 
          -choosing among the organization's                             
      198 -   existing SLA policies, or can they set fully custom targets
          - for that one ticket — and does an                            
      199 -   override require a recorded reason, the way some status tra
          -nsitions do?]                                                 
      195 +6. **Given** a Team Lead, **When** they override the SLA polic
          +y applied to one specific ticket by                           
      196 +   selecting a different existing SLA policy and supplying a r
          +eason, **Then** that ticket's                                 
      197 +   deadlines are recalculated from the newly selected policy g
          +oing forward and the reason is                                
      198 +   recorded; **When** they attempt the override without supply
          +ing a reason, **Then** it is                                  
      199 +   rejected.                                                  
      200  7. *(Tier D — specified, not built)* **Given** a ticket that b
           reaches or is about to breach its SLA,
      201     **When** the breach is recorded, **Then** the responsible a
           gent or lead receives a notification
      202     through an external channel (e.g. email) containing the tic
           ket's reference and the breached

● Update(spec.md)
  ⎿  Added 9 lines, removed 7 lines
      283  
      284  **Acceptance Scenarios**:
      285  
      286 -1. **Given** an inbound message from a sender with no matching
          - contact method, **When** it is                               
      287 -   processed, **Then** a new customer and a new ticket are cre
          -ated from it. [NEEDS CLARIFICATION:                           
      288 -   PLAN.md requires every customer and every ticket to belong 
          -to exactly one branch and one                                 
      289 -   department, but does not state how a channel-originated mes
          -sage — which arrives with no branch or                        
      290 -   department context — determines which one it belongs to. Is
          - this fixed by the receiving mailbox                          
      291 -   or channel configuration, chosen by a default branch/depart
          -ment, or resolved some other way?]                            
      292 -2. **Given** an inbound message that quotes an existing ticket
          -'s reference number, **When** it is                           
      286 +1. **Given** an inbound message arriving at a recipient identi
          +fier (e.g. a mailbox address) that is                         
      287 +   configured to a specific branch and department, **When** it
          + is processed and no matching contact                         
      288 +   method is found, **Then** a new customer and a new ticket a
          +re created from it, scoped to that                            
      289 +   configured branch and department.                          
      290 +2. **Given** an inbound message arriving at a recipient identi
          +fier with no matching channel                                 
      291 +   configuration, **When** it is processed, **Then** the resul
          +ting customer and ticket are scoped to                        
      292 +   a designated system-default branch and department, and the 
          +ticket is flagged as needing triage so                        
      293 +   it surfaces in the Unassigned queue (Story 4) rather than b
          +eing silently scoped incorrectly.                             
      294 +3. **Given** an inbound message that quotes an existing ticket
          +'s reference number, **When** it is                           
      295     processed, **Then** it is appended to that ticket's timelin
           e rather than creating a new ticket.
      296  3. **Given** the one functioning channel today (email), **When
           ** a hypothetical new channel is added
      297     later, **Then** doing so requires only that channel's own m
           essage-normalization behavior — no

  Read 1 file (ctrl+o to expand)

● Need to fix the duplicate "3." numbering.

● Update(spec.md)
  ⎿  Added 2 lines, removed 2 lines
      293     it surfaces in the Unassigned queue (Story 4) rather than b
           eing silently scoped incorrectly.
      294  3. **Given** an inbound message that quotes an existing ticket
           's reference number, **When** it is
      295     processed, **Then** it is appended to that ticket's timelin
           e rather than creating a new ticket.
      296 -3. **Given** the one functioning channel today (email), **When
          -** a hypothetical new channel is added                        
      296 +4. **Given** the one functioning channel today (email), **When
          +** a hypothetical new channel is added                        
      297     later, **Then** doing so requires only that channel's own m
           essage-normalization behavior — no
      298     change to how tickets are created or updated.
      299 -4. **Given** a channel not yet built (e.g. WhatsApp, SMS, live
          - chat), **When** it is selected or                            
      299 +5. **Given** a channel not yet built (e.g. WhatsApp, SMS, live
          + chat), **When** it is selected or                            
      300     invoked, **Then** the system responds that it is not yet av
           ailable, without affecting any other
      301     channel.
      302  

● Now Story 10 Scenario 2 (cross-branch permission):

● Update(spec.md)
  ⎿  Added 5 lines, removed 5 lines
      356  
      357  1. **Given** a Team Lead requesting reports, **When** they vie
           w ticket counts by status, SLA
      358     compliance, or per-agent volume, **Then** the figures cover
            only their own department.
      359 -2. **Given** an Administrator requesting the same reports with
          - an explicit cross-branch scope,                              
      360 -   **When** they view them, **Then** the figures cover every b
          -ranch. [NEEDS CLARIFICATION: PLAN.md                          
      361 -   states an admin "may pass an explicit cross-branch scope" b
          -ut does not say what specifically                             
      362 -   authorizes this — is cross-branch reporting available to ev
          -ery account holding the administrator                         
      363 -   role, or does it require a separate, distinctly grantable p
          -ermission?]                                                   
      359 +2. **Given** an Administrator who holds the separate cross-bra
          +nch reporting permission and requests                         
      360 +   the same reports with an explicit cross-branch scope, **Whe
          +n** they view them, **Then** the                              
      361 +   figures cover every branch; **Given** an Administrator who 
          +does not hold that permission,                                
      362 +   **When** they attempt the same request, **Then** it is refu
          +sed and their reports remain confined                         
      363 +   to their own branch/department, even though they hold other
          + administrative permissions.                                  
      364  3. *(Tier D — specified, not built)* **Given** satisfaction ra
           tings exist (Story 9), **When**
      365     reporting is viewed, **Then** satisfaction results appear a
           longside the existing report set. Not
      366     implemented this sprint.

● Now the FR section updates: FR-023 (channel config + new FR-023a), FR-039 (SLA
  override), FR-060 (cross-branch permission).

● Update(spec.md)
  ⎿  Added 7 lines, removed 2 lines
      517  - **FR-023**: An inbound message MUST be linked to an existing
            ticket when it references that
      518    ticket's reference number, or matched to an existing custome
           r when the sender matches a known
      519    contact method; otherwise, a new customer record MUST be cre
           ated from the sender's details.
      520 -  [NEEDS CLARIFICATION: see Story 8, Scenario 1 — how is the b
          -ranch and department determined for a                         
      521 -  customer or ticket created this way?]                       
      520 +- **FR-023a**: Each channel's receiving identifier (e.g. a mai
          +lbox address or a phone number) MUST                          
      521 +  be configurable to a specific branch and department (and opt
          +ionally a default category); an                               
      522 +  inbound message MUST be scoped to whatever branch and depart
          +ment its receiving identifier is                              
      523 +  configured to. A receiving identifier with no matching confi
          +guration MUST NOT block message                               
      524 +  intake — the resulting customer/ticket MUST instead be scope
          +d to a designated system-default                              
      525 +  branch and department, and MUST be flagged as needing triage
          + so it surfaces in the Unassigned                             
      526 +  queue (FR-026) instead of being silently mis-scoped.        
      527  - **FR-024**: Adding a new channel in the future MUST require 
           implementing only that channel's own
      528    message-normalization behavior, with no change to how ticket
           s are created or updated.
      529  - **FR-025**: A channel not yet built MUST be able to report t
           hat it is unavailable without affecting

● Update(spec.md)
  ⎿  Added 4 lines, removed 3 lines
      568  - **FR-038**: Unassigned tickets in a department MUST be distr
           ibutable automatically, in a fair
      569    rotating order, to active agents in that department who hold
            the relevant permission and are not
      570    marked unavailable.
      571 -- **FR-039**: A Team Lead MUST be able to override the SLA pol
          -icy applied to an individual ticket.                          
      572 -  [NEEDS CLARIFICATION: see Story 5, Scenario 6 — bounded to e
          -xisting policies only, or fully custom                        
      573 -  targets, and is a reason required?]                         
      571 +- **FR-039**: A Team Lead MUST be able to override the SLA pol
          +icy applied to an individual ticket by                        
      572 +  selecting a different existing SLA policy for it; a reason M
          +UST be supplied and recorded for the                          
      573 +  override, and an override without a reason MUST be rejected.
          + Custom, one-off SLA targets outside                          
      574 +  the organization's configured policies are out of scope.    
      575  - **FR-040** *(Tier D — specified, not built)*: When a ticket 
           breaches or nears breaching its SLA,
      576    the responsible agent or lead MUST be able to receive a noti
           fication through an external channel
      577    (e.g. email), containing the ticket's reference and the brea
           ched target. Breach events are already

● Update(spec.md)
  ⎿  Added 3 lines, removed 3 lines
      637    branch, department, and date range), SLA compliance percenta
           ge tracked separately for first
      638    response and for resolution, and per-agent volume (assigned,
            resolved, average resolution time).
      639  - **FR-060**: A report MUST reflect only the branch(es)/depart
           ment(s) the requester is authorized to
      640 -  see, except that an administrator MAY explicitly request a b
          -roader, cross-branch view.                                    
      641 -  [NEEDS CLARIFICATION: see Story 10, Scenario 2 — is cross-br
          -anch reporting inherent to the                                
      642 -  administrator role, or a separately grantable permission?]  
      640 +  see. Requesting a broader, explicit cross-branch view MUST r
          +equire a distinct, separately                                 
      641 +  grantable permission — holding other administrative permissi
          +ons MUST NOT by itself grant this                             
      642 +  view.                                                       
      643  - **FR-061** *(Tier D — specified, not built)*: Once customer 
           satisfaction ratings exist (FR-057),
      644    reporting MUST include satisfaction results alongside the ex
           isting report set.
      645  - **FR-062** *(Tier D — specified, not built)*: Reports MUST b
           e schedulable for automatic delivery by

● Now add the "Channel Configuration" key entity and a needs_triage note on the
  Ticket entity.

  Read 1 file (ctrl+o to expand)
● Update(spec.md)
  ⎿  Added 6 lines, removed 1 line
      716    the workflow configurable rather than fixed in software.
      717  - **Ticket**: The central support record — its subject, descri
           ption, category, priority, status,
      718    assignment (to an individual and/or a team), originating cha
           nnel, and language — that everything
      719 -  else in the product attaches to.                            
      719 +  else in the product attaches to. A ticket may carry a needs-
          +triage flag, set when it was created                          
      720 +  from a channel message whose receiving identifier had no mat
          +ching Channel Configuration, so it is                         
      721 +  surfaced for a human to correct its branch/department assign
          +ment.                                                         
      722 +- **Channel Configuration**: A mapping from one channel's rece
          +iving identifier (e.g. a mailbox                              
      723 +  address or a phone number) to the branch, department, and op
          +tional default category that inbound                          
      724 +  messages arriving there are scoped to.                      
      725  - **Ticket Activity Entry**: A single, permanent, ordered entr
           y in a ticket's timeline (status change,
      726    assignment, field edit, note, reply, attachment, AI suggesti
           on applied, SLA breach, reopen),
      727    attributed to whoever performed it.

  Searched for 1 pattern (ctrl+o to expand)

● That's sufficient — no edit needed there. Now update the checklist file to
  reflect resolution, and do a final grep sweep for any remaining markers.

  Searched for 1 pattern (ctrl+o to expand)

● That's just the quoted user input in the Input line — not an active marker.
  All three are resolved.

● Write(checklists\requirements.md)
  ⎿  Added 10 lines, removed 12 lines
      13  
      14  ## Requirement Completeness
      15  
      16 -- [ ] No [NEEDS CLARIFICATION] markers remain                  
      16 +- [x] No [NEEDS CLARIFICATION] markers remain                  
      17  - [x] Requirements are testable and unambiguous
      18  - [x] Success criteria are measurable
      19  - [x] Success criteria are technology-agnostic (no implementati
          on details)
     ...
      31  
      32  ## Notes
      33  
      34 -- Three `[NEEDS CLARIFICATION]` markers remain by deliberate in
         -struction (flag PLAN.md ambiguities                            
      35 -  rather than guess), not by omission:                         
      36 -  1. FR-023 / Story 8 — branch/department assignment for a cust
         -omer or ticket created from an                                 
      37 -     unmatched inbound channel message.                        
      38 -  2. FR-039 / Story 5 — bounds of a Team Lead's SLA policy over
         -ride (existing policies only vs.                               
      39 -     custom targets; reason required or not).                  
      40 -  3. FR-060 / Story 10 — what specifically authorizes an admini
         -strator's cross-branch reporting                               
      41 -     scope (inherent to the role, or a separate grantable permi
         -ssion).                                                        
      42 -- These three were presented to the user as clarification quest
         -ions per the spec-quality workflow.                            
      43 -  Resolve via `/speckit.clarify` (or a direct answer) before `/
         -speckit.plan`, which will otherwise                            
      44 -  need to make its own assumption about all three.             
      34 +- All three clarifications raised during drafting were resolved
         + by the user and folded into the                               
      35 +  spec on 2026-08-25:                                          
      36 +  1. Channel-originated branch/department assignment (Story 8 /
         + FR-023a) — resolved via a                                     
      37 +     per-channel-identifier configuration with a system-default
         + + needs-triage fallback.                                      
      38 +  2. Team Lead SLA override bounds (Story 5 / FR-039) — resolve
         +d as existing-policies-only, reason                            
      39 +     required.                                                 
      40 +  3. Cross-branch reporting authorization (Story 10 / FR-060) —
         + resolved as a separate, distinctly                            
      41 +     grantable permission, not inherent to the Administrator ro
         +le.                                                            
      42 +- Spec is ready for `/speckit.plan`.                           

● All three clarifications are folded into the spec and the quality checklist
  now passes clean.

  Feature directory: specs/001-bilingual-support-crm
  Spec file: specs/001-bilingual-support-crm/spec.md
  Checklist: all items pass — no open [NEEDS CLARIFICATION] markers remain

  What changed to resolve them:
  - Channel routing (Story 8 / FR-023a): added a "Channel Configuration" entity
    mapping each channel's receiving identifier to a branch/department/default
    category; unmatched identifiers fall back to a system default and flag the
    ticket needs_triage so it surfaces in the Unassigned queue.
  - SLA override (Story 5 / FR-039): bounded to picking among existing SLA
    policies only, with a required, recorded reason.
  - Cross-branch reporting (Story 10 / FR-060): now requires a distinct,
    separately grantable permission — not automatic for every Administrator.

  Ready for /speckit.plan.

✻ Baked for 2m 21s

※ recap: We're building out CRM-AZM-Solution's spec-driven plan: the constitution and full product spec (all 12 features, tier-tagged, 3 ambiguities resolved) are done. Next step is running /speckit.plan to turn the spec into a technical implementation plan. (disable recaps in /config)

❯ Three gaps in spec.md that /speckit.plan must not be left to guess. Add all   
  three as                                                                      
  explicit requirements. Do not renumber existing FRs — append with letter      
  suffixes if needed.                                                           
                                                                                
  1. FR-023 says an inbound message is matched to an existing customer "when    
  the sender                                                                    
     matches a known contact method" but never says in what scope. Specify:     
  contact-method                                                                
     matching MUST be scoped to the branch and department resolved from the     
  channel                                                                       
     configuration. The same person contacting two branches legitimately        
  produces two customer                                                         
     records — customer identity is per-branch, not global. State this in       
  FR-023 and in the                                                             
     Customer key entity, because the alternative (global customer identity     
  with per-branch                                                               
     relationships) is a materially different data model and the spec currently 
  implies neither.                                                              
  2. Precedence between a quoted ticket reference and the receiving identifier  
  is undefined.                                                                 
     Specify: when an inbound message quotes a valid ticket reference, that     
  reference MUST                                                                
     determine the ticket, branch, and department, overriding the channel       
  configuration. A                                                              
     mismatch between the referenced ticket's branch and the receiving          
  identifier's configured                                                       
     branch MUST be recorded on the ticket timeline but MUST NOT block the      
  append.                                                                       
  3. FR-039 says overridden SLA deadlines are "recalculated going forward"      
  without defining                                                              
     the basis. Specify: deadlines MUST be recomputed from the ticket's         
  original creation time                                                        
     under the newly selected policy, applying accumulated pause time as        
  normal. An override that                                                      
     renders the ticket immediately breached MUST be permitted and MUST record  
  the breach — the                                                              
     override changes the target, never the elapsed clock.                      
  4. FR-023a introduces a needs-triage flag but no requirement clears it. Add:  
  correcting a                                                                  
     needs-triage ticket's branch or department MUST clear the flag and record  
  the correction on                                                             
     the timeline with old and new values.                                      

  Read 1 file (ctrl+o to expand)

● Update(spec.md)
  ⎿  Added 16 lines, removed 1 line
      516    convert them into new tickets or into updates on existing ti
           ckets.
      517  - **FR-023**: An inbound message MUST be linked to an existing
            ticket when it references that
      518    ticket's reference number, or matched to an existing custome
           r when the sender matches a known
      519 -  contact method; otherwise, a new customer record MUST be cre
          -ated from the sender's details.                               
      519 +  contact method within the branch and department resolved for
          + the message (FR-023a); otherwise, a                          
      520 +  new customer record MUST be created from the sender's detail
          +s, scoped to that same branch and                             
      521 +  department. Contact-method matching MUST NOT be performed ac
          +ross branches: customer identity is                           
      522 +  per branch/department, not global — the same person legitima
          +tely contacting two different                                 
      523 +  branches MUST produce two separate customer records, one per
          + branch/department, never a single                            
      524 +  record shared or merged across them.                        
      525  - **FR-023a**: Each channel's receiving identifier (e.g. a mai
           lbox address or a phone number) MUST
      526    be configurable to a specific branch and department (and opt
           ionally a default category); an
      527    inbound message MUST be scoped to whatever branch and depart
           ment its receiving identifier is
     ...
      529    intake — the resulting customer/ticket MUST instead be scope
           d to a designated system-default
      530    branch and department, and MUST be flagged as needing triage
            so it surfaces in the Unassigned
      531    queue (FR-026) instead of being silently mis-scoped.
      532 +- **FR-023b**: When an inbound message quotes a valid, existin
          +g ticket's reference number, that                             
      533 +  referenced ticket MUST determine the branch and department t
          +he message is scoped to (i.e. the                             
      534 +  ticket's own branch and department), overriding whatever bra
          +nch/department the receiving                                  
      535 +  identifier's channel configuration would otherwise have assi
          +gned. If the referenced ticket's                              
      536 +  branch or department differs from the receiving identifier's
          + configured branch or department,                             
      537 +  that mismatch MUST be recorded as an entry on the ticket's t
          +imeline, but MUST NOT prevent the                             
      538 +  message from being appended to the ticket.                  
      539 +- **FR-023c**: Correcting a needs-triage ticket's branch or de
          +partment (per FR-023a) MUST clear the                         
      540 +  needs-triage flag and MUST record the correction as a timeli
          +ne entry showing both the old and the                         
      541 +  new branch/department values.                               
      542  - **FR-024**: Adding a new channel in the future MUST require 
           implementing only that channel's own
      543    message-normalization behavior, with no change to how ticket
           s are created or updated.
      544  - **FR-025**: A channel not yet built MUST be able to report t
           hat it is unavailable without affecting

● Now FR-039 (SLA override recompute basis):

● Update(spec.md)
  ⎿  Added 6 lines, removed 1 line
      586  - **FR-039**: A Team Lead MUST be able to override the SLA pol
           icy applied to an individual ticket by
      587    selecting a different existing SLA policy for it; a reason M
           UST be supplied and recorded for the
      588    override, and an override without a reason MUST be rejected.
            Custom, one-off SLA targets outside
      589 -  the organization's configured policies are out of scope.    
      589 +  the organization's configured policies are out of scope. Fol
          +lowing an override, the ticket's                              
      590 +  deadlines MUST be recomputed from the ticket's original crea
          +tion time under the newly selected                            
      591 +  policy, applying the ticket's accumulated pause time exactly
          + as it would for any other policy                             
      592 +  resolution (FR-033–FR-035) — the override changes which poli
          +cy applies, never the ticket's                                
      593 +  elapsed clock. If recomputing under the new policy means the
          + ticket is immediately in breach, that                        
      594 +  outcome MUST be permitted and the breach MUST be recorded (F
          +R-037), the same as any other breach.                         
      595  - **FR-040** *(Tier D — specified, not built)*: When a ticket 
           breaches or nears breaching its SLA,
      596    the responsible agent or lead MUST be able to receive a noti
           fication through an external channel
      597    (e.g. email), containing the ticket's reference and the brea
           ched target. Breach events are already

● Update(spec.md)
  ⎿  Added 5 lines, removed 1 line
      723  - **Permission**: A single, specific capability (e.g. assignin
           g a ticket, closing a ticket, deleting a
      724    customer, changing configuration) that can be required for a
           n action and granted through a role.
      725  - **Customer**: The person or organization a ticket is raised 
           on behalf of, with one or more contact
      726 -  methods, in the language they prefer to be addressed in.    
      726 +  methods, in the language they prefer to be addressed in. Cus
          +tomer identity is scoped per branch                           
      727 +  and department, not global: the same person legitimately con
          +tacting two different                                         
      728 +  branches/departments is represented as two separate customer
          + records, each with its own                                   
      729 +  independent contact methods and ticket history — there is no
          + shared or merged identity across                             
      730 +  branches.                                                   
      731  - **Contact Method**: A specific way to reach a customer (phon
           e, email, WhatsApp, other), one of
      732    which is designated primary.
      733  - **Category**: A classification applied to a ticket, organize
           d into a hierarchy, optionally specific

  Read 1 file (ctrl+o to expand)

● Update(spec.md)
  ⎿  Added 16 lines, removed 3 lines
      292     a designated system-default branch and department, and the 
           ticket is flagged as needing triage so
      293     it surfaces in the Unassigned queue (Story 4) rather than b
           eing silently scoped incorrectly.
      294  3. **Given** an inbound message that quotes an existing ticket
           's reference number, **When** it is
      295 -   processed, **Then** it is appended to that ticket's timelin
          -e rather than creating a new ticket.                          
      296 -4. **Given** the one functioning channel today (email), **When
          -** a hypothetical new channel is added                        
      295 +   processed, **Then** it is appended to that ticket's timelin
          +e rather than creating a new ticket,                          
      296 +   and the ticket's own branch and department govern the messa
          +ge — overriding whatever the                                  
      297 +   receiving identifier's channel configuration would otherwis
          +e have assigned.                                              
      298 +4. **Given** an inbound message that quotes an existing ticket
          +'s reference number, **When** the                             
      299 +   ticket's branch or department differs from the one the rece
          +iving identifier is configured to,                            
      300 +   **Then** the message is still appended to the ticket, and t
          +he mismatch is recorded as a timeline                         
      301 +   entry rather than blocking the append.                     
      302 +5. **Given** a customer who has legitimately contacted two dif
          +ferent branches, **When** their                               
      303 +   contact details are looked up for a new inbound message, **
          +Then** matching considers only the                            
      304 +   customer records already scoped to the branch/department th
          +e message resolves to — it never                              
      305 +   merges or matches across their separate, branch-specific cu
          +stomer records.                                               
      306 +6. **Given** a ticket flagged as needing triage, **When** a st
          +aff member corrects its branch or                             
      307 +   department, **Then** the needs-triage flag is cleared and t
          +he correction is recorded on the                              
      308 +   ticket's timeline with both the old and the new values.    
      309 +7. **Given** the one functioning channel today (email), **When
          +** a hypothetical new channel is added                        
      310     later, **Then** doing so requires only that channel's own m
           essage-normalization behavior — no
      311     change to how tickets are created or updated.
      299 -5. **Given** a channel not yet built (e.g. WhatsApp, SMS, live
          - chat), **When** it is selected or                            
      312 +8. **Given** a channel not yet built (e.g. WhatsApp, SMS, live
          + chat), **When** it is selected or                            
      313     invoked, **Then** the system responds that it is not yet av
           ailable, without affecting any other
      314     channel.
      315  

● Now update the Independent Test line for Story 8 to mention the new coverage,
  and update Story 5's Scenario 6 for the recompute basis / immediate-breach
  behavior.

● Update(spec.md)
  ⎿  Added 4 lines, removed 2 lines
      278  enough to prove it.
      279  
      280  **Independent Test**: Send an inbound message from an unrecogn
           ized sender and confirm both a new
      281 -customer and a new ticket are created from it; then send a sec
          -ond message that quotes an existing                           
      282 -ticket's reference number and confirm it appends to that ticke
          -t instead of creating a new one.                              
      281 +customer and a new ticket are created from it, scoped to the r
          +eceiving identifier's configured                              
      282 +branch/department; then send a second message that quotes an e
          +xisting ticket from a *different*                             
      283 +branch's receiving identifier and confirm the ticket's own bra
          +nch/department wins, with the mismatch                        
      284 +recorded rather than blocking the append.                     
      285  
      286  **Acceptance Scenarios**:
      287  

● Update(spec.md)
  ⎿  Added 6 lines, removed 3 lines
      194     skipping anyone marked unavailable.
      195  6. **Given** a Team Lead, **When** they override the SLA polic
           y applied to one specific ticket by
      196     selecting a different existing SLA policy and supplying a r
           eason, **Then** that ticket's
      197 -   deadlines are recalculated from the newly selected policy g
          -oing forward and the reason is                                
      198 -   recorded; **When** they attempt the override without supply
          -ing a reason, **Then** it is                                  
      199 -   rejected.                                                  
      197 +   deadlines are recomputed from the ticket's original creatio
          +n time under the newly selected                               
      198 +   policy — applying its accumulated pause time exactly as usu
          +al — and the reason is recorded;                              
      199 +   **When** they attempt the override without supplying a reas
          +on, **Then** it is rejected.                                  
      200 +   **Given** an override whose recomputed deadline has already
          + passed, **When** the override is                             
      201 +   applied, **Then** the ticket is immediately recorded as bre
          +ached rather than the override being                          
      202 +   blocked or the breach being suppressed.                    
      203  7. *(Tier D — specified, not built)* **Given** a ticket that b
           reaches or is about to breach its SLA,
      204     **When** the breach is recorded, **Then** the responsible a
           gent or lead receives a notification
      205     through an external channel (e.g. email) containing the tic
           ket's reference and the breached

  Searched for 1 pattern (ctrl+o to expand)

● All numbering is clean, sequential, and untouched apart from the new
  FR-023a/b/c suffixes. No duplicates or renumbering occurred. Now update the
  requirements checklist notes to record this second clarification round.

  Read 1 file (ctrl+o to expand)

● Update(checklists\requirements.md)
  ⎿  Added 9 lines
      39       required.
      40    3. Cross-branch reporting authorization (Story 10 / FR-060) —
           resolved as a separate, distinctly
      41       grantable permission, not inherent to the Administrator ro
          le.
      42 +- A second round of gaps identified independently (not `[NEEDS 
         +CLARIFICATION]` markers, but                                   
      43 +  under-specified requirements) was closed on 2026-08-25 withou
         +t renumbering any existing FR:                                 
      44 +  1. Contact-method matching scope and per-branch customer iden
         +tity — FR-023 (edited in place) and                            
      45 +     the Customer key entity.                                  
      46 +  2. Precedence between a quoted ticket reference and the recei
         +ving identifier's channel                                      
      47 +     configuration — new **FR-023b**.                          
      48 +  3. SLA override recompute basis (original creation time, accu
         +mulated pause, immediate breach                                
      49 +     permitted) — FR-039 (edited in place).                    
      50 +  4. Needs-triage flag clearing on correction — new **FR-023c**
         +.                                                              
      51  - Spec is ready for `/speckit.plan`.

● All four gaps are closed in specs/001-bilingual-support-crm/spec.md, with
  existing FR numbers untouched:

  - FR-023 (edited in place): contact-method matching is scoped to the message's
    resolved branch/department; customer identity is explicitly per-branch, not
    global — same person at two branches = two customer records. Also stated in
    the Customer key entity.
  - FR-023b (new): a quoted ticket reference overrides the channel configuration
    for branch/department; a mismatch is recorded on the timeline but never
    blocks the append.
  - FR-023c (new): correcting a needs-triage ticket's branch/department clears
    the flag and records old/new values on the timeline.
  - FR-039 (edited in place): SLA override deadlines recompute from the ticket's
    original creation time under the new policy, with accumulated pause applied
    as normal; an override that lands the ticket in immediate breach is
    permitted and recorded, not blocked or suppressed.

  Story 8 and Story 5 acceptance scenarios and the Independent Test lines were
  updated to match, and the requirements checklist logs this second round of
  fixes. Spec remains ready for /speckit.plan.

✻ Churned for 1m 57s
※ recap: We're building the spec-driven CRM plan: I just closed 4 spec gaps (customer scoping, channel-reference precedence, SLA override math, triage-flag clearing) in spec.md with no renumbering. Next: run /speckit.plan to turn this spec into an implementation plan.

❯ /speckit.plan ultrathink                                                      
                                                                                
  Follow docs/architecture/stack.md exactly. Introduce no dependency not listed 
  there without                                                                 
  recording the justification in research.md.                                   
                                                                                
  Build data-model.md directly from PLAN.md section 4. Every entity, every      
  field, every                                                                  
  constraint listed there is binding. Add indexes and foreign keys as needed    
  but do not remove                                                             
  or rename what is specified.                                                  
                                                                                
  Also read PLAN.md section 4.1. The scoping pattern assignment there (S1       
  through S6) is                                                                
  binding on every entity. Each table gets exactly one pattern; do not invent   
  new ones. Do not                                                              
  add branch_id or department_id to any table assigned S4 or S6.                
                                                                                
  Seed the status_transitions table from PLAN.md section 4.3. The workflow      
  engine is that table                                                          
  — no hardcoded enums or match statements on status.                           
                                                                                
  Enforce every constraint in PLAN.md section 2 at the layer named in its       
  enforcement column.                                                           
                                                                                
  The channel abstraction in F03 is a single interface with a normalized        
  message shape. The                                                            
  email adapter implements it; WhatsApp, SMS, and live chat are declared and    
  raise                                                                         
  NotImplementedError. Adding a channel later must require zero changes to      
  ticket creation logic.                                                        
                                                                                
  Accommodate Tier D features in the schema — reserved columns, tables, or enum 
  values — but                                                                  
  produce no implementation design for them. The csat_score and csat_comment    
  columns on tickets                                                            
  are examples.                                                                 
                                                                                
  Also read specs/002 through specs/007. These are roadmap specifications for   
  Tier D features                                                               
  and generate no tasks. Use them only to verify that data-model.md             
  accommodates them per                                                         
  Principle XIII: reserved columns or enum values where an extension is         
  additive, and no design                                                       
  decision that would force a migration across populated tables later. Record   
  each                                                                          
  accommodation in research.md.                                                 
                                                                                
  Business logic in service classes. Route handlers validate, delegate,         
  serialize.                                                                    
                                                                                
  IMPLEMENTATION MAY BE CARRIED OUT BY A LESS CAPABLE MODEL THAN THIS ONE.      
  Design the artifacts                                                          
  so implementation is mechanical rather than inferential:                      
                                                                                
  - Include the complete target file tree in plan.md, with the responsibility   
  of each module                                                                
    stated in one line.                                                         
  - Specify exact signatures for the five shared abstractions that every other  
  module depends                                                                
    on: ScopedRepository, the permission guard decorator, the audit-write       
  decorator, the                                                                
    ChannelAdapter interface, and the LiteLLM wrapper. These five carry the     
  constitution. If                                                              
    they are ambiguous, every downstream module violates it.                    
  - State each service class's public methods by name with parameter and return 
  types.                                                                        
  - Where a pattern repeats (CRUD over a scoped entity), define it once         
  explicitly and name the                                                       
    entities that follow it, rather than leaving it to be generalized.          
  - Never write "handle appropriately", "as needed", or "etc." Name the         
  behavior.                                                                     
                                                                                
  Deliver: research.md with every technology decision and every Tier D          
  accommodation;                                                                
  data-model.md complete; contracts/ with OpenAPI for all Tier M and Tier S     
  endpoints;                                                                    
  quickstart.md from docker compose up through seeded demo. also  Re-read       
  .specify/memory/constitution.md and PLAN.md section 4.1. List every place the 
  code                                                                          
  you just wrote violates a principle or diverges from the specified scoping    
  pattern. Fix them.                                                            
  Add no new functionality.                                                     

  Searched for 2 patterns (ctrl+o to expand)

● Skill(speckit-plan)
  ⎿  Successfully loaded skill

  Read 9 files, ran 1 shell command (ctrl+o to expand)

● Write(research.md)
  ⎿  Wrote 127 lines to research.md
       1 # Research: Bilingual Support CRM — Core Product
       3 **Input**: `specs/001-bilingual-support-crm/spec.md`, `PLAN.md`,
       4 `.specify/memory/constitution.md`, `specs/002-007` (Tier D roadm
         ap).
       6 This document has three parts: (1) technology decisions, all tak
       7 `docs/architecture/stack.md` with no substitution; (2) two docum
       8 `PLAN.md` §4.1 that had to be resolved to produce a complete `da
         ta-model.md`, resolved using
         e constitution; (3) a
         y Tier D roadmap spec
     … +316 lines (ctrl+o to expand)

● Now the data model — the largest artifact. Writing it directly and completely.

● Write(data-model.md)
  ⎿  Wrote 606 lines to data-model.md
       1 # Data Model: Bilingual Support CRM — Core Product
       3 **Source of truth**: PLAN.md §4 (Domain Model), binding on every
          entity, field, and constraint
         _article_chunks`, `api_keys`,
       5 `audit_logs`, `inbound_messages`) were missing from PLAN.md §4.1
       6 pattern assignment is resolved and justified in `research.md` Pa
         rt 2, not invented here. Nothing
         are added as needed per this
       8 document's own §0.4/§0.5 conventions.
      10 ## 0. Conventions (apply uniformly; not restated per table)
     … +842 lines (ctrl+o to expand)

● Write(contracts\openapi.yaml)
  ⎿  Wrote 1059 lines to contracts\openapi.yaml
       1 openapi: 3.0.3
       3   title: Bilingual Support CRM API
       4   version: "1.0.0"
       6     Tier M and Tier S endpoints only, per PLAN.md §3. Tier D cap
         abilities (audit-trail browsing,
         /webhook integrations) are
       8     intentionally absent — see specs/002 through specs/007. Ever
       9     permission code named in its `x-permission` extension, check
         ed by the service-layer guard
         t is the caller.
     … +1730 lines (ctrl+o to expand)

● Let me validate the OpenAPI YAML actually parses correctly.

  Ran 2 shell commands (ctrl+o to expand)

● I found real inconsistencies in what I've written so far — let me fix them now
  before drafting plan.md, since the user's final instruction asks for exactly
  this kind of audit.

  Read 1 file (ctrl+o to expand)

● Update(data-model.md)
  ⎿  Added 27 lines, removed 7 lines
      31  NOT** have `updated_at`/`updated_by` — Principle VI (constituti
          on) forbids the UPDATE that would
      32  ever set them. Enforced at the DB role/trigger layer per PLAN.m
          d C6 (§3 below).
      33  
      34 -### 0.4 Reference-data base — applied only to tables whose PLAN
         -.md §4.2 entry lists `label_ar`,                               
      35 -`label_en` as fields: `branches`, `departments`, `roles`, `perm
         -issions`, `categories`,                                        
      34 +### 0.4 Reference-data label base — applied only to tables whos
         +e PLAN.md §4.2 entry lists                                     
      35 +`label_ar`, `label_en` as fields: `branches`, `departments`, `r
         +oles`, `permissions`, `categories`,                            
      36  `priorities`, `ticket_statuses`, `sla_policies`, `quick_replies
          `, `teams`.
      37  
      38  | Column | Type | Null? | Default |
      39  |---|---|---|---|
      40  | `label_ar` | `TEXT` | NOT NULL | |
      41  | `label_en` | `TEXT` | NOT NULL | |
      42 -| `is_active` | `BOOLEAN` | NOT NULL | `true` |                
      43 -| `sort_order` | `INT` | NOT NULL | `0` |                      
      42  
      45 -Where PLAN.md §4.2 already names one of these four for a specif
         -ic table (e.g. `branches.is_active`,                           
      46 -`categories.sort_order`), that is the same column, not a duplic
         -ate — §4.2's per-entity field lists                            
      47 -below name only what is *additional* to this base.             
      43 +This is the *only* universally-applied part (it is what PLAN.md
         + C2 actually requires: "every                                  
      44 +reference-data table stores `label_ar` and `label_en`, both `NO
         +T NULL`"). **`is_active` and                                   
      45 +`sort_order` are NOT part of this shared base** — PLAN.md §4.2 
         +lists them per table inconsistently                            
      46 +(e.g. `branches.is_active` exists, `roles.is_active` does not; 
         +`categories.sort_order` exists,                                
      47 +`sla_policies.sort_order` does not), so each is added only wher
         +e §4.2 explicitly names it for that                            
      48 +specific table, per this table:                                
      49  
      50 +| Table | `is_active`? | `sort_order`? |                       
      51 +|---|---|---|                                                  
      52 +| `branches` | ✓ | — |                                         
      53 +| `departments` | ✓ | — |                                      
      54 +| `roles` | — | — |                                            
      55 +| `permissions` | — | — |                                      
      56 +| `categories` | ✓ | ✓ |                                       
      57 +| `priorities` | — | — |                                       
      58 +| `ticket_statuses` | — | ✓ |                                  
      59 +| `sla_policies` | — | — |                                     
      60 +| `quick_replies` | — | — |                                    
      61 +| `teams` | — | — |                                            
      62 +                                                               
      63 +A table with no `is_active` column is **hard-deletable** (§0.6)
         + rather than soft-deactivatable —                              
      64 +there is no flag to toggle. This is not a gap; it is what "Admi
         +n CRUD" (PLAN.md F10) means for a                              
      65 +table PLAN.md never gave a deactivation flag: the "D" is a real
         + `DELETE`, guarded by `ON DELETE                               
      66 +RESTRICT` on every table that references it, so deleting a row 
         +still in use fails cleanly instead                             
      67 +of silently orphaning data.                                    
      68 +                                                               
      69  ### 0.5 Enumerated values — implemented as `TEXT` + `CHECK`, ne
          ver a native Postgres `ENUM` type
      70  
      71  PLAN.md writes several fields as `ENUM(...)`. They are implemen
          ted here as `TEXT` with a `CHECK

● Update(data-model.md)
  ⎿  Added 7 lines, removed 3 lines
      83  ### 0.6 Foreign key delete behavior (applied uniformly; not res
          tated per FK below)
      84  
      85  - FK to a **reference-data table** (branches, departments, role
          s, permissions, categories,
      86 -  priorities, ticket_statuses, sla_policies, quick_replies, tea
         -ms): `ON DELETE RESTRICT`. These                               
      87 -  rows are deactivated (`is_active = false`), never deleted, so
         - this only guards against a manual                             
      88 -  `DELETE` bypassing the service layer.                        
      86 +  priorities, ticket_statuses, sla_policies, quick_replies, tea
         +ms): `ON DELETE RESTRICT`. For the                             
      87 +  four with an `is_active` column (`branches`, `departments`, `
         +categories`, plus `users` and                                  
      88 +  `customers` elsewhere in this document), rows are deactivated
         + in normal operation and this only                             
      89 +  guards a manual `DELETE` bypassing the service layer. For the
         + five *without* one (`roles`,                                  
      90 +  `permissions`, `priorities`, `ticket_statuses`, `sla_policies
         +`, `quick_replies`, `teams`),                                  
      91 +  deletion is the real, exposed operation (§0.4), and `RESTRICT
         +` is what makes it safe: it fails                              
      92 +  the moment the row is actually referenced anywhere.          
      93  - FK from a true parent-owned child row (`ticket_events`→`ticke
          ts`, `contact_methods`→`customers`,
      94    `team_members`→`teams`, `kb_article_chunks`→`kb_articles`, `r
          ole_permissions`→`roles`/
      95    `permissions`): `ON DELETE CASCADE`. Defensive only — `custom
          ers` and `tickets` are never hard-

  Read 1 file (ctrl+o to expand)

● Now fixing roles/permissions footnotes, role_permissions' inconsistent
  no-updated_at carve-out, and adding delete semantics:

● Update(data-model.md)
  ⎿  Added 6 lines, removed 2 lines
      163  |---|---|---|---|
      164  | `code` | `TEXT` | NOT NULL, UNIQUE | `CHECK (code IN ('admin
           ','lead','agent','customer'))` |
      165  
      166 -*(+ reference-data base §0.4.)*                               
      166 +*(+ `label_ar`, `label_en` per §0.4. No `is_active`/`sort_orde
          +r` — not listed in PLAN.md §4.2 for                           
      167 +this table, so removal of a role is a hard `DELETE`, `RESTRICT
          +`-guarded by `user_roles`/                                    
      168 +`role_permissions` referencing it.)*                          
      169  
      170  ### 1.5 `permissions` — S6
      171  
     ...
      173  |---|---|---|---|
      174  | `code` | `TEXT` | NOT NULL, UNIQUE | dotted form, e.g. `tick
           et.assign` — see §4 for the seeded set |
      175  
      174 -*(+ reference-data base §0.4.)*                               
      176 +*(+ `label_ar`, `label_en` per §0.4. No `is_active`/`sort_orde
          +r`.)* Not in PLAN.md F10's "Admin                             
      177 +CRUD" list — this table is read-only through the API (seeded a
          +nd migration-managed only); only                              
      178 +*assigning* a permission to a role is admin-editable (`role_pe
          +rmissions`, below).                                           
      179  
      180  ### 1.6 `role_permissions` — S6
      181  

● Update(data-model.md)
  ⎿  Added 5 lines, removed 4 lines
      184  | `role_id` | `UUID` FK → `roles.id`, CASCADE | NOT NULL | |
      185  | `permission_id` | `UUID` FK → `permissions.id`, CASCADE | NO
           T NULL | |
      186  
      187 -**Index**: `(role_id, permission_id)` UNIQUE. No `updated_at`/
          -`updated_by` — this is a pure                                 
      188 -association row, recreated rather than edited; still not one o
          -f the four insert-only tables in                              
      189 -§0.3, but has no mutable fields of its own beyond the two FKs,
          - so no `updated_*` columns are                                
      190 -meaningful. `created_by`/`created_at` per §0.1 record who gran
          -ted it.                                                       
      187 +*(+ `updated_at`/`updated_by` per §0.2 — like every other muta
          +ble table; this is not one of the                             
      188 +four insert-only tables in §0.3, and PLAN.md's base-column rul
          +e draws no exception for join                                 
      189 +tables, so none is invented here either.)*                    
      190  
      191 +**Index**: `(role_id, permission_id)` UNIQUE.                 
      192 +                                                              
      193  ### 1.7 `user_roles` — S1
      194  
      195  | Column | Type | Null? | Notes |

● Now let's fix priorities, ticket_statuses, sla_policies, quick_replies, teams
  footnotes:
  Read 1 file (ctrl+o to expand)

● Update(data-model.md)
  ⎿  Added 2 lines, removed 1 line
      288  | `severity` | `INT` | NOT NULL | lower = more urgent |
      289  | `color` | `TEXT` | NOT NULL | |
      290  
      291 -*(+ reference-data base §0.4.)*                               
      291 +*(+ `label_ar`, `label_en` per §0.4. No `is_active`/`sort_orde
          +r` — hard `DELETE`, `RESTRICT`-                               
      292 +guarded by `tickets`/`sla_policies` referencing it.)*         
      293  
      294  ### 1.14 `ticket_statuses` — S2
      295  

● Update(data-model.md)
  ⎿  Added 3 lines, removed 3 lines
      301  | `is_terminal` | `BOOLEAN` | NOT NULL | |
      302  | `pauses_sla` | `BOOLEAN` | NOT NULL | |
      303  
      304 -*(+ reference-data base §0.4 — includes `sort_order`; `is_acti
          -ve` also applies here even though                             
      305 -PLAN.md §4.2's field list doesn't restate it, since `ticket_st
          -atuses` is a reference-data table by                          
      306 -§0.4's rule.)*                                                
      304 +*(+ `label_ar`, `label_en`, `sort_order` per §0.4. No `is_acti
          +ve` — PLAN.md §4.2 lists `sort_order`                         
      305 +for this table but not `is_active`, so a status is hard-`DELET
          +E`d, `RESTRICT`-guarded by                                    
      306 +`tickets`/`status_transitions` referencing it, not deactivated
          +.)*                                                           
      307  
      308  ### 1.15 `status_transitions` — S2 — **this table is the workf
           low engine (Principle XI)**
      309  

● Now let's fix sla_policies, quick_replies, teams, and the
  ticket_events.event_type enum:

  Read 1 file (ctrl+o to expand)

● Update(data-model.md)
  ⎿  Added 4 lines, removed 3 lines
      411  | `resolution_minutes` | `INT` | NOT NULL | |
      412  | `business_hours_only` | `BOOLEAN` | NOT NULL | default `fals
           e` |
      413  
      414 -*(+ reference-data base §0.4.)*                               
      414 +*(+ `label_ar`, `label_en` per §0.4. No `is_active`/`sort_orde
          +r` — hard `DELETE`, `RESTRICT`-                               
      415 +guarded by `tickets.sla_policy_id` referencing it.)*          
      416  
      417  **Index**: `(branch_id, department_id, category_id, priority_i
           d)` — backs the resolution-order
      418  query (exact match → priority-only → category-only → default; 
           FR-033).
     ...
      427  | `body_en` | `TEXT` | NOT NULL | |
      428  | `category_id` | `UUID` FK → `categories.id`, RESTRICT | NULL
            | |
      429  
      429 -*(+ reference-data base §0.4 — `label_ar`, `label_en` are the 
          -reply's display name, distinct                                
      430 -from `body_ar`/`body_en`, its inserted content.)*             
      430 +*(+ `label_ar`, `label_en` per §0.4 — the reply's display name
          +, distinct from `body_ar`/`body_en`,                          
      431 +its inserted content. No `is_active`/`sort_order` — hard `DELE
          +TE`.)*                                                        
      432  
      433  ### 1.21 `kb_articles` — **S2** (see `research.md` Part 2)
      434  

● Now find and fix the teams entity footnote:

  Read 1 file (ctrl+o to expand)

● Update(data-model.md)
  ⎿  Added 3 lines, removed 1 line
      211  | `branch_id` | `UUID` FK → `branches.id`, RESTRICT | NOT NULL
            | S1 scoping column |
      212  | `department_id` | `UUID` FK → `departments.id`, RESTRICT | N
           OT NULL | S1 scoping column |
      213  
      214 -*(+ reference-data base §0.4 — `label_ar`, `label_en`, `is_act
          -ive`, `sort_order`.)*                                         
      214 +*(+ `label_ar`, `label_en` per §0.4. No `is_active`/`sort_orde
          +r` — hard `DELETE`, `RESTRICT`-                               
      215 +guarded by `team_members` (CASCADE, so members are removed wit
          +h it) and `tickets.team_id`                                   
      216 +(RESTRICT, so a team with team-queued tickets cannot be delete
          +d until they are reassigned).)*                               
      217  
      218  ### 1.9 `team_members` — S4 (via `teams`)
      219  

● Update(data-model.md)
  ⎿  Added 4 lines, removed 3 lines
      211  | `branch_id` | `UUID` FK → `branches.id`, RESTRICT | NOT NULL
            | S1 scoping column |
      212  | `department_id` | `UUID` FK → `departments.id`, RESTRICT | N
           OT NULL | S1 scoping column |
      213  
      214 -*(+ `label_ar`, `label_en` per §0.4. No `is_active`/`sort_orde
          -r` — hard `DELETE`, `RESTRICT`-                               
      215 -guarded by `team_members` (CASCADE, so members are removed wit
          -h it) and `tickets.team_id`                                   
      216 -(RESTRICT, so a team with team-queued tickets cannot be delete
          -d until they are reassigned).)*                               
      214 +*(+ `label_ar`, `label_en` per §0.4. No `is_active`/`sort_orde
          +r` — hard `DELETE`. `team_members`                            
      215 +rows cascade-delete with it (§0.6); `tickets.team_id` is `SET 
          +NULL` (§1.16), so a team-queued                               
      216 +ticket survives its team being deleted, simply becoming unassi
          +gned-to-a-team rather than blocking                           
      217 +the deletion.)*                                               
      218  
      219  ### 1.9 `team_members` — S4 (via `teams`)
      220  

  Read 1 file (ctrl+o to expand)

● These are already accurate (branches and departments both keep is_active per
  the corrected table). Let me tighten the wording slightly for clarity and
  consistency with the other footnotes:

● Update(data-model.md)
  ⎿  Added 3 lines, removed 2 lines
      124  | `timezone` | `TEXT` (IANA name) | NOT NULL | |
      125  | `business_hours` | `JSONB` | NOT NULL | per-weekday open/clo
           se, e.g. `{"mon": {"open":"08:00","close":"17:00"}, ...}` |
      126  
      127 -*(+ reference-data base §0.4, incl. its own `is_active`.)*    
      127 +*(+ `label_ar`, `label_en`, `is_active` per §0.4 — no `sort_or
          +der`, not listed in PLAN.md §4.2 for                          
      128 +this table.)*                                                 
      129  
      130  No `branch_id`/`department_id` columns — S6. Top of the tenanc
           y hierarchy; nothing scopes *to* it
      131  via a parent join.
     ...
      137  | `branch_id` | `UUID` FK → `branches.id`, RESTRICT | NOT NULL
            | S3 scoping column |
      138  | `code` | `TEXT` | NOT NULL | UNIQUE per `(branch_id, code)` 
           |
      139  
      139 -*(+ reference-data base §0.4.)*                               
      140 +*(+ `label_ar`, `label_en`, `is_active` per §0.4 — no `sort_or
          +der`.)*                                                       
      141  
      142  **Index**: `(branch_id, code)` UNIQUE.
      143  

  Read 1 file (ctrl+o to expand)

● Good, categories is already correct (both is_active and sort_order are
  legitimately listed in PLAN.md §4.2 for this table). Now let's fix the
  priorities footnote and the ticket_events.event_type enum invention:

● Update(data-model.md)
  ⎿  Error editing file

  Read 1 file (ctrl+o to expand)

● Confirmed — already fixed. Now let's find and fix the ticket_events.event_type
  enum invention:

  Read 1 file (ctrl+o to expand)

● Update(data-model.md)
  ⎿  Added 1 line, removed 1 line
      373  |---|---|---|---|
      374  | `ticket_id` | `UUID` FK → `tickets.id`, CASCADE | NOT NULL |
            parent for S4 scoping join |
      375  | `actor_id` | `UUID` FK → `users.id`, RESTRICT | NULL | `NULL
           ` only for system-generated events (e.g. the SLA sweep's `sla_
           breached` event) |
      376 -| `event_type` | `TEXT` | NOT NULL | `CHECK (event_type IN ('c
          -reated','status_changed','assigned','reassigned','field_change
          -d','note_added','reply_sent','attachment_added','sla_breached'
          -,'reopened','ai_suggestion_applied','branch_corrected'))` — `b
          -ranch_corrected` added for FR-023c's needs-triage correction e
          -vent, a `field_changed`-shaped event that is distinct enough t
          -o name explicitly rather than overload `field_changed`'s gener
          -ic shape |                                                    
      376 +| `event_type` | `TEXT` | NOT NULL | `CHECK (event_type IN ('c
          +reated','status_changed','assigned','reassigned','field_change
          +d','note_added','reply_sent','attachment_added','sla_breached'
          +,'reopened','ai_suggestion_applied'))` — exactly PLAN.md §4.2'
          +s list, no value added. FR-023c's needs-triage correction (old
          +/new branch and department) is recorded as a `field_changed` e
          +vent with `field_name = 'branch_id'` or `'department_id'`, reu
          +sing the existing generic old/new-value shape rather than inve
          +nting a new `event_type`. |                                   
      377  | `field_name` | `TEXT` | NULL | |
      378  | `old_value` | `JSONB` | NULL | |
      379  | `new_value` | `JSONB` | NULL | |

● Now let's fix contracts/openapi.yaml to match: the TicketEvent enum, the
  ReferenceDataBase schema, and the deactivate→delete endpoint changes for the
  seven entities without is_active.

  Searched for 1 pattern, read 1 file (ctrl+o to expand)

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      915          id: { type: string, format: uuid }
      916          ticket_id: { type: string, format: uuid }
      917          actor_id: { type: string, format: uuid, nullable: true
            }
      918 -        event_type: { type: string, enum: [created, status_cha
          -nged, assigned, reassigned, field_changed, note_added, reply_s
          -ent, attachment_added, sla_breached, reopened, ai_suggestion_a
          -pplied, branch_corrected] }                                   
      918 +        event_type: { type: string, enum: [created, status_cha
          +nged, assigned, reassigned, field_changed, note_added, reply_s
          +ent, attachment_added, sla_breached, reopened, ai_suggestion_a
          +pplied], description: "Exactly PLAN.md §4.2's list. A needs-tr
          +iage branch/department correction (FR-023c) is a field_changed
          + event with field_name=branch_id or department_id, not a separ
          +ate type." }                                                  
      919          field_name: { type: string, nullable: true }
      920          old_value: {}
      921          new_value: {}
● Now let's look at the full schema block to fix ReferenceDataBase and the
  affected entity schemas:

  Read 1 file (ctrl+o to expand)

● Update(contracts\openapi.yaml)
  ⎿  Added 9 lines, removed 5 lines
      669      TokenPair: { type: object, required: [access_token, refres
           h_token], properties: { access_token: { type: string }, refres
           h_token: { type: string }, expires_in: { type: integer } } }
      670  
      671      ReferenceDataBase:
      672 +      description: >                                          
      673 +        Only label_ar/label_en are universal (PLAN.md C2). is_
          +active and sort_order are NOT part                            
      674 +        of this shared base — PLAN.md §4.2 lists them per tabl
          +e inconsistently, so each entity                              
      675 +        schema below adds them itself only where data-model.md
          + §0.4's table says it applies. An                             
      676 +        entity with no is_active property is hard-deletable (s
          +ee its DELETE operation), not                                 
      677 +        soft-deactivatable.                                   
      678        type: object
      673 -      required: [id, label_ar, label_en, is_active, sort_order
          -]                                                             
      679 +      required: [id, label_ar, label_en]                      
      680        properties:
      681          id: { type: string, format: uuid }
      682          label_ar: { type: string }
      683          label_en: { type: string }
      678 -        is_active: { type: boolean }                          
      679 -        sort_order: { type: integer }                         
      684  
      685      Branch:
      686        allOf:
      687          - $ref: "#/components/schemas/ReferenceDataBase"
      688          - type: object
      685 -          required: [code, timezone, business_hours]          
      686 -          properties: { code: { type: string }, timezone: { ty
          -pe: string }, business_hours: { type: object } }              
      689 +          required: [code, timezone, business_hours, is_active
          +]                                                             
      690 +          properties: { code: { type: string }, timezone: { ty
          +pe: string }, business_hours: { type: object }, is_active: { t
          +ype: boolean } }                                              
      691      BranchCreate: { type: object, required: [code, label_ar, l
           abel_en, timezone, business_hours], properties: { code: { type
           : string }, label_ar: { type: string }, label_en: { type: stri
           ng }, timezone: { type: string }, business_hours: { type: obje
           ct } } }
      692      BranchUpdate: { type: object, properties: { label_ar: { ty
           pe: string }, label_en: { type: string }, timezone: { type: st
           ring }, business_hours: { type: object } } }
      693  

● Update(contracts\openapi.yaml)
  ⎿  Added 2 lines, removed 2 lines
      695        allOf:
      696          - $ref: "#/components/schemas/ReferenceDataBase"
      697          - type: object
      698 -          required: [branch_id, code]                         
      699 -          properties: { branch_id: { type: string, format: uui
          -d }, code: { type: string } }                                 
      698 +          required: [branch_id, code, is_active]              
      699 +          properties: { branch_id: { type: string, format: uui
          +d }, code: { type: string }, is_active: { type: boolean } }   
      700      DepartmentCreate: { type: object, required: [branch_id, co
           de, label_ar, label_en], properties: { branch_id: { type: stri
           ng, format: uuid }, code: { type: string }, label_ar: { type: 
           string }, label_en: { type: string } } }
      701      DepartmentUpdate: { type: object, properties: { label_ar: 
           { type: string }, label_en: { type: string } } }
      702  

● Now Category needs both is_active and sort_order:

● Update(contracts\openapi.yaml)
  ⎿  Added 2 lines, removed 2 lines
      738        allOf:
      739          - $ref: "#/components/schemas/ReferenceDataBase"
      740          - type: object
      741 -          required: [branch_id]                               
      742 -          properties: { branch_id: { type: string, format: uui
          -d }, department_id: { type: string, format: uuid, nullable: tr
          -ue }, parent_id: { type: string, format: uuid, nullable: true 
          -} }                                                           
      741 +          required: [branch_id, is_active, sort_order]        
      742 +          properties: { branch_id: { type: string, format: uui
          +d }, department_id: { type: string, format: uuid, nullable: tr
          +ue }, parent_id: { type: string, format: uuid, nullable: true 
          +}, is_active: { type: boolean }, sort_order: { type: integer }
          + }                                                            
      743      CategoryCreate: { type: object, required: [branch_id, labe
           l_ar, label_en], properties: { branch_id: { type: string, form
           at: uuid }, department_id: { type: string, format: uuid, nulla
           ble: true }, parent_id: { type: string, format: uuid, nullable
           : true }, label_ar: { type: string }, label_en: { type: string
            }, sort_order: { type: integer } } }
      744      CategoryUpdate: { type: object, properties: { label_ar: { 
           type: string }, label_en: { type: string }, parent_id: { type:
            string, format: uuid, nullable: true }, sort_order: { type: i
           nteger } } }
      745  

● Now TicketStatus needs sort_order only:

● Update(contracts\openapi.yaml)
  ⎿  Added 3 lines, removed 3 lines
      756        allOf:
      757          - $ref: "#/components/schemas/ReferenceDataBase"
      758          - type: object
      759 -          required: [branch_id, code, is_terminal, pauses_sla]
      760 -          properties: { branch_id: { type: string, format: uui
          -d }, department_id: { type: string, format: uuid, nullable: tr
          -ue }, code: { type: string }, is_terminal: { type: boolean }, 
          -pauses_sla: { type: boolean } }                               
      761 -    TicketStatusCreate: { type: object, required: [branch_id, 
          -code, label_ar, label_en, is_terminal, pauses_sla], properties
          -: { branch_id: { type: string, format: uuid }, department_id: 
          -{ type: string, format: uuid, nullable: true }, code: { type: 
          -string }, label_ar: { type: string }, label_en: { type: string
          - }, is_terminal: { type: boolean }, pauses_sla: { type: boolea
          -n } } }                                                       
      759 +          required: [branch_id, code, is_terminal, pauses_sla,
          + sort_order]                                                  
      760 +          properties: { branch_id: { type: string, format: uui
          +d }, department_id: { type: string, format: uuid, nullable: tr
          +ue }, code: { type: string }, is_terminal: { type: boolean }, 
          +pauses_sla: { type: boolean }, sort_order: { type: integer } }
      761 +    TicketStatusCreate: { type: object, required: [branch_id, 
          +code, label_ar, label_en, is_terminal, pauses_sla], properties
          +: { branch_id: { type: string, format: uuid }, department_id: 
          +{ type: string, format: uuid, nullable: true }, code: { type: 
          +string }, label_ar: { type: string }, label_en: { type: string
          + }, is_terminal: { type: boolean }, pauses_sla: { type: boolea
          +n }, sort_order: { type: integer } } }                        
      762      TicketStatusUpdate: { type: object, properties: { label_ar
           : { type: string }, label_en: { type: string } } }
      763  
      764      StatusTransition:

  Read 1 file (ctrl+o to expand)

● Good, Team is already clean. Now let's replace the five AdminDeactivate_* path
  items (Priority, TicketStatus, SlaPolicy, QuickReply, Team) with delete
  operations, and add one for Role too. Let's view them in context:

  Read 1 file (ctrl+o to expand)

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line
      1018      AdminCreate_Role: { post: { tags: [admin], operationId: c
            reateRole, x-permission: admin.config, requestBody: { require
            d: true, content: { application/json: { schema: { $ref: "#/co
            mponents/schemas/RoleCreate" } } } }, responses: { "201": { d
            escription: Created, content: { application/json: { schema: {
             $ref: "#/components/schemas/Role" } } } } } } }
      1019      AdminGet_Role: { get: { tags: [admin], operationId: getRo
            le, x-permission: admin.config, parameters: [{ $ref: "#/compo
            nents/parameters/PathId" }], responses: { "200": { descriptio
            n: OK, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/Role" } } } } } } }
      1020      AdminUpdate_Role: { patch: { tags: [admin], operationId: 
            updateRole, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], requestBody: { required:
             true, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/RoleUpdate" } } } }, responses: { "200": { des
            cription: OK, content: { application/json: { schema: { $ref: 
            "#/components/schemas/Role" } } } } } } }
      1021 +    AdminDelete_Role: { delete: { tags: [admin], operationId:
           + deleteRole, x-permission: admin.config, description: "No is_
           +active column (data-model.md §0.4) — hard delete, RESTRICT-gu
           +arded by user_roles/role_permissions.", parameters: [{ $ref: 
           +"#/components/parameters/PathId" }], responses: { "204": { de
           +scription: Deleted } } } }                                   
      1022  
      1023      AdminList_Category: { get: { tags: [admin], operationId: 
            listCategories, x-permission: admin.config, parameters: [{ $r
            ef: "#/components/parameters/Limit" }, { $ref: "#/components/
            parameters/Offset" }], responses: { "200": { description: OK,
             content: { application/json: { schema: { type: array, items:
             { $ref: "#/components/schemas/Category" } } } } } } } }
      1024      AdminCreate_Category: { post: { tags: [admin], operationI
            d: createCategory, x-permission: admin.config, requestBody: {
             required: true, content: { application/json: { schema: { $re
            f: "#/components/schemas/CategoryCreate" } } } }, responses: 
            { "201": { description: Created, content: { application/json:
             { schema: { $ref: "#/components/schemas/Category" } } } } } 
            } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1030      AdminCreate_Priority: { post: { tags: [admin], operationI
            d: createPriority, x-permission: admin.config, requestBody: {
             required: true, content: { application/json: { schema: { $re
            f: "#/components/schemas/PriorityCreate" } } } }, responses: 
            { "201": { description: Created, content: { application/json:
             { schema: { $ref: "#/components/schemas/Priority" } } } } } 
            } }
      1031      AdminGet_Priority: { get: { tags: [admin], operationId: g
            etPriority, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], responses: { "200": { de
            scription: OK, content: { application/json: { schema: { $ref:
             "#/components/schemas/Priority" } } } } } } }
      1032      AdminUpdate_Priority: { patch: { tags: [admin], operation
            Id: updatePriority, x-permission: admin.config, parameters: [
            { $ref: "#/components/parameters/PathId" }], requestBody: { r
            equired: true, content: { application/json: { schema: { $ref:
             "#/components/schemas/PriorityUpdate" } } } }, responses: { 
            "200": { description: OK, content: { application/json: { sche
            ma: { $ref: "#/components/schemas/Priority" } } } } } } }
      1033 -    AdminDeactivate_Priority: { post: { tags: [admin], operat
           -ionId: deactivatePriority, x-permission: admin.config, parame
           -ters: [{ $ref: "#/components/parameters/PathId" }], responses
           -: { "200": { description: OK, content: { application/json: { 
           -schema: { $ref: "#/components/schemas/Priority" } } } } } } }
      1033 +    AdminDelete_Priority: { delete: { tags: [admin], operatio
           +nId: deletePriority, x-permission: admin.config, description:
           + "No is_active column — hard delete, RESTRICT-guarded by tick
           +ets/sla_policies referencing it.", parameters: [{ $ref: "#/co
           +mponents/parameters/PathId" }], responses: { "204": { descrip
           +tion: Deleted } } } }                                        
      1034  
      1035      AdminList_TicketStatus: { get: { tags: [admin], operation
            Id: listTicketStatuses, x-permission: admin.config, responses
            : { "200": { description: OK, content: { application/json: { 
            schema: { type: array, items: { $ref: "#/components/schemas/T
            icketStatus" } } } } } } } }
      1036      AdminCreate_TicketStatus: { post: { tags: [admin], operat
            ionId: createTicketStatus, x-permission: admin.config, reques
            tBody: { required: true, content: { application/json: { schem
            a: { $ref: "#/components/schemas/TicketStatusCreate" } } } },
             responses: { "201": { description: Created, content: { appli
            cation/json: { schema: { $ref: "#/components/schemas/TicketSt
            atus" } } } } } } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1036      AdminCreate_TicketStatus: { post: { tags: [admin], operat
            ionId: createTicketStatus, x-permission: admin.config, reques
            tBody: { required: true, content: { application/json: { schem
            a: { $ref: "#/components/schemas/TicketStatusCreate" } } } },
             responses: { "201": { description: Created, content: { appli
            cation/json: { schema: { $ref: "#/components/schemas/TicketSt
            atus" } } } } } } }
      1037      AdminGet_TicketStatus: { get: { tags: [admin], operationI
            d: getTicketStatus, x-permission: admin.config, parameters: [
            { $ref: "#/components/parameters/PathId" }], responses: { "20
            0": { description: OK, content: { application/json: { schema:
             { $ref: "#/components/schemas/TicketStatus" } } } } } } }
      1038      AdminUpdate_TicketStatus: { patch: { tags: [admin], opera
            tionId: updateTicketStatus, x-permission: admin.config, param
            eters: [{ $ref: "#/components/parameters/PathId" }], requestB
            ody: { required: true, content: { application/json: { schema:
             { $ref: "#/components/schemas/TicketStatusUpdate" } } } }, r
            esponses: { "200": { description: OK, content: { application/
            json: { schema: { $ref: "#/components/schemas/TicketStatus" }
             } } } } } }
      1039 -    AdminDeactivate_TicketStatus: { post: { tags: [admin], op
           -erationId: deactivateTicketStatus, x-permission: admin.config
           -, parameters: [{ $ref: "#/components/parameters/PathId" }], r
           -esponses: { "200": { description: OK, content: { application/
           -json: { schema: { $ref: "#/components/schemas/TicketStatus" }
           - } } } } } }                                                 
      1039 +    AdminDelete_TicketStatus: { delete: { tags: [admin], oper
           +ationId: deleteTicketStatus, x-permission: admin.config, desc
           +ription: "No is_active column — hard delete, RESTRICT-guarded
           + by tickets/status_transitions referencing it.", parameters: 
           +[{ $ref: "#/components/parameters/PathId" }], responses: { "2
           +04": { description: Deleted } } } }                          
      1040  
      1041      AdminList_StatusTransition: { get: { tags: [admin], opera
            tionId: listStatusTransitions, x-permission: admin.config, re
            sponses: { "200": { description: OK, content: { application/j
            son: { schema: { type: array, items: { $ref: "#/components/sc
            hemas/StatusTransition" } } } } } } } }
      1042      AdminCreate_StatusTransition: { post: { tags: [admin], op
            erationId: createStatusTransition, x-permission: admin.config
            , requestBody: { required: true, content: { application/json:
             { schema: { $ref: "#/components/schemas/StatusTransitionCrea
            te" } } } }, responses: { "201": { description: Created, cont
            ent: { application/json: { schema: { $ref: "#/components/sche
            mas/StatusTransition" } } } } } } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1046      AdminCreate_SlaPolicy: { post: { tags: [admin], operation
            Id: createSlaPolicy, x-permission: admin.config, requestBody:
             { required: true, content: { application/json: { schema: { $
            ref: "#/components/schemas/SlaPolicyCreate" } } } }, response
            s: { "201": { description: Created, content: { application/js
            on: { schema: { $ref: "#/components/schemas/SlaPolicy" } } } 
            } } } }
      1047      AdminGet_SlaPolicy: { get: { tags: [admin], operationId: 
            getSlaPolicy, x-permission: admin.config, parameters: [{ $ref
            : "#/components/parameters/PathId" }], responses: { "200": { 
            description: OK, content: { application/json: { schema: { $re
            f: "#/components/schemas/SlaPolicy" } } } } } } }
      1048      AdminUpdate_SlaPolicy: { patch: { tags: [admin], operatio
            nId: updateSlaPolicy, x-permission: admin.config, parameters:
             [{ $ref: "#/components/parameters/PathId" }], requestBody: {
             required: true, content: { application/json: { schema: { $re
            f: "#/components/schemas/SlaPolicyUpdate" } } } }, responses:
             { "200": { description: OK, content: { application/json: { s
            chema: { $ref: "#/components/schemas/SlaPolicy" } } } } } } }
      1049 -    AdminDeactivate_SlaPolicy: { post: { tags: [admin], opera
           -tionId: deactivateSlaPolicy, x-permission: admin.config, para
           -meters: [{ $ref: "#/components/parameters/PathId" }], respons
           -es: { "200": { description: OK, content: { application/json: 
           -{ schema: { $ref: "#/components/schemas/SlaPolicy" } } } } } 
           -} }                                                          
      1049 +    AdminDelete_SlaPolicy: { delete: { tags: [admin], operati
           +onId: deleteSlaPolicy, x-permission: admin.config, descriptio
           +n: "No is_active column — hard delete, RESTRICT-guarded by ti
           +ckets.sla_policy_id referencing it.", parameters: [{ $ref: "#
           +/components/parameters/PathId" }], responses: { "204": { desc
           +ription: Deleted } } } }                                     
      1050  
      1051      AdminList_QuickReply: { get: { tags: [admin], operationId
            : listQuickReplies, x-permission: admin.config, responses: { 
            "200": { description: OK, content: { application/json: { sche
            ma: { type: array, items: { $ref: "#/components/schemas/Quick
            Reply" } } } } } } } }
      1052      AdminCreate_QuickReply: { post: { tags: [admin], operatio
            nId: createQuickReply, x-permission: admin.config, requestBod
            y: { required: true, content: { application/json: { schema: {
             $ref: "#/components/schemas/QuickReplyCreate" } } } }, respo
            nses: { "201": { description: Created, content: { application
            /json: { schema: { $ref: "#/components/schemas/QuickReply" } 
            } } } } } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1052      AdminCreate_QuickReply: { post: { tags: [admin], operatio
            nId: createQuickReply, x-permission: admin.config, requestBod
            y: { required: true, content: { application/json: { schema: {
             $ref: "#/components/schemas/QuickReplyCreate" } } } }, respo
            nses: { "201": { description: Created, content: { application
            /json: { schema: { $ref: "#/components/schemas/QuickReply" } 
            } } } } } }
      1053      AdminGet_QuickReply: { get: { tags: [admin], operationId:
             getQuickReply, x-permission: admin.config, parameters: [{ $r
            ef: "#/components/parameters/PathId" }], responses: { "200": 
            { description: OK, content: { application/json: { schema: { $
            ref: "#/components/schemas/QuickReply" } } } } } } }
      1054      AdminUpdate_QuickReply: { patch: { tags: [admin], operati
            onId: updateQuickReply, x-permission: admin.config, parameter
            s: [{ $ref: "#/components/parameters/PathId" }], requestBody:
             { required: true, content: { application/json: { schema: { $
            ref: "#/components/schemas/QuickReplyUpdate" } } } }, respons
            es: { "200": { description: OK, content: { application/json: 
            { schema: { $ref: "#/components/schemas/QuickReply" } } } } }
             } }
      1055 -    AdminDeactivate_QuickReply: { post: { tags: [admin], oper
           -ationId: deactivateQuickReply, x-permission: admin.config, pa
           -rameters: [{ $ref: "#/components/parameters/PathId" }], respo
           -nses: { "200": { description: OK, content: { application/json
           -: { schema: { $ref: "#/components/schemas/QuickReply" } } } }
           - } } }                                                       
      1055 +    AdminDelete_QuickReply: { delete: { tags: [admin], operat
           +ionId: deleteQuickReply, x-permission: admin.config, descript
           +ion: "No is_active column — hard delete.", parameters: [{ $re
           +f: "#/components/parameters/PathId" }], responses: { "204": {
           + description: Deleted } } } }                                
      1056  
      1057      AdminList_Team: { get: { tags: [admin], operationId: list
            Teams, x-permission: admin.config, responses: { "200": { desc
            ription: OK, content: { application/json: { schema: { type: a
            rray, items: { $ref: "#/components/schemas/Team" } } } } } } 
            } }
      1058      AdminCreate_Team: { post: { tags: [admin], operationId: c
            reateTeam, x-permission: admin.config, requestBody: { require
            d: true, content: { application/json: { schema: { $ref: "#/co
            mponents/schemas/TeamCreate" } } } }, responses: { "201": { d
            escription: Created, content: { application/json: { schema: {
             $ref: "#/components/schemas/Team" } } } } } } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1058      AdminCreate_Team: { post: { tags: [admin], operationId: c
            reateTeam, x-permission: admin.config, requestBody: { require
            d: true, content: { application/json: { schema: { $ref: "#/co
            mponents/schemas/TeamCreate" } } } }, responses: { "201": { d
            escription: Created, content: { application/json: { schema: {
             $ref: "#/components/schemas/Team" } } } } } } }
      1059      AdminGet_Team: { get: { tags: [admin], operationId: getTe
            am, x-permission: admin.config, parameters: [{ $ref: "#/compo
            nents/parameters/PathId" }], responses: { "200": { descriptio
            n: OK, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/Team" } } } } } } }
      1060      AdminUpdate_Team: { patch: { tags: [admin], operationId: 
            updateTeam, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], requestBody: { required:
             true, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/TeamUpdate" } } } }, responses: { "200": { des
            cription: OK, content: { application/json: { schema: { $ref: 
            "#/components/schemas/Team" } } } } } } }
      1061 -    AdminDeactivate_Team: { post: { tags: [admin], operationI
           -d: deactivateTeam, x-permission: admin.config, parameters: [{
           - $ref: "#/components/parameters/PathId" }], responses: { "200
           -": { description: OK, content: { application/json: { schema: 
           -{ $ref: "#/components/schemas/Team" } } } } } } }            
      1061 +    AdminDelete_Team: { delete: { tags: [admin], operationId:
           + deleteTeam, x-permission: admin.config, description: "No is_
           +active column — hard delete. team_members cascade with it; ti
           +ckets.team_id is SET NULL, not blocked.", parameters: [{ $ref
           +: "#/components/parameters/PathId" }], responses: { "204": { 
           +description: Deleted } } } }                                 
      1062  
      1063  security:
      1064    - BearerAuth: []

● Now update the paths: section references to these renamed pathItems, and add
  the new /roles/{id} delete:

  Searched for 1 pattern, read 1 file (ctrl+o to expand)

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line
      114    /roles/{id}:
      115      get: { $ref: "#/components/pathItems/AdminGet_Role" }
      116      patch: { $ref: "#/components/pathItems/AdminUpdate_Role" }
      117 +    delete: { $ref: "#/components/pathItems/AdminDelete_Role" 
          +}                                                             
      118    /roles/{id}/permissions:
      119      get:
      120        tags: [admin]

● Now the five deactivate paths → delete-on-existing-{id}-path pattern:

  Read 1 file (ctrl+o to expand)

● Update(contracts\openapi.yaml)
  ⎿  Added 9 lines, removed 13 lines
      152    /priorities/{id}:
      153      get: { $ref: "#/components/pathItems/AdminGet_Priority" }
      154      patch: { $ref: "#/components/pathItems/AdminUpdate_Priorit
           y" }
      155 -  /priorities/{id}/deactivate:                                
      156 -    post: { $ref: "#/components/pathItems/AdminDeactivate_Prio
          -rity" }                                                       
      155 +    delete: { $ref: "#/components/pathItems/AdminDelete_Priori
          +ty" }                                                         
      156  
      157    /ticket-statuses:
      158      get: { $ref: "#/components/pathItems/AdminList_TicketStatu
           s" }
     ...
      160    /ticket-statuses/{id}:
      161      get: { $ref: "#/components/pathItems/AdminGet_TicketStatus
           " }
      162      patch: { $ref: "#/components/pathItems/AdminUpdate_TicketS
           tatus" }
      164 -  /ticket-statuses/{id}/deactivate:                           
      165 -    post: { $ref: "#/components/pathItems/AdminDeactivate_Tick
          -etStatus" }                                                   
      163 +    delete: { $ref: "#/components/pathItems/AdminDelete_Ticket
          +Status" }                                                     
      164  
      165    /status-transitions:
      166      get: { $ref: "#/components/pathItems/AdminList_StatusTrans
           ition" }
     ...
      172        operationId: deleteStatusTransition
      173        x-permission: admin.config
      174        description: >
      177 -        Hard delete is permitted here (unlike every other refe
          -rence-data entity) because a stray                            
      178 -        transition row is a workflow-configuration mistake, no
          -t operational history — removing one                          
      179 -        does not orphan any ticket, since tickets reference st
          -atuses, never transition rows.                                
      175 +        Hard delete, RESTRICT-guarded — a stray transition row
          + is a workflow-configuration                                  
      176 +        mistake, not operational history; removing one does no
          +t orphan any ticket, since tickets                            
      177 +        reference statuses, never transition rows. Several oth
          +er reference-data entities are also                           
      178 +        hard-deletable (data-model.md §0.4) — this is not the 
          +only one.                                                     
      179        parameters: [{ $ref: "#/components/parameters/PathId" }]
      180        responses: { "204": { description: Deleted } }
      181  
     ...
      185    /sla-policies/{id}:
      186      get: { $ref: "#/components/pathItems/AdminGet_SlaPolicy" }
      187      patch: { $ref: "#/components/pathItems/AdminUpdate_SlaPoli
           cy" }
      189 -  /sla-policies/{id}/deactivate:                              
      190 -    post: { $ref: "#/components/pathItems/AdminDeactivate_SlaP
          -olicy" }                                                      
      188 +    delete: { $ref: "#/components/pathItems/AdminDelete_SlaPol
          +icy" }                                                        
      189  
      190    /quick-replies:
      191      get: { $ref: "#/components/pathItems/AdminList_QuickReply"
            }
     ...
      193    /quick-replies/{id}:
      194      get: { $ref: "#/components/pathItems/AdminGet_QuickReply" 
           }
      195      patch: { $ref: "#/components/pathItems/AdminUpdate_QuickRe
           ply" }
      198 -  /quick-replies/{id}/deactivate:                             
      199 -    post: { $ref: "#/components/pathItems/AdminDeactivate_Quic
          -kReply" }                                                     
      196 +    delete: { $ref: "#/components/pathItems/AdminDelete_QuickR
          +eply" }                                                       
      197  
      198    /teams:
      199      get: { $ref: "#/components/pathItems/AdminList_Team" }
     ...
      201    /teams/{id}:
      202      get: { $ref: "#/components/pathItems/AdminGet_Team" }
      203      patch: { $ref: "#/components/pathItems/AdminUpdate_Team" }
      207 -  /teams/{id}/deactivate:                                     
      208 -    post: { $ref: "#/components/pathItems/AdminDeactivate_Team
          -" }                                                           
      204 +    delete: { $ref: "#/components/pathItems/AdminDelete_Team" 
          +}                                                             
      205    /teams/{id}/members:
      206      post:
      207        tags: [admin]

● Now let's re-validate the YAML and check for dangling refs after all these
  edits:
  Ran 1 shell command (ctrl+o to expand)

● All clean — only the four is_active-bearing entities (Branch, Department,
  User, Category) still use deactivate; everything else is now a proper DELETE.
  Now let's write plan.md.

  Read 1 file (ctrl+o to expand)

● Now writing plan.md — the master document with the file tree, five
  abstractions, service signatures, and the final compliance audit.

● Write(plan.md)
  ⎿  Added 617 lines, removed 76 lines
        1 -# Implementation Plan: [FEATURE]                              
        1 +# Implementation Plan: Bilingual Support CRM — Core Product   
        2  
        3 -**Branch**: `[###-feature-name]` | **Date**: [DATE] | **Spec**
          -: [link]                                                      
        3 +**Branch**: `001-bilingual-support-crm` | **Date**: 2026-08-25
          + | **Spec**: [spec.md](./spec.md)                             
        4  
        5 -**Input**: Feature specification from `/specs/[###-feature-nam
          -e]/spec.md`                                                   
        5 +**Input**: Feature specification from `specs/001-bilingual-sup
          +port-crm/spec.md`, `PLAN.md`,                                 
        6 +`docs/architecture/stack.md`, `.specify/memory/constitution.md
          +`.                                                            
        7  
        7 -**Note**: This template is filled in by the `/speckit.plan` co
          -mmand; its definition describes the execution workflow.       
        8 -                                                              
        8  ## Summary
        9  
       11 -[Extract from feature spec: primary requirement + technical ap
          -proach from research]                                         
       10 +Build the Tier M/S surface of PLAN.md's twelve feature areas a
          +s one FastAPI backend (Python                                 
       11 +3.12, SQLAlchemy async, PostgreSQL 16) and one Next.js 15 fron
          +tend, on Docker Compose. Every                                
       12 +constitutional guarantee (bilingual, RTL, tenant scoping, immu
          +table audit/timeline, AI gateway                              
       13 +abstraction, deterministic AI fallback, service-layer permissi
          +ons, data-driven workflow, stateless                          
       14 +SLA) is carried by five shared abstractions (§Shared Abstracti
          +ons) that every other module is                               
       15 +built on top of — not re-implemented per feature. Tier D capab
          +ilities are schema-accommodated only                          
       16 +(`data-model.md`, `research.md` Part 3) and generate no tasks 
          +(Principle XIII).                                             
       17  
       18  ## Technical Context
       19  
       15 -<!--                                                          
       16 -  ACTION REQUIRED: Replace the content in this section with th
          -e technical details                                           
       17 -  for the project. The structure here is presented in advisory
          - capacity to guide                                            
       18 -  the iteration process.                                      
       19 --->                                                           
       20 +| | |                                                         
       21 +|---|---|                                                     
       22 +| **Language/Version** | Python 3.12 (backend), TypeScript 5.6
          + (frontend) |                                                 
       23 +| **Primary Dependencies** | FastAPI 0.115, SQLAlchemy 2.0 (as
          +ync), Alembic 1.14, Pydantic 2.9, Uvicorn 0.32, ARQ 0.26, http
          +x 0.27, LiteLLM · Next.js 15 (App Router), Tailwind 3.4, shadc
          +n/ui, next-intl 3, TanStack Query 5, react-hook-form + zod |  
       24 +| **Storage** | PostgreSQL 16 (+ `pgvector` 0.7, `pg_trgm`), R
          +edis 7 (ARQ broker), MinIO (S3 API) |                         
       25 +| **Testing** | pytest (branching business logic only — status
          + transitions, permission checks, SLA computation, tenant scopi
          +ng; Principle XIV) · bilingual golden set of 20 tickets, score
          +d by script, for AI features · Schemathesis against `contracts
          +/openapi.yaml`, time-permitting |                             
       26 +| **Target Platform** | Docker Compose, single host (laptop de
          +v now, on-prem server later — identical architecture, `docs/ar
          +chitecture/stack.md`) |                                       
       27 +| **Project Type** | Web application (backend + frontend) |   
       28 +| **Performance Goals** | Not independently set by this plan —
          + PLAN.md and `spec.md` define no throughput target; the only s
          +tated latency constraint is FR-049 (AI never blocks ticket cre
          +ation) and F06's 200–400 ms/query embedding search |          
       29 +| **Constraints** | No external network dependency at runtime 
          +except the configured LLM endpoint (PLAN.md); every AI feature
          + must degrade cleanly offline (FR-009) |                      
       30 +| **Scale/Scope** | Sprint seed data only (PLAN.md §7): 2 bran
          +ches, 3 departments, 5 users, 20 customers, 40 tickets, 10 KB 
          +articles — this plan does not size for production load |      
       31  
       21 -**Language/Version**: [e.g., Python 3.11, Swift 5.9, Rust 1.75
          - or NEEDS CLARIFICATION]                                      
       32 +No `NEEDS CLARIFICATION` remains — `docs/architecture/stack.md
          +` fully specifies the stack, and                              
       33 +`spec.md`'s three clarifications (channel routing, SLA overrid
          +e bounds, cross-branch permission)                            
       34 +were already resolved before this plan was written.           
       35  
       23 -**Primary Dependencies**: [e.g., FastAPI, UIKit, LLVM or NEEDS
          - CLARIFICATION]                                               
       36 +## Constitution Check                                         
       37  
       25 -**Storage**: [if applicable, e.g., PostgreSQL, CoreData, files
          - or N/A]                                                      
       38 +*GATE: checked before Phase 0 research below, and re-checked a
          +fter Phase 1 design in                                        
       39 +§Post-Design Constitution Check & Self-Audit at the end of thi
          +s document.*                                                  
       40  
       27 -**Testing**: [e.g., pytest, XCTest, cargo test or NEEDS CLARIF
          -ICATION]                                                      
       41 +| Principle | How this plan satisfies it |                    
       42 +|---|---|                                                     
       43 +| I. Bilingual String Externalization | `messages/ar.json`/`me
          +ssages/en.json` (next-intl) on the frontend; every API error u
          +ses `ErrorResponse{message_ar, message_en}` (`contracts/openap
          +i.yaml`) — no literal user-facing string in either codebase | 
       44 +| II. Reference-Data Bilingual Completeness | `data-model.md` 
          +§0.4 — `label_ar`/`label_en` `NOT NULL` on every reference-dat
          +a table |                                                     
       45 +| III. Structural RTL/LTR Parity | Tailwind logical properties
          + only (`docs/architecture/stack.md`); `<LtrText>` component fo
          +r reference numbers/emails/phones/URLs, per the constitution's
          + `rtl-exempt:` exception mechanism |                          
       46 +| IV. Universal Tenant Attribution | `data-model.md` §2 — ever
          +y table has exactly one of the six patterns, exhaustively |   
       47 +| V. Repository-Layer Tenant Scoping | `ScopedRepository` (§Sh
          +ared Abstractions #1) is the *only* place any scope filter is 
          +written |                                                     
       48 +| VI. Immutable Event & Audit Trails | `alembic/versions/0001_
          +initial.py` revokes `UPDATE`/`DELETE` grants on the four inser
          +t-only tables (`data-model.md` §0.3) at the DB role level, bac
          +ked by a trigger |                                            
       49 +| VII. Atomic Audit Writes | `audited` decorator (§Shared Abst
          +ractions #3) writes inside the same session/transaction as the
          + mutation it wraps |                                          
       50 +| VIII. Single AI Gateway | `LiteLlmWrapper` (§Shared Abstract
          +ions #5) is the only module importing an LLM HTTP client; `app
          +/ai/*` and `app/channels/*` never import a vendor SDK — enforc
          +ed by an import-lint rule (`app/core/lint_no_vendor_sdk.py`, r
          +un in `pyproject.toml`'s lint step) |                         
       51 +| IX. Deterministic AI Degradation | Every `AiService` method 
          +catches `LiteLlmWrapper`'s own internal failure (it never rais
          +es — see #5) and returns the documented fallback; `contracts/o
          +penapi.yaml`'s `fallback_used` field on every AI response make
          +s this observable |                                           
       52 +| X. Service-Layer Permission Enforcement | `require_permissio
          +n` (§Shared Abstractions #2) wraps *service* methods, never ro
          +ute handlers — route handlers cannot bypass it by construction
          + |                                                            
       53 +| XI. Data-Driven Status Transitions | `TicketTransitionServic
          +e.change_status` (§Service Classes) issues exactly one query a
          +gainst `status_transitions`; no status code appears in an `if`
          +/`match` anywhere in the plan |                               
       54 +| XII. Stateless SLA Derivation | `SlaService.compute_due_date
          +s`/`compute_breach_state` are pure functions over stored field
          +s — `data-model.md` has no SLA-state column to hold state in |
       55 +| XIII. Scope Discipline | `research.md` Part 3; Tier D items 
          +get zero entries in the file tree below and zero service metho
          +ds |                                                          
       56 +| XIV. Testing Proportionality | `tests/unit/` targets exactly
          + the branching logic named in Technical Context's Testing row;
          + `tests/golden/` for AI; no test scaffolding for generic CRUD 
          +|                                                             
       57  
       29 -**Target Platform**: [e.g., Linux server, iOS 15+, WASM or NEE
          -DS CLARIFICATION]                                             
       58 +No violations — **Complexity Tracking is empty.**             
       59  
       31 -**Project Type**: [e.g., library/cli/web-service/mobile-app/co
          -mpiler/desktop-app or NEEDS CLARIFICATION]                    
       60 +## Project Structure                                          
       61  
       33 -**Performance Goals**: [domain-specific, e.g., 1000 req/s, 10k
          - lines/sec, 60 fps or NEEDS CLARIFICATION]                    
       62 +### Documentation (this feature)                              
       63  
       35 -**Constraints**: [domain-specific, e.g., <200ms p95, <100MB me
          -mory, offline-capable or NEEDS CLARIFICATION]                 
       64 +```text                                                       
       65 +specs/001-bilingual-support-crm/                              
       66 +├── plan.md              # This file                          
       67 +├── research.md          # Phase 0 output                     
       68 +├── data-model.md         # Phase 1 output                    
       69 +├── quickstart.md         # Phase 1 output                    
       70 +├── contracts/                                                
       71 +│   └── openapi.yaml      # Phase 1 output — all Tier M/S endp
          +oints                                                         
       72 +└── tasks.md               # Phase 2 output (/speckit.tasks — 
          +not created by this command)                                  
       73 +```                                                           
       74  
       37 -**Scale/Scope**: [domain-specific, e.g., 10k users, 1M LOC, 50
          - screens or NEEDS CLARIFICATION]                              
       75 +### Source Code (repository root)                             
       76  
       39 -## Constitution Check                                         
       77 +```text                                                       
       78 +backend/                                                      
       79 +├── alembic/                                                  
       80 +│   ├── env.py                          # Alembic runtime conf
          +ig (async engine)                                             
       81 +│   └── versions/                                             
       82 +│       └── 0001_initial.py             # Full schema from dat
          +a-model.md, incl. DB-role grant revocation for the four insert
          +-only tables (C6)                                             
       83 +├── app/                                                      
       84 +│   ├── main.py                         # FastAPI app factory;
          + mounts app.api.router; structlog + correlation-id middleware 
       85 +│   ├── config.py                       # Pydantic Settings: D
          +B/Redis/MinIO URLs, LITELLM_*, JWT secrets, SYSTEM_DEFAULT_BRA
          +NCH_ID/DEPARTMENT_ID                                          
       86 +│   ├── db.py                           # Async engine/session
          + factory; get_session() dependency (one transaction per reques
          +t)                                                            
       87 +│   ├── models/                         # SQLAlchemy 2.0 ORM m
          +odels — one file per data-model.md entity, no business logic  
       88 +│   │   ├── base.py                     # Base, id/created_at/
          +created_by mixin (§0.1), updated_at/updated_by mixin (§0.2)   
       89 +│   │   ├── branch.py                                         
       90 +│   │   ├── department.py                                     
       91 +│   │   ├── user.py                                           
       92 +│   │   ├── role.py                     # Role, Permission, Ro
          +lePermission                                                  
       93 +│   │   ├── user_role.py                                      
       94 +│   │   ├── team.py                     # Team, TeamMember    
       95 +│   │   ├── customer.py                 # Customer, ContactMet
          +hod                                                           
       96 +│   │   ├── category.py                                       
       97 +│   │   ├── priority.py                                       
       98 +│   │   ├── ticket_status.py                                  
       99 +│   │   ├── status_transition.py                              
      100 +│   │   ├── ticket.py                                         
      101 +│   │   ├── ticket_event.py                                   
      102 +│   │   ├── attachment.py                                     
      103 +│   │   ├── sla_policy.py                                     
      104 +│   │   ├── quick_reply.py                                    
      105 +│   │   ├── kb_article.py               # KbArticle, KbArticle
          +Chunk                                                         
      106 +│   │   ├── api_key.py                                        
      107 +│   │   ├── audit_log.py                                      
      108 +│   │   ├── inbound_message.py                                
      109 +│   │   ├── channel_config.py                                 
      110 +│   │   └── llm_call.py                                       
      111 +│   ├── schemas/                        # Pydantic request/res
          +ponse models — mirrors contracts/openapi.yaml 1:1, one file pe
          +r models/ file                                                
      112 +│   ├── repositories/                                         
      113 +│   │   ├── scoped_repository.py        # TenantScope, Scoping
          +Mode, ScopedRepository — Shared Abstraction #1                
      114 +│   │   └── (no other files — every entity repository is `Scop
          +edRepository[Model]` instantiated with class attributes, not a
          + subclass with its own code, unless it needs a bespoke query —
          + see kb_repository.py, ticket_repository.py below)            
      115 +│   │   ├── ticket_repository.py        # ScopedRepository[Tic
          +ket] + view/filter query builders for F04's five dashboard vie
          +ws                                                            
      116 +│   │   └── kb_repository.py            # ScopedRepository[KbA
          +rticle] + hybrid trigram/vector search query                  
      117 +│   ├── services/                                             
      118 +│   │   ├── admin_crud_service.py       # AdminCrudService[Mod
          +elT, CreateT, UpdateT] — Generic CRUD Pattern, defined once   
      119 +│   │   ├── auth_service.py                                   
      120 +│   │   ├── customer_service.py                               
      121 +│   │   ├── ticket_service.py                                 
      122 +│   │   ├── ticket_transition_service.py                      
      123 +│   │   ├── assignment_service.py                             
      124 +│   │   ├── sla_service.py                                    
      125 +│   │   ├── kb_service.py                                     
      126 +│   │   ├── ai_service.py                                     
      127 +│   │   ├── channel_service.py                                
      128 +│   │   ├── portal_service.py                                 
      129 +│   │   ├── report_service.py                                 
      130 +│   │   └── api_key_service.py                                
      131 +│   ├── channels/                                             
      132 +│   │   ├── base.py                     # ChannelAdapter, Norm
          +alizedMessage, NormalizedAttachment — Shared Abstraction #4   
      133 +│   │   ├── email_adapter.py            # Functional — IMAP po
          +ll                                                            
      134 +│   │   ├── whatsapp_adapter.py         # Declared; normalize(
          +)/send_reply() raise NotImplementedError                      
      135 +│   │   ├── sms_adapter.py              # Declared; raises Not
          +ImplementedError                                              
      136 +│   │   └── chat_adapter.py             # Declared; raises Not
          +ImplementedError                                              
      137 +│   ├── ai/                                                   
      138 +│   │   └── litellm_wrapper.py          # LlmCapability, LlmRe
          +sult, LiteLlmWrapper — Shared Abstraction #5                  
      139 +│   ├── core/                                                 
      140 +│   │   ├── security.py                 # JWT issue/verify, Ar
          +gon2 hashing (passlib)                                        
      141 +│   │   ├── permissions.py              # CurrentActor, requir
          +e_permission — Shared Abstraction #2                          
      142 +│   │   ├── audit.py                    # audited decorator — 
          +Shared Abstraction #3                                         
      143 +│   │   ├── errors.py                   # PermissionDeniedErro
          +r, IllegalTransitionError, NotFoundError → HTTP mapping with l
          +ocalized ErrorResponse                                        
      144 +│   │   └── lint_no_vendor_sdk.py       # Import-lint enforcin
          +g Principle VIII / PLAN.md C8                                 
      145 +│   ├── api/                                                  
      146 +│   │   ├── deps.py                     # get_current_actor() 
          +(JWT or API-key → CurrentActor), get_session()                
      147 +│   │   ├── router.py                   # Aggregates every rou
          +ter below under /api/v1, per contracts/openapi.yaml           
      148 +│   │   └── routers/                    # One file per contrac
          +ts/openapi.yaml tag; each handler: validate request → call one
          + service method → serialize response. No business logic here (
          +constitution/PLAN.md: "route handlers validate, delegate, seri
          +alize")                                                       
      149 +│   │       ├── auth.py                                       
      150 +│   │       ├── admin_config.py         # branches, department
          +s, users, roles, categories, priorities, ticket-statuses, stat
          +us-transitions, sla-policies, quick-replies, teams            
      151 +│   │       ├── customers.py                                  
      152 +│   │       ├── tickets.py                                    
      153 +│   │       ├── kb.py                                         
      154 +│   │       ├── ai.py                                         
      155 +│   │       ├── channels.py                                   
      156 +│   │       ├── portal.py                                     
      157 +│   │       ├── reports.py                                    
      158 +│   │       ├── api_keys.py                                   
      159 +│   │       └── health.py                                     
      160 +│   ├── jobs/                                                 
      161 +│   │   ├── worker.py                   # ARQ WorkerSettings —
          + registers the three jobs below                               
      162 +│   │   ├── email_poll_job.py           # Runs ChannelService.
          +poll_email() every N seconds                                  
      163 +│   │   ├── categorization_job.py       # Runs AiService.categ
          +orize(ticket_id); enqueued by TicketService.create            
      164 +│   │   └── sla_sweep_job.py            # Runs SlaService.swee
          +p_breaches() every 5 minutes (PLAN.md F05)                    
      165 +│   └── seed/                                                 
      166 +│       └── seed.py                     # Idempotent seed per 
          +PLAN.md §7 — branches/departments/users/customers/tickets/KB/c
          +ategories/priorities/statuses+transitions/SLA policies/quick r
          +eplies/channel_configs/permissions incl. audit.read + report.c
          +ross_branch granted to admin                                  
      167 +├── tests/                                                    
      168 +│   ├── unit/                           # Principle XIV scope 
          +only: transition legality, permission checks, SLA computation,
          + tenant scoping                                               
      169 +│   ├── integration/                                          
      170 +│   └── golden/                                               
      171 +│       └── bilingual_tickets.json      # 20-ticket golden set
          + for AI categorization scoring (FR-050)                       
      172 +├── docker-compose.yml                                        
      173 +├── Dockerfile                                                
      174 +├── alembic.ini                                               
      175 +├── pyproject.toml                                            
      176 +└── .env.example                                              
      177  
       41 -*GATE: Must pass before Phase 0 research. Re-check after Phase
          - 1 design.*                                                   
      178 +frontend/                                                     
      179 +├── app/                                                      
      180 +│   ├── [locale]/                                             
      181 +│   │   ├── layout.tsx                  # Sets <html dir> from
          + active locale (next-intl) — the only place `dir` is set      
      182 +│   │   ├── (agent)/                                          
      183 +│   │   │   ├── dashboard/page.tsx      # F04's five views via
          + TanStack Query against GET /tickets?view=                    
      184 +│   │   │   ├── tickets/[id]/page.tsx   # Ticket detail incl. 
          +inline customer context, AI panels, timeline                  
      185 +│   │   │   ├── customers/page.tsx                            
      186 +│   │   │   ├── customers/[id]/page.tsx                       
      187 +│   │   │   ├── kb/page.tsx                                   
      188 +│   │   │   ├── reports/page.tsx                              
      189 +│   │   │   └── admin/                  # One route per admin_
          +config.py router group                                        
      190 +│   │   └── (portal)/                                         
      191 +│   │       ├── submit/page.tsx                               
      192 +│   │       ├── track/page.tsx                                
      193 +│   │       └── kb/page.tsx                                   
      194 +├── components/                                               
      195 +│   ├── ui/                             # shadcn/ui primitives
      196 +│   └── ltr-text.tsx                    # <LtrText> — sets dir
          +="ltr" locally for reference numbers/emails/phones/URLs (const
          +itution III)                                                  
      197 +├── lib/                                                      
      198 +│   └── api-client.ts                   # Typed client generat
          +ed from contracts/openapi.yaml                                
      199 +├── messages/                                                 
      200 +│   ├── ar.json                                               
      201 +│   └── en.json                                               
      202 +├── next.config.js                                            
      203 +├── tailwind.config.ts                  # Logical properties o
          +nly — no ml-/mr-/pl-/pr-/left-/right-                         
      204 +└── package.json                                              
      205  
       43 -[Gates determined based on constitution file]                 
      206 +docs/                                                         
      207 +├── architecture/stack.md               # Already exists — aut
          +horitative tech source                                        
      208 +└── DEBT.md                             # To be created before
          + batch 4a per constitution Principle XIII / PLAN.md §8 (not pa
          +rt of this plan's output)                                     
      209 +```                                                           
      210  
       45 -## Project Structure                                          
      211 +**Structure Decision**: Web application (Option 2) — separate 
          +`backend/` (FastAPI) and                                      
      212 +`frontend/` (Next.js), matching `docs/architecture/stack.md`'s
          + two-runtime design and PLAN.md                               
      213 +§6's batch sequencing (backend batches 4b–4d, 4f–4h build agai
          +nst the API directly; frontend                                
      214 +batch 4e/4i consumes it).                                     
      215  
       47 -### Documentation (this feature)                              
      216 +## Shared Abstractions                                        
      217  
       49 -```text                                                       
       50 -specs/[###-feature]/                                          
       51 -├── plan.md              # This file (/speckit.plan command ou
          -tput)                                                         
       52 -├── research.md          # Phase 0 output (/speckit.plan comma
          -nd)                                                           
       53 -├── data-model.md        # Phase 1 output (/speckit.plan comma
          -nd)                                                           
       54 -├── quickstart.md        # Phase 1 output (/speckit.plan comma
          -nd)                                                           
       55 -├── contracts/           # Phase 1 output (/speckit.plan comma
          -nd)                                                           
       56 -└── tasks.md             # Phase 2 output (/speckit.tasks comm
          -and - NOT created by /speckit.plan)                           
      218 +These five carry the constitution. Every other module composes
          + them; none of them is                                        
      219 +reimplemented, subclassed-around, or bypassed anywhere else in
          + this plan.                                                   
      220 +                                                              
      221 +### 1. `ScopedRepository` (`app/repositories/scoped_repository
          +.py`) — Principle V / PLAN.md C5                              
      222 +                                                              
      223 +```python                                                     
      224 +class ScopingMode(str, Enum):                                 
      225 +    S1_FULL = "s1_full"                        # branch_id + d
          +epartment_id, both NOT NULL                                   
      226 +    S2_BRANCH_DEPT_OPTIONAL = "s2_branch_dept_optional"  # bra
          +nch_id NOT NULL, department_id NULL                           
      227 +    S3_BRANCH_ONLY = "s3_branch_only"           # branch_id NO
          +T NULL only                                                   
      228 +    S4_TRANSITIVE = "s4_transitive"             # no columns; 
          +join to parent_model via parent_fk_column                     
      229 +    S5_SYSTEM_NULLABLE = "s5_system_nullable"   # branch_id NU
          +LL                                                            
      230 +    S6_GLOBAL = "s6_global"                     # no columns, 
          +no filter                                                     
      231 +                                                              
      232 +@dataclass(frozen=True)                                       
      233 +class TenantScope:                                            
      234 +    branch_id: UUID | None                                    
      235 +    department_id: UUID | None                                
      236 +    cross_branch: bool = False   # honored only by ReportServi
          +ce, and only after a permission check                         
      237 +                                                              
      238 +class ScopedRepository(Generic[ModelT]):                      
      239 +    model: ClassVar[type[Any]]                                
      240 +    scoping_mode: ClassVar[ScopingMode]                       
      241 +    parent_model: ClassVar[type[Any] | None] = None      # req
          +uired iff scoping_mode is S4                                  
      242 +    parent_fk_column: ClassVar[str | None] = None         # e.
          +g. "ticket_id" — required iff S4                              
      243 +    has_soft_delete: ClassVar[bool] = False                # T
          +rue iff the model has an is_active column (data-model.md §0.4)
      244 +                                                              
      245 +    def __init__(self, session: AsyncSession, scope: TenantSco
          +pe) -> None: ...                                              
      246 +                                                              
      247 +    async def get(self, id: UUID) -> ModelT | None: ...       
      248 +    async def list(self, filters: Mapping[str, Any] | None = N
          +one, order_by: str | None = None,                             
      249 +                    limit: int = 50, offset: int = 0) -> list[
          +ModelT]: ...                                                  
      250 +    async def create(self, values: Mapping[str, Any]) -> Model
          +T: ...                                                        
      251 +    async def update(self, id: UUID, values: Mapping[str, Any]
          +) -> ModelT: ...                                              
      252 +    async def deactivate(self, id: UUID) -> ModelT:           
      253 +        """Sets is_active=False. Raises NotImplementedError if
          + has_soft_delete is False —                                   
      254 +        use delete() instead (data-model.md §0.4's hard-delete
          + tables)."""                                                  
      255 +    async def delete(self, id: UUID) -> None:                 
      256 +        """Hard DELETE. Raises NotImplementedError if has_soft
          +_delete is True — use                                         
      257 +        deactivate() instead. Lets the DB's ON DELETE RESTRICT
          + surface as an IntegrityError,                                
      258 +        mapped by app/core/errors.py to a 409 with a localized
          + message."""                                                  
      259 +                                                              
      260 +    def _scoped_select(self) -> Select[tuple[ModelT]]:        
      261 +        """The ONLY method in the entire codebase permitted to
          + add a branch_id/department_id                                
      262 +        predicate or a parent-join predicate to a query. Every
          + public method above calls this                               
      263 +        first. Behavior by scoping_mode:                      
      264 +          S1: WHERE model.branch_id = scope.branch_id AND mode
          +l.department_id = scope.department_id                         
      265 +          S2: WHERE model.branch_id = scope.branch_id         
      266 +              AND (model.department_id = scope.department_id O
          +R model.department_id IS NULL)                                
      267 +          S3: WHERE model.branch_id = scope.branch_id         
      268 +          S4: JOIN parent_model ON model.parent_fk_column == p
          +arent_model.id, then apply                                    
      269 +              parent_model's OWN _scoped_select predicate (rec
          +ursive — a S4 child of an S4                                  
      270 +              child, e.g. none currently exist, still resolves
          + correctly)                                                   
      271 +          S5: WHERE model.branch_id = scope.branch_id OR model
          +.branch_id IS NULL                                            
      272 +              (a system-level/pre-resolution row is visible wi
          +thin any scope that could plausibly                           
      273 +              own it — narrower filtering, where needed, is ap
          +plied by the calling service, e.g.                            
      274 +              AuditService only ever queries S5 rows explicitl
          +y by actor/entity, never lists all)                           
      275 +          S6: no predicate at all                             
      276 +        cross_branch=True on the scope skips the branch predic
          +ate entirely for S1/S2/S3; it is                              
      277 +        set only by ReportService, and only after that service
          + has itself checked                                           
      278 +        `report.cross_branch` is in the caller's CurrentActor.
          +permissions (§Service Classes).                               
      279 +        """                                                   
      280  ```
      281  
       59 -### Source Code (repository root)                             
       60 -<!--                                                          
       61 -  ACTION REQUIRED: Replace the placeholder tree below with the
          - concrete layout                                              
       62 -  for this feature. Delete unused options and expand the chose
          -n structure with                                              
       63 -  real paths (e.g., apps/admin, packages/something). The deliv
          -ered plan must                                                
       64 -  not include Option labels.                                  
       65 --->                                                           
      282 +Every entity repository is `ScopedRepository[Model]` with `mod
          +el`/`scoping_mode`/                                           
      283 +`parent_model`/`parent_fk_column`/`has_soft_delete` set as cla
          +ss attributes — no subclass writes                            
      284 +its own `WHERE`. `TicketRepository` and `KbRepository` (the tw
          +o files listed under                                          
      285 +`repositories/` beyond the base) *add* methods (dashboard view
          +s, hybrid search) but never                                   
      286 +override `_scoped_select`.                                    
      287  
       67 -```text                                                       
       68 -# [REMOVE IF UNUSED] Option 1: Single project (DEFAULT)       
       69 -src/                                                          
       70 -├── models/                                                   
       71 -├── services/                                                 
       72 -├── cli/                                                      
       73 -└── lib/                                                      
      288 +### 2. Permission guard (`app/core/permissions.py`) — Principl
          +e X / PLAN.md C10                                             
      289  
       75 -tests/                                                        
       76 -├── contract/                                                 
       77 -├── integration/                                              
       78 -└── unit/                                                     
      290 +```python                                                     
      291 +@dataclass(frozen=True)                                       
      292 +class CurrentActor:                                           
      293 +    user_id: UUID                                             
      294 +    scope: TenantScope                                        
      295 +    permissions: frozenset[str]                               
      296 +    correlation_id: UUID                                      
      297  
       80 -# [REMOVE IF UNUSED] Option 2: Web application (when "frontend
          -" + "backend" detected)                                       
       81 -backend/                                                      
       82 -├── src/                                                      
       83 -│   ├── models/                                               
       84 -│   ├── services/                                             
       85 -│   └── api/                                                  
       86 -└── tests/                                                    
      298 +class PermissionDeniedError(Exception):                       
      299 +    def __init__(self, code: str) -> None: ...                
      300  
       88 -frontend/                                                     
       89 -├── src/                                                      
       90 -│   ├── components/                                           
       91 -│   ├── pages/                                                
       92 -│   └── services/                                             
       93 -└── tests/                                                    
      301 +def require_permission(code: str):                            
      302 +    """Wraps an async SERVICE method (never a route handler). 
          +The wrapped method's signature                                
      303 +    MUST be `(self, actor: CurrentActor, *args, **kwargs)`. Be
          +fore calling it, checks                                       
      304 +    `code in actor.permissions`; raises PermissionDeniedError(
          +code) if not, which app/core/errors.py                        
      305 +    maps to HTTP 403 with a localized ErrorResponse. On succes
          +s, calls straight through with no                             
      306 +    other side effect."""                                     
      307 +    def decorator(fn):                                        
      308 +        @functools.wraps(fn)                                  
      309 +        async def wrapper(self, actor: CurrentActor, *args, **
          +kwargs):                                                      
      310 +            if code not in actor.permissions:                 
      311 +                raise PermissionDeniedError(code)             
      312 +            return await fn(self, actor, *args, **kwargs)     
      313 +        return wrapper                                        
      314 +    return decorator                                          
      315 +```                                                           
      316  
       95 -# [REMOVE IF UNUSED] Option 3: Mobile + API (when "iOS/Android
          -" detected)                                                   
       96 -api/                                                          
       97 -└── [same as backend above]                                   
      317 +A route handler's only auth-adjacent responsibility is calling
          + `get_current_actor()` (which                                 
      318 +decodes the JWT or API key into a `CurrentActor`, resolving `p
          +ermissions` as the union of                                   
      319 +`role_permissions` reachable via every matching `user_roles` r
          +ow, or the API key's `scopes`) and                            
      320 +passing it as the first argument to a service method. The perm
          +ission decision itself always                                 
      321 +executes inside the decorated service method, never in `app/ap
          +i/routers/*`.                                                 
      322  
       99 -ios/ or android/                                              
      100 -└── [platform-specific structure: feature modules, UI flows, p
          -latform tests]                                                
      323 +### 3. Audit-write decorator (`app/core/audit.py`) — Principle
          + VII / PLAN.md C7                                             
      324 +                                                              
      325 +```python                                                     
      326 +def audited(entity_type: str, action: str):                   
      327 +    """Wraps an async SERVICE method already wrapped by (or it
          +self calling) require_permission,                             
      328 +    whose signature is `(self, actor: CurrentActor, id: UUID |
          + None, *args, **kwargs) -> ModelT | None`.                    
      329 +                                                              
      330 +    Behavior, all inside the SAME AsyncSession/transaction as 
          +the wrapped call (never a separate                            
      331 +    commit):                                                  
      332 +      1. If `id` is not None, load the entity's current row an
          +d serialize it as `before`                                    
      333 +         (None if the row doesn't exist yet — the create case)
          +.                                                             
      334 +      2. Call the wrapped method; let any exception propagate 
          +uncaught (the transaction rolls                               
      335 +         back, so no audit row is written — this IS the "rolle
          +d-back transaction leaves no audit                            
      336 +         row" guarantee, not a separate try/except).          
      337 +      3. Serialize the result as `after` (None for delete/deac
          +tivate-returning-None).                                       
      338 +      4. session.add(AuditLog(actor_id=actor.user_id, action=a
          +ction, entity_type=entity_type,                               
      339 +         entity_id=(result.id if result is not None else id), 
          +before=before, after=after,                                   
      340 +         correlation_id=actor.correlation_id)) — added to the 
          +session, not committed                                        
      341 +         independently; the request-scoped get_session() depen
          +dency commits once, at the end of                             
      342 +         the request, per app/db.py.                          
      343 +      5. Return the wrapped method's result unchanged.        
      344 +    """                                                       
      345  ```
      346  
      103 -**Structure Decision**: [Document the selected structure and r
          -eference the real                                             
      104 -directories captured above]                                   
      347 +### 4. `ChannelAdapter` (`app/channels/base.py`) — Principle V
          +III's channel half / PLAN.md F03                              
      348  
      349 +```python                                                     
      350 +class NormalizedAttachment(BaseModel):                        
      351 +    filename: str                                             
      352 +    content_type: str                                         
      353 +    data: bytes | None                                        
      354 +    source_url: str | None                                    
      355 +                                                              
      356 +class NormalizedMessage(BaseModel):                           
      357 +    external_id: str                                          
      358 +    channel: ChannelEnum                                      
      359 +    from_identity: str                                        
      360 +    to_identity: str                    # the receiving identi
          +fier — matched against channel_configs.identifier             
      361 +    subject: str | None                                       
      362 +    body: str                                                 
      363 +    locale: Literal["ar", "en"]                               
      364 +    attachments: list[NormalizedAttachment]                   
      365 +    received_at: datetime                                     
      366 +                                                              
      367 +class ChannelAdapter(Protocol):                               
      368 +    channel: ClassVar[ChannelEnum]                            
      369 +    def normalize(self, raw: dict[str, Any]) -> NormalizedMess
          +age: ...                                                      
      370 +    async def send_reply(self, ticket_id: UUID, body: str, loc
          +ale: Literal["ar", "en"]) -> None: ...                        
      371 +```                                                           
      372 +                                                              
      373 +`EmailAdapter` implements both methods functionally. `Whatsapp
          +Adapter`, `SmsAdapter`,                                       
      374 +`ChatAdapter` implement the `channel` class attribute and defi
          +ne both methods with a body of                                
      375 +exactly `raise NotImplementedError(f"{self.channel} channel is
          + Tier D — see specs/00X")` — present                          
      376 +and importable, not absent, per PLAN.md F03. `ChannelService.i
          +ngest()` (§Service Classes) is the                            
      377 +only caller of `normalize()`; it is written entirely against t
          +he `ChannelAdapter` protocol, never                           
      378 +against a concrete adapter class, which is what makes "adding 
          +a channel later requires zero                                 
      379 +changes to ticket creation logic" mechanically true rather tha
          +n just documented.                                            
      380 +                                                              
      381 +### 5. `LiteLlmWrapper` (`app/ai/litellm_wrapper.py`) — Princi
          +ple VIII/IX / PLAN.md C8/C9                                   
      382 +                                                              
      383 +```python                                                     
      384 +class LlmCapability(str, Enum):                               
      385 +    CATEGORIZE = "categorize"                                 
      386 +    SUMMARIZE = "summarize"                                   
      387 +    SUGGEST_REPLY = "suggest_reply"                           
      388 +                                                              
      389 +class LlmResult(BaseModel):                                   
      390 +    text: str | None                                          
      391 +    structured: dict[str, Any] | None                         
      392 +    fallback_used: bool                                       
      393 +    latency_ms: int                                           
      394 +    input_tokens: int | None                                  
      395 +    output_tokens: int | None                                 
      396 +                                                              
      397 +class LiteLlmWrapper:                                         
      398 +    def __init__(self, api_base: str, api_key: str, model_chat
          +: str, model_classify: str,                                   
      399 +                 timeout_s: float = 10.0, max_retries: int = 1
          +) -> None: ...                                                
      400 +                                                              
      401 +    async def complete(self, capability: LlmCapability, prompt
          +: str, prompt_version: str,                                   
      402 +                        ticket_id: UUID | None, correlation_id
          +: UUID) -> LlmResult:                                         
      403 +        """NEVER raises to the caller. On timeout, HTTP error,
          + or retry exhaustion, returns                                 
      404 +        LlmResult(text=None, structured=None, fallback_used=Tr
          +ue, ...) instead of propagating.                              
      405 +        Every call — success or failure — inserts exactly one 
          +llm_calls row (capability, model,                             
      406 +        prompt_version, token counts if known, latency_ms, fal
          +lback_used, error if any,                                     
      407 +        correlation_id) via its own short-lived session, commi
          +tted immediately (not tied to the                             
      408 +        caller's transaction — an AI call outcome must be reco
          +rded even if the caller's ticket                              
      409 +        transaction later rolls back for an unrelated reason).
          + This is the only module in the                               
      410 +        codebase that imports an LLM HTTP client; app/core/lin
          +t_no_vendor_sdk.py fails CI if any                            
      411 +        other module imports one directly."""                 
      412 +```                                                           
      413 +                                                              
      414 +`AiService` (§Service Classes) is the only caller. No `AiServi
          +ce` method ever raises — it catches                           
      415 +nothing, because `LiteLlmWrapper.complete()` already never rai
          +ses; the fallback text/behavior                               
      416 +named in `spec.md` FR-009/FR-047 is simply what each `AiServic
          +e` method returns when                                        
      417 +`result.fallback_used` is `True`.                             
      418 +                                                              
      419 +## Generic CRUD Pattern                                       
      420 +                                                              
      421 +Defined once. **Entities that follow it verbatim** (i.e. their
          + router calls only                                            
      422 +`AdminCrudService` methods, with no bespoke service code): `br
          +anches`, `departments`, `users`                               
      423 +(with one override — see below), `roles`, `categories`, `prior
          +ities`, `ticket_statuses`,                                    
      424 +`sla_policies`, `quick_replies`, `teams`. `status_transitions`
          + uses the `list`/`create`/`update`/                           
      425 +`delete` subset only (no single-`get`, matching `contracts/ope
          +napi.yaml`).                                                  
      426 +                                                              
      427 +```python                                                     
      428 +class AdminCrudService(Generic[ModelT, CreateSchemaT, UpdateSc
          +hemaT]):                                                      
      429 +    repository_cls: ClassVar[type[ScopedRepository]]          
      430 +    permission: ClassVar[str] = "admin.config"                
      431 +    entity_type: ClassVar[str]                 # for the audit
          +ed() decorator                                                
      432 +                                                              
      433 +    def __init__(self, session: AsyncSession) -> None: ...    
      434 +                                                              
      435 +    @require_permission_via(lambda self: self.permission)   # 
          +see note below                                                
      436 +    async def list(self, actor: CurrentActor, filters: Mapping
          +[str, Any] | None = None,                                     
      437 +                    limit: int = 50, offset: int = 0) -> list[
          +ModelT]: ...                                                  
      438 +    async def get(self, actor: CurrentActor, id: UUID) -> Mode
          +lT: ...     # raises NotFoundError                            
      439 +    async def create(self, actor: CurrentActor, data: CreateSc
          +hemaT) -> ModelT: ...                                         
      440 +    async def update(self, actor: CurrentActor, id: UUID, data
          +: UpdateSchemaT) -> ModelT: ...                               
      441 +    async def remove(self, actor: CurrentActor, id: UUID) -> M
          +odelT | None:                                                 
      442 +        """Calls repository.deactivate(id) if repository_cls.h
          +as_soft_delete else                                           
      443 +        repository.delete(id) — the branch is on the class att
          +ribute, not re-decided per call."""                           
      444 +```                                                           
      445 +                                                              
      446 +`create`, `update`, and `remove` are each individually wrapped
          + with `@require_permission(...)` and                          
      447 +`@audited(entity_type, action)` at definition time in `admin_c
          +rud_service.py` — `list`/`get` carry                          
      448 +only `require_permission` (reads are not audited, matching `da
          +ta-model.md`'s `audit_logs` schema,                           
      449 +which has no concept of a read event). `require_permission_via
          +` (a two-line helper alongside                                
      450 +`require_permission` in `app/core/permissions.py`) exists sole
          +ly so the permission code can be a                            
      451 +class attribute instead of a literal, since every subclass exc
          +ept `status_transitions`,                                     
      452 +`kb_articles` (`kb_article.create`/`.publish`), and the ticket
          +-workflow permissions uses the same                           
      453 +`"admin.config"` string.                                      
      454 +                                                              
      455 +**`UserService(AdminCrudService[User, UserCreate, UserUpdate])
          +`** overrides `create` only, to hash                          
      456 +`password` into `password_hash` via `passlib`'s Argon2 hasher 
          +before delegating to                                          
      457 +`super().create()` — everything else (list/get/update/remove, 
          +audit, permission) is inherited                               
      458 +unchanged.                                                    
      459 +                                                              
      460 +Every other S1/S2/S3 entity not covered by the ten above (`kb_
          +articles`, `quick_replies` is                                 
      461 +already listed) gets its CRUD permission named `{entity}.read`
          +/`{entity}.create` following the                              
      462 +same `ticket.read`/`ticket.create` convention documented in `d
          +ata-model.md` §5 — `KbService`                                
      463 +(bespoke, below) still composes `AdminCrudService`'s `list`/`g
          +et` for the plain-CRUD parts of its                           
      464 +surface and only adds bespoke methods for publish/search/chunk
          +ing.                                                          
      465 +                                                              
      466 +## Service Classes                                            
      467 +                                                              
      468 +Bespoke services — i.e. containing at least one method the Gen
          +eric CRUD Pattern does not cover.                             
      469 +Every mutating method below carries `@require_permission(...)`
          + and, where it returns a                                      
      470 +persisted entity, `@audited(...)`; both are omitted from the s
          +ignatures below for brevity and                               
      471 +stated once here instead, per this plan's "never write 'as nee
          +ded'" instruction — the permission                            
      472 +code for each is the one named in `contracts/openapi.yaml`'s m
          +atching `x-permission`.                                       
      473 +                                                              
      474 +**`AuthService`**                                             
      475 +- `async def login(self, email: str, password: str) -> TokenPa
          +ir`                                                           
      476 +- `async def refresh(self, refresh_token: str) -> TokenPair`  
      477 +- `async def logout(self, actor: CurrentActor) -> None`       
      478 +                                                              
      479 +**`CustomerService`**                                         
      480 +- `async def search(self, actor: CurrentActor, q: str | None, 
          +limit: int, offset: int) -> list[Customer]`                   
      481 +- `async def get(self, actor: CurrentActor, id: UUID) -> Custo
          +mer`                                                          
      482 +- `async def create(self, actor: CurrentActor, data: CustomerC
          +reate) -> Customer` — rejects if `data.contact_methods` has ze
          +ro items or more/less than exactly one `is_primary=True` (FR-0
          +11; also DB-enforced, §data-model.md §1.11)                   
      483 +- `async def update(self, actor: CurrentActor, id: UUID, data:
          + CustomerUpdate) -> Customer`                                 
      484 +- `async def deactivate(self, actor: CurrentActor, id: UUID) -
          +> Customer`                                                   
      485 +- `async def get_history(self, actor: CurrentActor, id: UUID) 
          +-> CustomerHistory` — merges `tickets` and `ticket_events` for
          + every ticket owned by this customer, sorted by timestamp (FR-
          +014)                                                          
      486 +- `async def add_contact_method(self, actor: CurrentActor, cus
          +tomer_id: UUID, data: ContactMethodCreate) -> ContactMethod`  
      487 +- `async def add_attachment(self, actor: CurrentActor, custome
          +r_id: UUID, file: UploadFile) -> Attachment`                  
      488 +                                                              
      489 +**`TicketService`**                                           
      490 +- `async def list(self, actor: CurrentActor, view: TicketView 
          +| None, filters: TicketFilters, limit: int, offset: int) -> li
          +st[TicketSummary]` — `view` maps to one of F04's five queue de
          +finitions via `TicketRepository`                              
      491 +- `async def get(self, actor: CurrentActor, id: UUID) -> Ticke
          +t` — includes computed `sla_first_response_due_at`/`sla_resolu
          +tion_due_at`/`sla_breach_state` via `SlaService` (never stored
          +)                                                             
      492 +- `async def create(self, actor: CurrentActor, data: TicketCre
          +ate) -> Ticket` — generates `reference_no` from a DB sequence 
          +(`TKT-{year}-{6-digit}`), resolves `sla_policy_id` via `SlaSer
          +vice.resolve_policy`, writes the `created` event, enqueues `ca
          +tegorization_job` (never awaits it — FR-049)                  
      493 +- `async def update(self, actor: CurrentActor, id: UUID, data:
          + TicketUpdate) -> Ticket`                                     
      494 +- `async def assign(self, actor: CurrentActor, id: UUID, assig
          +nee_id: UUID | None, team_id: UUID | None) -> Ticket`         
      495 +- `async def add_note(self, actor: CurrentActor, id: UUID, bod
          +y: str) -> TicketEvent`                                       
      496 +- `async def add_reply(self, actor: CurrentActor, id: UUID, bo
          +dy: str) -> TicketEvent` — stamps `first_response_at` iff it i
          +s currently `NULL` (FR-021)                                   
      497 +- `async def add_attachment(self, actor: CurrentActor, id: UUI
          +D, file: UploadFile) -> Attachment`                           
      498 +- `async def correct_triage(self, actor: CurrentActor, id: UUI
          +D, branch_id: UUID, department_id: UUID) -> Ticket` — clears `
          +needs_triage`, writes a `field_changed` event per `field_name`
          + (FR-023c; `data-model.md` §1.17)                             
      499 +- `async def get_events(self, actor: CurrentActor, id: UUID) -
          +> list[TicketEvent]`                                          
      500 +                                                              
      501 +**`TicketTransitionService`**                                 
      502 +- `async def change_status(self, actor: CurrentActor, ticket_i
          +d: UUID, to_status_id: UUID, reason: str | None) -> Ticket` — 
          +the ONLY method in the codebase that queries `status_transitio
          +ns`; raises `IllegalTransitionError(current_status_id, permitt
          +ed_status_ids)` (→ HTTP 422) when no `(from_status_id, to_stat
          +us_id)` row matches the ticket's branch/department (falling ba
          +ck to the `department_id IS NULL` default row); raises `Permis
          +sionDeniedError` if the matched row's `required_permission` is
          + set and absent from `actor.permissions`; raises a validation 
          +error if `requires_reason` is true and `reason` is `None`     
      503 +                                                              
      504 +**`AssignmentService`**                                       
      505 +- `async def auto_assign_ticket(self, ticket_id: UUID) -> Tick
          +et | None` — round-robins over active `ticket.own`-holding age
          +nts in the ticket's department who are not flagged unavailable
          +; returns `None` (ticket stays unassigned) if none are eligibl
          +e. Called by `categorization_job` after categorization complet
          +es, and directly by `TicketService.create` when the ticket has
          + no `assignee_id`/`team_id` set at creation.                  
      506 +                                                              
      507 +**`SlaService`**                                              
      508 +- `def compute_due_dates(self, ticket: Ticket, policy: SlaPoli
          +cy, branch: Branch) -> SlaDueDates` — pure function; applies `
          +sla_paused_ms` and, if `policy.business_hours_only`, `branch.b
          +usiness_hours`/`branch.timezone`                              
      509 +- `def compute_breach_state(self, due_dates: SlaDueDates, now:
          + datetime) -> Literal["on_track", "at_risk", "breached"]` — pu
          +re function; `at_risk` when <25% of the relevant window remain
          +s                                                             
      510 +- `async def resolve_policy(self, branch_id: UUID, department_
          +id: UUID, category_id: UUID, priority_id: UUID) -> SlaPolicy |
          + None` — exact category+priority → priority-only → category-on
          +ly → default (`data-model.md` §1.19 index)                    
      511 +- `async def override_policy(self, actor: CurrentActor, ticket
          +_id: UUID, sla_policy_id: UUID, reason: str) -> Ticket` — `req
          +uire_permission("ticket.sla_override")`; recomputes both due d
          +ates from the ticket's original `created_at` under the new pol
          +icy (FR-039); an immediately-breaching result is permitted and
          + is picked up by the next `sweep_breaches()` run rather than r
          +ecorded inline, keeping breach recording in exactly one place 
      512 +- `async def sweep_breaches(self) -> int` — `sla_sweep_job`'s 
          +body; for every open ticket whose computed `breach_state == "b
          +reached"` with no existing `sla_breached` event for its curren
          +t target, writes one, raises priority one severity level, reas
          +signs to the department's lead; returns the count written (ide
          +mpotency check is "does an `sla_breached` event already exist 
          +for this ticket+target", so re-running produces zero new event
          +s)                                                            
      513 +                                                              
      514 +**`KbService`**                                               
      515 +- `async def create_article(self, actor: CurrentActor, data: K
          +bArticleCreate) -> KbArticle`                                 
      516 +- `async def update_article(self, actor: CurrentActor, id: UUI
          +D, data: KbArticleUpdate) -> KbArticle` — re-chunks (~500 toke
          +ns, 50 overlap) and re-embeds both locales on any body change 
      517 +- `async def publish_article(self, actor: CurrentActor, id: UU
          +ID) -> KbArticle` — rejects (422) unless `title_ar`/`title_en`
          +/`body_ar`/`body_en` are all non-empty (FR-041)               
      518 +- `async def search(self, actor: CurrentActor, query: str, lim
          +it: int) -> list[KbSearchResult]` — `pg_trgm` lexical + `pgvec
          +tor` cosine, reciprocal-rank-fused; reranked with `bge-reranke
          +r-v2-m3` iff the reranker feature flag is on, else fused order
          + returned directly (FR-043)                                   
      519 +                                                              
      520 +**`AiService`** — every method below wraps `LiteLlmWrapper.com
          +plete()`/`KbService.search()`; none raises                    
      521 +- `async def summarize(self, ticket_id: UUID) -> AiSummaryResp
          +onse` — fallback: first 300 characters of `description`       
      522 +- `async def suggest_reply(self, ticket_id: UUID) -> AiSuggest
          +edReplyResponse` — fallback: empty draft                      
      523 +- `async def suggest_solution(self, ticket_id: UUID) -> list[K
          +bSearchResult]` — top 3 from `KbService.search()` over subject
          ++description; fallback: empty list                            
      524 +- `async def categorize(self, ticket_id: UUID) -> None` — `cat
          +egorization_job`'s body; writes `ai_suggested_category_id`/`ai
          +_category_confidence` directly (never `category_id`); fallback
          +: leaves both `NULL`                                          
      525 +- `async def apply_categorization_decision(self, actor: Curren
          +tActor, ticket_id: UUID, accepted: bool, override_category_id:
          + UUID | None) -> Ticket` — writes the ticket's real `category_
          +id` and an `ai_suggestion_applied` event recording the origina
          +l suggestion + confidence (FR-044)                            
      526 +- `async def run_categorization_benchmark(self, actor: Current
          +Actor) -> BenchmarkResult` — scores `tests/golden/bilingual_ti
          +ckets.json` (FR-050)                                          
      527 +                                                              
      528 +**`ChannelService`**                                          
      529 +- `def register_adapter(self, adapter: ChannelAdapter) -> None
          +` — called once per adapter at app startup (`app/main.py`)    
      530 +- `async def ingest(self, channel: ChannelEnum, raw: dict[str,
          + Any]) -> InboundMessageAccepted` —                           
      531 +  1. `adapter.normalize(raw)` → `NormalizedMessage`; the raw p
          +ayload is persisted to                                        
      532 +     `inbound_messages` *before* any further processing, per P
          +LAN.md F03.                                                   
      533 +  2. If `NormalizedMessage.subject` (or body) contains a valid
          +, existing `reference_no`: resolve                            
      534 +     the ticket by that reference; its `branch_id`/`department
          +_id` **override** whatever                                    
      535 +     `channel_configs` would have resolved (FR-023b). If the r
          +eceiving identifier's configured                              
      536 +     branch/department differs, append a `field_changed` timel
          +ine event recording the mismatch —                            
      537 +     it does not block the append.                            
      538 +  3. Otherwise: look up `channel_configs` by `to_identity`. A 
          +match gives `(branch_id,                                      
      539 +     department_id, default_category_id)`; no match falls back
          + to `config.SYSTEM_DEFAULT_BRANCH_ID`/                        
      540 +     `SYSTEM_DEFAULT_DEPARTMENT_ID` and sets `needs_triage=Tru
          +e` (FR-023a).                                                 
      541 +  4. Constructs a **fresh `TenantScope`** from the branch/depa
          +rtment resolved in step 2 or 3 —                              
      542 +     *never* reuses the caller's own `CurrentActor.scope` (the
          + caller here is typically an API-key                          
      543 +     client with no single home branch). Contact-method matchi
          +ng (FR-023) is performed through a                            
      544 +     `ContactMethodRepository`/`CustomerRepository` pair const
          +ructed with this resolved scope, so                           
      545 +     it is structurally impossible for it to match a customer 
          +in a different branch/department.                             
      546 +  5. No match on `from_identity` → creates a new `Customer` + 
          +primary `ContactMethod` in the                                
      547 +     resolved scope, then a new `Ticket`; a match on `external
          +_id` or `reference_no` → appends a                            
      548 +     `reply_sent`/`note_added` event to the existing ticket in
          +stead (FR-024's threading rule).                              
      549 +- `async def poll_email(self) -> int` — `email_poll_job`'s bod
          +y; IMAP poll, calls `ingest()` per message, returns count proc
          +essed                                                         
      550 +                                                              
      551 +**`PortalService`** (unauthenticated — no `CurrentActor`/`requ
          +ire_permission`; every method is                              
      552 +inherently public per `contracts/openapi.yaml`'s `security: []
          +`)                                                            
      553 +- `async def submit_ticket(self, data: PortalTicketSubmit) -> 
          +str` — returns `reference_no`.                                
      554 +  Branch is resolved from `data.category_id`'s own `branch_id`
          + (categories are always S2-scoped                             
      555 +  to exactly one branch); department is the category's `depart
          +ment_id` if set, else the same                                
      556 +  system-default-plus-`needs_triage` fallback as `ChannelServi
          +ce` step 3 above — reusing the                                
      557 +  identical accommodation rather than inventing a second one. 
      558 +- `async def track_ticket(self, reference_no: str, contact_val
          +ue: str) -> PortalTicketView | None` — `None` on any mismatch 
          +(unknown reference OR wrong contact), so the router returns an
          + identical 404 either way (FR-053) — no branching on "which re
          +ason" anywhere past this method                               
      559 +- `async def get_history(self, reference_no: str, contact_valu
          +e: str) -> list[PortalTicketView] | None` — same resolution; f
          +ilters out every `visibility="internal"` event before returnin
          +g (FR-054)                                                    
      560 +                                                              
      561 +**`ReportService`**                                           
      562 +- `async def tickets_by_status(self, actor: CurrentActor, date
          +_from: date | None, date_to: date | None, cross_branch: bool) 
          +-> TicketsByStatusReport`                                     
      563 +- `async def sla_compliance(self, actor: CurrentActor, date_fr
          +om: date | None, date_to: date | None, cross_branch: bool) -> 
          +SlaComplianceReport`                                          
      564 +- `async def agent_volume(self, actor: CurrentActor, date_from
          +: date | None, date_to: date | None, cross_branch: bool) -> Ag
          +entVolumeReport`                                              
      565 +                                                              
      566 +  All three: if `cross_branch` is `True`, raise `PermissionDen
          +iedError("report.cross_branch")`                              
      567 +  unless that code is in `actor.permissions`; only then does t
          +he constructed `TenantScope` set                              
      568 +  `cross_branch=True` before querying (FR-060).               
      569 +                                                              
      570 +**`ApiKeyService`**                                           
      571 +- `async def issue(self, actor: CurrentActor, data: ApiKeyCrea
          +te) -> tuple[ApiKey, str]` — generates a random secret, return
          +s `(record, plaintext)`; only `key_hash` (Argon2) is persisted
      572 +- `async def revoke(self, actor: CurrentActor, id: UUID) -> Ap
          +iKey` — sets `expires_at = now()`                             
      573 +- `async def authenticate(self, plaintext_key: str) -> Current
          +Actor` — looks up by `key_hash`, builds a `CurrentActor` whose
          + `permissions` is exactly `scopes` (JSONB) and whose `scope` i
          +s `TenantScope(branch_id=api_keys.branch_id, department_id=Non
          +e)`; used by `get_current_actor()` (`app/api/deps.py`) when th
          +e request carries `X-API-Key` instead of a bearer JWT         
      574 +                                                              
      575 +No bespoke `AuditService` exists — `audited` (§Shared Abstract
          +ions #3) is the entire write path,                            
      576 +and audit *reading* is Tier D (`specs/006-audit-log-ui`), out 
          +of this plan's scope entirely.                                
      577 +                                                              
      578  ## Complexity Tracking
      579  
      108 -> **Fill ONLY if Constitution Check has violations that must b
          -e justified**                                                 
      580 +*Empty — the Constitution Check above found no violation requi
          +ring justification.*                                          
      581  
      110 -| Violation | Why Needed | Simpler Alternative Rejected Becaus
          -e |                                                           
      111 -|-----------|------------|------------------------------------
          --|                                                            
      112 -| [e.g., 4th project] | [current need] | [why 3 projects insuf
          -ficient] |                                                    
      113 -| [e.g., Repository pattern] | [specific problem] | [why direc
          -t DB access insufficient] |                                   
      582 +## Post-Design Constitution Check & Self-Audit                
      583 +                                                              
      584 +*Re-checked after Phase 1 design, per this command's own workf
          +low, and per this turn's explicit                             
      585 +instruction to re-read the constitution and PLAN.md §4.1 and r
          +eport every place the artifacts                               
      586 +produced in this run violate a principle or diverge from the a
          +ssigned scoping pattern. Three real                           
      587 +issues were found while drafting `data-model.md`/`contracts/op
          +enapi.yaml` in this session — they                            
      588 +were fixed in place before this plan was written, and are repo
          +rted here rather than silently                                
      589 +corrected, per the instruction to list them.*                 
      590 +                                                              
      591 +### Issues found and fixed in this run                        
      592 +                                                              
      593 +1. **Over-applied "reference-data base."** An early draft of `
          +data-model.md` §0.4 gave every                                
      594 +   `label_ar`/`label_en` table (`branches`, `departments`, `ro
          +les`, `permissions`, `categories`,                            
      595 +   `priorities`, `ticket_statuses`, `sla_policies`, `quick_rep
          +lies`, `teams`) both `is_active` and                          
      596 +   `sort_order` uniformly. PLAN.md §4.2 lists these two fields
          + per table *inconsistently* (e.g.                             
      597 +   `roles` has neither; `ticket_statuses` has `sort_order` but
          + not `is_active`) — the uniform draft                         
      598 +   invented four fields PLAN.md never specified (`roles.is_act
          +ive`, `roles.sort_order`,                                     
      599 +   `permissions.is_active`, `permissions.sort_order`, `priorit
          +ies.is_active`,                                               
      600 +   `priorities.sort_order`, `ticket_statuses.is_active`, `sla_
          +policies.is_active`,                                          
      601 +   `sla_policies.sort_order`, `quick_replies.is_active`, `quic
          +k_replies.sort_order`,                                        
      602 +   `teams.is_active`, `teams.sort_order`, `departments.sort_or
          +der`, `branches.sort_order`).                                 
      603 +   **Fixed**: §0.4 now lists only `label_ar`/`label_en` as uni
          +versal, with a per-table matrix for                           
      604 +   `is_active`/`sort_order` matching PLAN.md §4.2 exactly; `co
          +ntracts/openapi.yaml`'s                                       
      605 +   `ReferenceDataBase` schema and the seven affected entity sc
          +hemas were corrected to match.                                
      606 +2. **Invented `ticket_events.event_type` value.** An early dra
          +ft added `'branch_corrected'` to the                          
      607 +   `event_type` `CHECK` constraint to represent FR-023c's need
          +s-triage correction — a value not in                          
      608 +   PLAN.md §4.2's explicit eleven-value list. **Fixed**: the c
          +orrection is now recorded as a                                
      609 +   `field_changed` event (`field_name = 'branch_id'` or `'depa
          +rtment_id'`), reusing the existing                            
      610 +   generic shape; the `CHECK` constraint and the OpenAPI `Tick
          +etEvent.event_type` enum both now                             
      611 +   match PLAN.md's list exactly, with zero added values.      
      612 +3. **Inconsistent `updated_at`/`updated_by` carve-out.** An ea
          +rly draft excluded `role_permissions`                         
      613 +   from §0.2's "every mutable table" rule with an ad hoc justi
          +fication, while leaving the                                   
      614 +   structurally identical `user_roles` and `team_members` join
          + tables to inherit it normally —                              
      615 +   an unjustified, self-inconsistent exception. **Fixed**: `ro
          +le_permissions` now inherits                                  
      616 +   `updated_at`/`updated_by` like every other mutable table; n
          +o exception is carved out anywhere                            
      617 +   in `data-model.md` that PLAN.md does not itself state.     
      618 +                                                              
      619 +A related but *unfixed-by-design* item, carried over from `res
          +earch.md` Part 2 rather than fixed                            
      620 +here: PLAN.md §4.1's own Assignment table is missing rows for 
          +six tables (resolved using PLAN.md's                          
      621 +own pattern definitions, not invented) and its `channel_config
          +s`/`llm_calls` rows are visibly                               
      622 +misplaced (a formatting defect, not a content one). This plan 
          +does not edit PLAN.md — see                                   
      623 +`research.md` Part 2 for the full reasoning and the recommenda
          +tion to correct PLAN.md §4.1 itself.                          
      624 +Also carried over: the constitution's Principle IV exemption l
          +ist (`contact_methods`, `kb_articles`                         
      625 +among tables it says need both `branch_id`/`department_id`) co
          +ntradicts PLAN.md's explicit S4                               
      626 +assignment for `contact_methods`; this plan follows PLAN.md (t
          +he authoritative domain-model                                 
      627 +source) per this turn's explicit instruction, and flags the co
          +nstitution text as due for a                                  
      628 +wording amendment, not a content one — `/speckit.constitution`
          + is the correct channel for that,                             
      629 +not this plan.                                                
      630 +                                                              
      631 +### Scoping-pattern verification (PLAN.md §4.1, this turn's ex
          +plicit check)                                                 
      632 +                                                              
      633 +Every table in `data-model.md` §2 was checked individually aga
          +inst its assigned pattern:                                    
      634 +                                                              
      635 +- **No S4 or S6 table carries `branch_id` or `department_id`.*
          +* Verified for all nine                                       
      636 +  (`branches`, `roles`, `permissions`, `role_permissions` — S6
          +; `team_members`, `contact_methods`,                          
      637 +  `ticket_events`, `kb_article_chunks` — S4). None has either 
          +column, in `data-model.md` or in any                          
      638 +  `contracts/openapi.yaml` schema.                            
      639 +- **`ChannelService.ingest()` (§Service Classes) constructs a 
          +fresh `TenantScope` from the                                  
      640 +  message's resolved branch/department**, not the calling API 
          +key's own scope — checked                                     
      641 +  specifically because reusing an API-key's scope here would h
          +ave been a subtle, easy-to-miss                               
      642 +  violation of FR-023's per-branch customer-identity requireme
          +nt despite every table still having                           
      643 +  the "correct" pattern on paper.                             
      644 +- **`ReportService`'s `cross_branch` flag is gated by a permis
          +sion check before it reaches                                  
      645 +  `TenantScope`**, never trusted from the request directly — c
          +hecked because `TenantScope.                                  
      646 +  cross_branch` is the one deliberate, narrow escape hatch fro
          +m `ScopedRepository`'s default                                
      647 +  filtering, and an ungated path to it would undermine Princip
          +le V everywhere it's used.                                    
      648 +- **No route handler in `app/api/routers/*` (per the file tree
          + above) contains a permission check                           
      649 +  or an audit write** — both decorators are applied at service
          +-method definition time only,                                 
      650 +  confirmed by design (§Shared Abstractions #2, #3) rather tha
          +n left implicit.                                              
      651 +                                                              
      652 +No further violations found. **No new functionality was added 
          +while fixing the three issues                                 
      653 +above** — each fix removed an invented field/value/exception r
          +ather than adding one, per this                               
      654 +turn's instruction.                                           

● Now the quickstart.md — a validation guide, not implementation code.

● Write(quickstart.md)
  ⎿  Wrote 154 lines to quickstart.md
       1 # Quickstart: Bilingual Support CRM — Core Product
       2 
         nstrable, seeded system, against
       4 PLAN.md §10's Definition of Done. This is a run/validation guide
          — implementation code lives in
       6 
       7 ## Prerequisites
       8 
      10 - A configured LLM endpoint reachable from the host (`LITELLM_AP
         I_BASE`, `LITELLM_API_KEY`,
     … +222 lines (ctrl+o to expand)

  Searched for 1 pattern (ctrl+o to expand)

● All footnotes were updated. Let's do a final listing to confirm every
  deliverable is in place.

  Listed 1 directory (ctrl+o to expand)

● Plan complete

  Branch: 001-bilingual-support-crm | Plan:
  specs/001-bilingual-support-crm/plan.md

  Generated:
  - research.md — every tech decision sourced from docs/architecture/stack.md,
    the two documentation gaps found in PLAN.md §4.1 (resolved, not invented),
    and the Tier D compatibility check against specs/002–007
  - data-model.md — all 27 tables, full fields/FKs/indexes, complete S1–S6
    assignment, status_transitions seed data, constraint-enforcement map
  - contracts/openapi.yaml — 70 Tier M/S endpoints, validated (no dangling
    $refs)
  - plan.md — full backend/frontend file tree, exact signatures for the five
    shared abstractions (ScopedRepository, require_permission, audited,
    ChannelAdapter, LiteLlmWrapper), the generic admin-CRUD pattern, every
    service class's public methods
  - quickstart.md — docker compose up → seeded demo → PLAN.md §10 Definition of
    Done, as a validation guide

  The self-audit you asked for at the end found three real issues, fixed in
  place (not left for later):
  1. data-model.md had uniformly applied is_active/sort_order to all ten
     reference-data tables, but PLAN.md §4.2 lists them inconsistently per table
     (e.g. roles has neither). Fixed by matching PLAN.md exactly per table —
     this also meant switching roles, permissions, priorities, ticket_statuses,
     sla_policies, quick_replies, teams from "deactivate" to a real,
     RESTRICT-guarded DELETE in both the data model and the OpenAPI contract,
     since they have no flag to soft-deactivate.
  2. ticket_events.event_type had gained an invented 'branch_corrected' value
     for the needs-triage correction event. Fixed — it's now a field_changed
     event reusing the existing shape, and PLAN.md's eleven-value list is
     unchanged.
  3. role_permissions had an inconsistent, unjustified exception from the "every
     mutable table gets updated_at/updated_by" rule that
     user_roles/team_members didn't share. Fixed — it now inherits normally like
     every other table.

  Two things not fixed here, flagged for you: PLAN.md §4.1's own Assignment
  table is missing six rows and has a formatting defect
  (channel_configs/llm_calls orphaned above the table) — I filled the gaps in
  data-model.md using PLAN.md's own pattern definitions rather than editing
  PLAN.md itself. Separately, the constitution's Principle IV still lists
  contact_methods/kb_articles as needing both branch_id+department_id, which
  contradicts PLAN.md's explicit S4 assignment for contact_methods — this plan
  follows PLAN.md (the authoritative source) per your instruction, but the
  constitution's wording is due for a cleanup pointing at PLAN.md §4.1 instead
  of duplicating it.

  Also still outstanding from the constitution phase: docs/DEBT.md doesn't exist
  yet — needed before batch 4a.

  Ready for /speckit.tasks.

✻ Cogitated for 27m 36s

※ recap: We're building out the spec-kit plan for the bilingual support CRM in specs/001-bilingual-support-crm; the full plan, data model, API contract, and quickstart are done and self-audited. Next: run /speckit.tasks to generate the implementation task list.

❯ /speckit.tasks Generate tasks for Tier M and Tier S requirements only. No     
  tasks for anything                                                            
  marked Tier D.                                                                
                                                                                
  Use the batch structure in PLAN.md section 6 exactly — batches 4a through 4i, 
  in that order.                                                                
  Order tasks by dependency within each batch and mark which can run in         
  parallel.                                                                     
                                                                                
  Each batch must end with the verification step named in PLAN.md section 6's   
  gate column,                                                                  
  expressed as something checkable without reading code.                        
                                                                                
  Include the seed data in PLAN.md section 7 as tasks in batch 4i.              
                                                                                
  Batch 4a must include creating the five shared abstractions whose signatures  
  are specified in                                                              
  plan.md — ScopedRepository, require_permission, audited, ChannelAdapter,      
  LiteLlmWrapper —                                                              
  before any entity work begins. Every later batch depends on them existing.    
                                                                                
  Each task must name the specific files it creates or modifies, using the file 
  tree in plan.md.                                                              
  A task that says "implement the customer service" without naming paths is     
  underspecified. /speckit-analyze                                              

● Skill(speckit-tasks)
  ⎿  Successfully loaded skill

  Read 1 file, ran 1 shell command (ctrl+o to expand)

● Write(tasks.md)
  ⎿  Wrote 541 lines to tasks.md
       1 ---
       2 description: "Task list for Bilingual Support CRM — Tier M/S imp
       3 ---
       4 
       5 # Tasks: Bilingual Support CRM — Core Product
       7 **Input**: `plan.md`, `data-model.md`, `contracts/openapi.yaml`,
          `research.md`, `quickstart.md`,
       8 `spec.md` — all in `specs/001-bilingual-support-crm/`
      10 **Scope**: Tier M and Tier S requirements only. No task exists a
         nywhere in this document for a
     … +981 lines (ctrl+o to expand)

  Searched for 1 pattern, read 1 file (ctrl+o to expand)

● Update(tasks.md)
  ⎿  Added 6 lines, removed 3 lines
      460        statuses with the full `status_transitions` table from `
           data-model.md` §4, 3 SLA policies,
      461        8 quick replies, 2 `channel_configs` rows (one per depar
           tment), and the full seeded
      462        permission set from `data-model.md` §5 — **including `au
           dit.read` and `report.cross_branch`
      463 -      granted to the seeded admin**, per PLAN.md §7's explicit
          - instruction (depends on T030, T050,                          
      464 -      T136 itself is the last piece needing every model to exi
          -st, i.e. depends on all of Batch                              
      465 -      4a's models plus every entity built in Batches 4b–4h)   
      463 +      granted to the seeded admin**, per PLAN.md §7's explicit
          + instruction. This task's seed                                
      464 +      script writes rows through every model created in Batch 
          +4a and touches functionality from                             
      465 +      every batch since (customers, tickets, KB articles, SLA 
          +policies, channel configs), so it                             
      466 +      depends on T030 (full schema) and, transitively, on ever
          +y batch's models being complete —                             
      467 +      in practice this is the last task started, after Batch 4
          +h's T116 (depends on T030, T136 is                            
      468 +      scheduled last in this batch)                           
      469  - [ ] T137 Run `docker compose exec backend python -m app.seed
           .seed` twice in succession and
      470        diff row counts — must be identical, confirming idempote
           ncy (depends on T136)
      471  

  Searched for 2 patterns, ran 1 shell command (ctrl+o to expand)

● /speckit.tasks complete — 137 tasks in
  specs/001-bilingual-support-crm/tasks.md, organized by PLAN.md §6's nine
  batches (not by user story, per your explicit override), 76 marked [P], every
  task naming exact file paths from plan.md's tree, each batch ending in the
  checkable gate from PLAN.md §6's own gate column. Batch 4a builds all five
  shared abstractions before any entity work. Seed data is T136–T137 in batch
  4i. No Tier D task exists anywhere.

  Now running /speckit-analyze.

● Skill(speckit-analyze)
  ⎿  Successfully loaded skill
  Searched for 10 patterns, ran 4 shell commands (ctrl+o to expand)

● Specification Analysis Report

  Scope: spec.md (77 FR entries, 65 Tier M/S), plan.md, data-model.md,
  contracts/openapi.yaml, tasks.md (137 tasks), research.md, quickstart.md,
  .specify/memory/constitution.md.

  ID: D1
  Category: Constitution Alignment
  Severity: CRITICAL
  Location(s): contracts/openapi.yaml (all AdminList_*/AdminGet_* for Category,
  Priority, TicketStatus, SlaPolicy, Team, QuickReply, Branch, Department),
  plan.md §Generic CRUD Pattern
  Summary: The Generic CRUD Pattern's list/get reuse the same permission class
  attribute as create/update/remove — all set to admin.config. Only the admin
  role is seeded with admin.config (data-model.md §5). This means an ordinary
  Agent cannot read categories, priorities, ticket-statuses,  SLA policies, 
  teams, or quick replies — breaking FR-016 (category/priority selection on
  ticket create), FR-019 (team assignment), and FR-029 (quick-reply insertion)
  for the exact actor those requirements name. Root cause is systemic (one
  shared permission attribute for both reads and writes), not per-entity.
  Recommendation: Split AdminCrudService into a read_permission (e.g.
  {entity}.read, granted broadly to agent/lead/admin) and a write_permission
  (admin.config, admin-only) — the convention data-model.md §5 already
  documents for kb_article/ticket/customer but never applied to the ten
  Generic-CRUD entities. Update plan.md, contracts/openapi.yaml's x-permission
  on every AdminList_*/AdminGet_*, data-model.md §5's permission table, and
  seed grants in tasks.md T136.
  ────────────────────────────────────────
  ID: F1
  Category: Inconsistency
  Severity: HIGH
  Location(s): plan.md §Service Classes (AssignmentService.auto_assign_ticket)
  vs. tasks.md T073, T096, T112
  Summary: plan.md states auto_assign_ticket is "called by categorization_job
  after categorization completes, and directly by TicketService.create when the
   ticket has no assignee/team set." tasks.md's actual task breakdown never has
   T073 (TicketService.create) call it directly — only the job does (once, in
  T096/T112). If an implementer follows plan.md's prose literally,
  auto_assign_ticket gets called twice per ticket (a synchronous call from
  create() plus the async job's call), which can double-assign or race against
  categorization.
  Recommendation: Correct plan.md's AssignmentService bullet to state the single

  call site tasks.md actually builds: only via categorization_job, never
  directly from TicketService.create.
  ────────────────────────────────────────
  ID: D2
  Category: Constitution Alignment
  Severity: HIGH
  Location(s): .specify/memory/constitution.md Principle III vs.
  plan.md/tasks.md
  T087 / quickstart.md §10
  Summary: The constitution's stated enforcement command for RTL parity greps
  web/**/*.{ts,tsx,css}. The project's actual frontend directory, fixed in
  plan.md's file tree and used consistently in tasks.md (T087) and
  quickstart.md, is frontend/. Run literally, the constitution's own
  verification command matches zero files and would falsely appear to pass.
  Recommendation: Amend constitution Principle III's enforcement-point path from

  web/**/* to frontend/**/* (a wording fix, via /speckit.constitution — out of
  scope for this command to apply).
  ────────────────────────────────────────
  ID: D3
  Category: Constitution Alignment
  Severity: CRITICAL (pre-existing, tracked)
  Location(s): .specify/memory/constitution.md Principle IV vs. PLAN.md §4.1 /
  data-model.md §2
  Summary: Already surfaced and deliberately resolved during /speckit.plan (see
  research.md Part 2, plan.md §Post-Design Constitution Check): Principle IV's
  exemption list still says contact_methods/kb_articles must carry both
  branch_id+department_id, contradicting PLAN.md's explicit S4 assignment for
  contact_methods. This plan follows PLAN.md (authoritative domain-model
  source) per explicit instruction in that turn. Flagging again here only
  because /speckit-analyze's own rules mark every constitution conflict
  CRITICAL regardless of prior discussion.
  Recommendation: No new action — re-confirms the existing recommendation:
  reword
  Principle IV to point at "PLAN.md §4.1's per-table assignment" instead of
  maintaining a second, drifting list.
  ────────────────────────────────────────
  ID: E1
  Category: Coverage Gap
  Severity: HIGH
  Location(s): spec.md FR-001 / constitution Principle I ("Enforcement point:
  lint rule + review") vs. tasks.md
  Summary: No task creates an automated lint rule catching
  hardcoded/non-externalized user-facing strings, unlike the structurally
  identical FR-008/Principle VIII case, which does get one (T038,
  lint_no_vendor_sdk.py). i18n itself is structurally present
  (messages/ar.json/en.json, next-intl), but the stated enforcement mechanism
  is unbuilt.
  Recommendation: Add a task in Batch 4a or the Polish phase: an ESLint rule (or

  a grep-based CI check) flagging JSX string literals not routed through
  next-intl's translation functions.
  ────────────────────────────────────────
  ID: C1
  Category: Underspecification
  Severity: MEDIUM
  Location(s): spec.md FR-016, plan.md TicketService.create, tasks.md T073
  Summary: FR-016 requires category/priority to be active and, if
  department-scoped, to match the ticket's department. plan.md's
  TicketService.create signature description names reference-number generation,
   SLA policy resolution, and the categorization job — but never this
  validation. T073 inherits the same omission ("never write 'as needed'" — this
   is exactly that gap).
  Recommendation: Add the FR-016 validation explicitly to plan.md's
  TicketService.create bullet and to T073's description before implementation
  starts.
  ────────────────────────────────────────
  ID: E2
  Category: Coverage Gap
  Severity: MEDIUM
  Location(s): spec.md FR-003/SC-007 (375px) vs. tasks.md
  Summary: No task verifies the 375px-width usability requirement directly —
  T087
  only checks the RTL-utility grep, not a viewport. Responsive behavior is
  presumably inherent to using Tailwind, but nothing in tasks.md makes it
  checkable.
  Recommendation: Add a manual/automated viewport check (e.g. a
  Playwright/Storybook viewport test, or an explicit line in quickstart.md's
  Step-by-step) at 375px for the dashboard and ticket-detail pages.
  ────────────────────────────────────────
  ID: F2
  Category: Terminology
  Severity: LOW
  Location(s): —
  Summary: Spot-checked report.cross_branch, audit.read, ticket.sla_override,
  ScopedRepository, TenantScope, needs_triage across all six artifacts — all
  consistent, no drift found.
  Recommendation: None — noted for completeness.

  Coverage Summary Table (grouped by feature area; Tier D FRs excluded per Scope
  Discipline):

  Feature Area: Platform-wide (F12)
  FR Keys: FR-001–010, 072, 073
  Has Task?: Partial
  Task IDs: T038 (partial), T041/T044/T087
  Notes: FR-001's lint enforcement missing (E1); FR-003 untested (E2)
  ────────────────────────────────────────
  Feature Area: F01 Customer
  FR Keys: FR-011–014
  Has Task?: Yes
  Task IDs: T060–067
  Notes: —
  ────────────────────────────────────────
  Feature Area: F02 Ticket
  FR Keys: FR-015–021
  Has Task?: Yes
  Task IDs: T068–079
  Notes: FR-016 validation underspecified (C1)
  ────────────────────────────────────────
  Feature Area: F03 Channels
  FR Keys: FR-022–025, 023a/b/c
  Has Task?: Yes
  Task IDs: T117–123
  Notes: —
  ────────────────────────────────────────
  Feature Area: F04 Dashboard
  FR Keys: FR-026–031
  Has Task?: Yes
  Task IDs: T072, T080–087
  Notes: —
  ────────────────────────────────────────
  Feature Area: F05 SLA
  FR Keys: FR-033–039
  Has Task?: Yes
  Task IDs: T088–100
  Notes: AssignmentService call-site inconsistency (F1)
  ────────────────────────────────────────
  Feature Area: F06 KB
  FR Keys: FR-041–043
  Has Task?: Yes
  Task IDs: T101–106
  Notes: —
  ────────────────────────────────────────
  Feature Area: F07 AI
  FR Keys: FR-044–050
  Has Task?: Yes
  Task IDs: T107–116
  Notes: —
  ────────────────────────────────────────
  Feature Area: F08 Portal
  FR Keys: FR-052–055
  Has Task?: Yes
  Task IDs: T124, T129, T133
  Notes: —
  ────────────────────────────────────────
  Feature Area: F09 Reports
  FR Keys: FR-059–060
  Has Task?: Yes
  Task IDs: T125, T130, T134
  Notes: —
  ────────────────────────────────────────
  Feature Area: F10 Security
  FR Keys: FR-063–066
  Has Task?: Yes
  Task IDs: T046–059
  Notes: Reference-data reads gated by admin.config (D1)
  ────────────────────────────────────────
  Feature Area: F11 Integrations
  FR Keys: FR-068–069
  Has Task?: Yes
  Task IDs: T126–128, T131–132, T135
  Notes: —

  Constitution Alignment Issues: D1 (new, CRITICAL), D2 (new, HIGH), D3
  (pre-existing, tracked).

  Unmapped Tasks: None found — every task traces to a batch's PLAN.md §6 content
  or a shared abstraction all batches depend on.

  Metrics:
  - Total Requirements (Tier M/S): 65
  - Total Tasks: 137
  - Coverage (≥1 task): ~97% (63/65 — the 2 without a dedicated task are E1/E2,
    both enforcement/verification gaps, not missing functionality)
  - Ambiguity Count: 0 (no vague adjectives, no unresolved placeholders)
  - Duplication Count: 0
  - Critical Issues Count: 2 (D1 new, D3 pre-existing)

  Next Actions

  D1 is a real, launch-blocking bug — as designed, an Agent gets 403 trying to
  populate a ticket's category/priority dropdown or read quick replies. Resolve
  before /speckit-implement reaches Batch 4b (where admin_crud_service.py is
  built) or Batch 4d/4e (where agents start consuming these lists) — whichever
  comes first in your actual work order.

  F1 and C1 are cheap to fix now (documentation-only, no schema/contract change)
  and should also land before implementation starts, to avoid an implementer
  inventing behavior plan.md doesn't specify.

  D2/D3 require a constitution amendment, not a spec/plan/tasks edit — run
  /speckit.constitution separately when convenient; neither blocks starting
  Batch 4a.

  E1/E2 are lower-cost, can be folded into the Batch 4a/Polish task list at your
  discretion.

  Suggested commands:
  - Manually patch plan.md + contracts/openapi.yaml + data-model.md §5 for D1's
    read/write permission split, then re-run /speckit-tasks (or hand-edit
    tasks.md's affected tasks) to match.
  - Manually correct the AssignmentService bullet in plan.md for F1.
  - Run /speckit.constitution to reword Principle III's path and Principle IV's
    exemption list.

  Remediation

  Would you like me to suggest concrete remediation edits for D1, F1, and C1
  (the three actionable-now findings)? I will not apply anything without your
  explicit go-ahead.

✻ Crunched for 10m 57s

※ recap: Building the CRM spec-kit pipeline (constitution → spec → plan → tasks → analyze) for the bilingual support CRM; analysis just finished. Waiting on your go-ahead to apply the three fixes found (agent permissions, assignment double-call, ticket validation gap).

❯ Apply remediation for D1, F1, and C1. Do not touch anything else.             
                                                                                
  D1 — Split the Generic CRUD Pattern's permission attribute into               
  read_permission and                                                           
  write_permission. read_permission is {entity}.read, granted to agent, lead,   
  and admin;                                                                    
  write_permission stays admin.config, admin-only. Apply across plan.md's       
  Generic CRUD Pattern,                                                         
  every AdminList_*/AdminGet_* x-permission in contracts/openapi.yaml,          
  data-model.md §5's                                                            
  permission table, and the seed grants in T136. Then hand-edit the affected    
  tasks in tasks.md                                                             
  to match — do not re-run /speckit.tasks, the rest of the task list is         
  correct.                                                                      
                                                                                
  F1 — Correct plan.md's AssignmentService bullet: auto_assign_ticket is called 
  only by                                                                       
  categorization_job, never directly from TicketService.create.                 
                                                                                
  C1 — Add FR-016 validation explicitly to plan.md's TicketService.create       
  bullet and to T073:                                                           
  category and priority must be active, and if department-scoped must match the 
  ticket's                                                                      
  department; a violation is rejected with a localized error.                   
                                                                                
  Then two cheap additions:                                                     
  - Add a task in batch 4a: a grep-based check flagging JSX string literals not 
  routed through                                                                
    next-intl. A grep script, not an ESLint rule.                               
  - Add a line to quickstart.md's step-by-step: manually verify the dashboard   
  and ticket-detail                                                             
    pages at 375px viewport width.                                              

  Searched for 1 pattern (ctrl+o to expand)

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      993      # below requires x-permission: admin.config (PLAN.md F10: 
           "Admin CRUD for branches,
      994      # departments, users, roles, categories, priorities, statu
           ses, transitions, SLA policies,
      995      # quick replies" is one permission, not one per entity — s
           ee data-model.md §5).
      996 -    AdminList_Branch: { get: { tags: [admin], operationId: lis
          -tBranches, x-permission: admin.config, parameters: [{ $ref: "#
          -/components/parameters/Limit" }, { $ref: "#/components/paramet
          -ers/Offset" }], responses: { "200": { description: OK, content
          -: { application/json: { schema: { type: array, items: { $ref: 
          -"#/components/schemas/Branch" } } } } } } } }                 
      996 +    AdminList_Branch: { get: { tags: [admin], operationId: lis
          +tBranches, x-permission: branch.read, parameters: [{ $ref: "#/
          +components/parameters/Limit" }, { $ref: "#/components/paramete
          +rs/Offset" }], responses: { "200": { description: OK, content:
          + { application/json: { schema: { type: array, items: { $ref: "
          +#/components/schemas/Branch" } } } } } } } }                  
      997      AdminCreate_Branch: { post: { tags: [admin], operationId: 
           createBranch, x-permission: admin.config, requestBody: { requi
           red: true, content: { application/json: { schema: { $ref: "#/c
           omponents/schemas/BranchCreate" } } } }, responses: { "201": {
            description: Created, content: { application/json: { schema: 
           { $ref: "#/components/schemas/Branch" } } } } } } }
      998      AdminGet_Branch: { get: { tags: [admin], operationId: getB
           ranch, x-permission: admin.config, parameters: [{ $ref: "#/com
           ponents/parameters/PathId" }], responses: { "200": { descripti
           on: OK, content: { application/json: { schema: { $ref: "#/comp
           onents/schemas/Branch" } } } }, "404": { $ref: "#/components/r
           esponses/NotFound" } } } }
      999      AdminUpdate_Branch: { patch: { tags: [admin], operationId:
            updateBranch, x-permission: admin.config, parameters: [{ $ref
           : "#/components/parameters/PathId" }], requestBody: { required
           : true, content: { application/json: { schema: { $ref: "#/comp
           onents/schemas/BranchUpdate" } } } }, responses: { "200": { de
           scription: OK, content: { application/json: { schema: { $ref: 
           "#/components/schemas/Branch" } } } } } } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
       995      # quick replies" is one permission, not one per entity — 
            see data-model.md §5).
       996      AdminList_Branch: { get: { tags: [admin], operationId: li
            stBranches, x-permission: branch.read, parameters: [{ $ref: "
            #/components/parameters/Limit" }, { $ref: "#/components/param
            eters/Offset" }], responses: { "200": { description: OK, cont
            ent: { application/json: { schema: { type: array, items: { $r
            ef: "#/components/schemas/Branch" } } } } } } } }
       997      AdminCreate_Branch: { post: { tags: [admin], operationId:
             createBranch, x-permission: admin.config, requestBody: { req
            uired: true, content: { application/json: { schema: { $ref: "
            #/components/schemas/BranchCreate" } } } }, responses: { "201
            ": { description: Created, content: { application/json: { sch
            ema: { $ref: "#/components/schemas/Branch" } } } } } } }
       998 -    AdminGet_Branch: { get: { tags: [admin], operationId: get
           -Branch, x-permission: admin.config, parameters: [{ $ref: "#/c
           -omponents/parameters/PathId" }], responses: { "200": { descri
           -ption: OK, content: { application/json: { schema: { $ref: "#/
           -components/schemas/Branch" } } } }, "404": { $ref: "#/compone
           -nts/responses/NotFound" } } } }                              
       998 +    AdminGet_Branch: { get: { tags: [admin], operationId: get
           +Branch, x-permission: branch.read, parameters: [{ $ref: "#/co
           +mponents/parameters/PathId" }], responses: { "200": { descrip
           +tion: OK, content: { application/json: { schema: { $ref: "#/c
           +omponents/schemas/Branch" } } } }, "404": { $ref: "#/componen
           +ts/responses/NotFound" } } } }                               
       999      AdminUpdate_Branch: { patch: { tags: [admin], operationId
            : updateBranch, x-permission: admin.config, parameters: [{ $r
            ef: "#/components/parameters/PathId" }], requestBody: { requi
            red: true, content: { application/json: { schema: { $ref: "#/
            components/schemas/BranchUpdate" } } } }, responses: { "200":
             { description: OK, content: { application/json: { schema: { 
            $ref: "#/components/schemas/Branch" } } } } } } }
      1000      AdminDeactivate_Branch: { post: { tags: [admin], operatio
            nId: deactivateBranch, x-permission: admin.config, parameters
            : [{ $ref: "#/components/parameters/PathId" }], responses: { 
            "200": { description: OK, content: { application/json: { sche
            ma: { $ref: "#/components/schemas/Branch" } } } } } } }
      1001  

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
       999      AdminUpdate_Branch: { patch: { tags: [admin], operationId
            : updateBranch, x-permission: admin.config, parameters: [{ $r
            ef: "#/components/parameters/PathId" }], requestBody: { requi
            red: true, content: { application/json: { schema: { $ref: "#/
            components/schemas/BranchUpdate" } } } }, responses: { "200":
             { description: OK, content: { application/json: { schema: { 
            $ref: "#/components/schemas/Branch" } } } } } } }
      1000      AdminDeactivate_Branch: { post: { tags: [admin], operatio
            nId: deactivateBranch, x-permission: admin.config, parameters
            : [{ $ref: "#/components/parameters/PathId" }], responses: { 
            "200": { description: OK, content: { application/json: { sche
            ma: { $ref: "#/components/schemas/Branch" } } } } } } }
      1001  
      1002 -    AdminList_Department: { get: { tags: [admin], operationId
           -: listDepartments, x-permission: admin.config, parameters: [{
           - $ref: "#/components/parameters/Limit" }, { $ref: "#/componen
           -ts/parameters/Offset" }], responses: { "200": { description: 
           -OK, content: { application/json: { schema: { type: array, ite
           -ms: { $ref: "#/components/schemas/Department" } } } } } } } }
      1002 +    AdminList_Department: { get: { tags: [admin], operationId
           +: listDepartments, x-permission: department.read, parameters:
           + [{ $ref: "#/components/parameters/Limit" }, { $ref: "#/compo
           +nents/parameters/Offset" }], responses: { "200": { descriptio
           +n: OK, content: { application/json: { schema: { type: array, 
           +items: { $ref: "#/components/schemas/Department" } } } } } } 
           +} }                                                          
      1003      AdminCreate_Department: { post: { tags: [admin], operatio
            nId: createDepartment, x-permission: admin.config, requestBod
            y: { required: true, content: { application/json: { schema: {
             $ref: "#/components/schemas/DepartmentCreate" } } } }, respo
            nses: { "201": { description: Created, content: { application
            /json: { schema: { $ref: "#/components/schemas/Department" } 
            } } } } } }
      1004      AdminGet_Department: { get: { tags: [admin], operationId:
             getDepartment, x-permission: admin.config, parameters: [{ $r
            ef: "#/components/parameters/PathId" }], responses: { "200": 
            { description: OK, content: { application/json: { schema: { $
            ref: "#/components/schemas/Department" } } } } } } }
      1005      AdminUpdate_Department: { patch: { tags: [admin], operati
            onId: updateDepartment, x-permission: admin.config, parameter
            s: [{ $ref: "#/components/parameters/PathId" }], requestBody:
             { required: true, content: { application/json: { schema: { $
            ref: "#/components/schemas/DepartmentUpdate" } } } }, respons
            es: { "200": { description: OK, content: { application/json: 
            { schema: { $ref: "#/components/schemas/Department" } } } } }
             } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1001  
      1002      AdminList_Department: { get: { tags: [admin], operationId
            : listDepartments, x-permission: department.read, parameters:
             [{ $ref: "#/components/parameters/Limit" }, { $ref: "#/compo
            nents/parameters/Offset" }], responses: { "200": { descriptio
            n: OK, content: { application/json: { schema: { type: array, 
            items: { $ref: "#/components/schemas/Department" } } } } } } 
            } }
      1003      AdminCreate_Department: { post: { tags: [admin], operatio
            nId: createDepartment, x-permission: admin.config, requestBod
            y: { required: true, content: { application/json: { schema: {
             $ref: "#/components/schemas/DepartmentCreate" } } } }, respo
            nses: { "201": { description: Created, content: { application
            /json: { schema: { $ref: "#/components/schemas/Department" } 
            } } } } } }
      1004 -    AdminGet_Department: { get: { tags: [admin], operationId:
           - getDepartment, x-permission: admin.config, parameters: [{ $r
           -ef: "#/components/parameters/PathId" }], responses: { "200": 
           -{ description: OK, content: { application/json: { schema: { $
           -ref: "#/components/schemas/Department" } } } } } } }         
      1004 +    AdminGet_Department: { get: { tags: [admin], operationId:
           + getDepartment, x-permission: department.read, parameters: [{
           + $ref: "#/components/parameters/PathId" }], responses: { "200
           +": { description: OK, content: { application/json: { schema: 
           +{ $ref: "#/components/schemas/Department" } } } } } } }      
      1005      AdminUpdate_Department: { patch: { tags: [admin], operati
            onId: updateDepartment, x-permission: admin.config, parameter
            s: [{ $ref: "#/components/parameters/PathId" }], requestBody:
             { required: true, content: { application/json: { schema: { $
            ref: "#/components/schemas/DepartmentUpdate" } } } }, respons
            es: { "200": { description: OK, content: { application/json: 
            { schema: { $ref: "#/components/schemas/Department" } } } } }
             } }
      1006      AdminDeactivate_Department: { post: { tags: [admin], oper
            ationId: deactivateDepartment, x-permission: admin.config, pa
            rameters: [{ $ref: "#/components/parameters/PathId" }], respo
            nses: { "200": { description: OK, content: { application/json
            : { schema: { $ref: "#/components/schemas/Department" } } } }
             } } }
      1007  

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1005      AdminUpdate_Department: { patch: { tags: [admin], operati
            onId: updateDepartment, x-permission: admin.config, parameter
            s: [{ $ref: "#/components/parameters/PathId" }], requestBody:
             { required: true, content: { application/json: { schema: { $
            ref: "#/components/schemas/DepartmentUpdate" } } } }, respons
            es: { "200": { description: OK, content: { application/json: 
            { schema: { $ref: "#/components/schemas/Department" } } } } }
             } }
      1006      AdminDeactivate_Department: { post: { tags: [admin], oper
            ationId: deactivateDepartment, x-permission: admin.config, pa
            rameters: [{ $ref: "#/components/parameters/PathId" }], respo
            nses: { "200": { description: OK, content: { application/json
            : { schema: { $ref: "#/components/schemas/Department" } } } }
             } } }
      1007  
      1008 -    AdminList_User: { get: { tags: [admin], operationId: list
           -Users, x-permission: admin.config, parameters: [{ $ref: "#/co
           -mponents/parameters/Limit" }, { $ref: "#/components/parameter
           -s/Offset" }], responses: { "200": { description: OK, content:
           - { application/json: { schema: { type: array, items: { $ref: 
           -"#/components/schemas/User" } } } } } } } }                  
      1008 +    AdminList_User: { get: { tags: [admin], operationId: list
           +Users, x-permission: user.read, parameters: [{ $ref: "#/compo
           +nents/parameters/Limit" }, { $ref: "#/components/parameters/O
           +ffset" }], responses: { "200": { description: OK, content: { 
           +application/json: { schema: { type: array, items: { $ref: "#/
           +components/schemas/User" } } } } } } } }                     
      1009      AdminCreate_User: { post: { tags: [admin], operationId: c
            reateUser, x-permission: admin.config, requestBody: { require
            d: true, content: { application/json: { schema: { $ref: "#/co
            mponents/schemas/UserCreate" } } } }, responses: { "201": { d
            escription: Created, content: { application/json: { schema: {
             $ref: "#/components/schemas/User" } } } } } } }
      1010      AdminGet_User: { get: { tags: [admin], operationId: getUs
            er, x-permission: admin.config, parameters: [{ $ref: "#/compo
            nents/parameters/PathId" }], responses: { "200": { descriptio
            n: OK, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/User" } } } } } } }
      1011      AdminUpdate_User: { patch: { tags: [admin], operationId: 
            updateUser, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], requestBody: { required:
             true, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/UserUpdate" } } } }, responses: { "200": { des
            cription: OK, content: { application/json: { schema: { $ref: 
            "#/components/schemas/User" } } } } } } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1007  
      1008      AdminList_User: { get: { tags: [admin], operationId: list
            Users, x-permission: user.read, parameters: [{ $ref: "#/compo
            nents/parameters/Limit" }, { $ref: "#/components/parameters/O
            ffset" }], responses: { "200": { description: OK, content: { 
            application/json: { schema: { type: array, items: { $ref: "#/
            components/schemas/User" } } } } } } } }
      1009      AdminCreate_User: { post: { tags: [admin], operationId: c
            reateUser, x-permission: admin.config, requestBody: { require
            d: true, content: { application/json: { schema: { $ref: "#/co
            mponents/schemas/UserCreate" } } } }, responses: { "201": { d
            escription: Created, content: { application/json: { schema: {
             $ref: "#/components/schemas/User" } } } } } } }
      1010 -    AdminGet_User: { get: { tags: [admin], operationId: getUs
           -er, x-permission: admin.config, parameters: [{ $ref: "#/compo
           -nents/parameters/PathId" }], responses: { "200": { descriptio
           -n: OK, content: { application/json: { schema: { $ref: "#/comp
           -onents/schemas/User" } } } } } } }                           
      1010 +    AdminGet_User: { get: { tags: [admin], operationId: getUs
           +er, x-permission: user.read, parameters: [{ $ref: "#/componen
           +ts/parameters/PathId" }], responses: { "200": { description: 
           +OK, content: { application/json: { schema: { $ref: "#/compone
           +nts/schemas/User" } } } } } } }                              
      1011      AdminUpdate_User: { patch: { tags: [admin], operationId: 
            updateUser, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], requestBody: { required:
             true, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/UserUpdate" } } } }, responses: { "200": { des
            cription: OK, content: { application/json: { schema: { $ref: 
            "#/components/schemas/User" } } } } } } }
      1012      AdminDeactivate_User: { post: { tags: [admin], operationI
            d: deactivateUser, x-permission: admin.config, parameters: [{
             $ref: "#/components/parameters/PathId" }], responses: { "200
            ": { description: OK, content: { application/json: { schema: 
            { $ref: "#/components/schemas/User" } } } } } } }
      1013  

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1011      AdminUpdate_User: { patch: { tags: [admin], operationId: 
            updateUser, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], requestBody: { required:
             true, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/UserUpdate" } } } }, responses: { "200": { des
            cription: OK, content: { application/json: { schema: { $ref: 
            "#/components/schemas/User" } } } } } } }
      1012      AdminDeactivate_User: { post: { tags: [admin], operationI
            d: deactivateUser, x-permission: admin.config, parameters: [{
             $ref: "#/components/parameters/PathId" }], responses: { "200
            ": { description: OK, content: { application/json: { schema: 
            { $ref: "#/components/schemas/User" } } } } } } }
      1013  
      1014 -    AdminList_Role: { get: { tags: [admin], operationId: list
           -Roles, x-permission: admin.config, responses: { "200": { desc
           -ription: OK, content: { application/json: { schema: { type: a
           -rray, items: { $ref: "#/components/schemas/Role" } } } } } } 
           -} }                                                          
      1014 +    AdminList_Role: { get: { tags: [admin], operationId: list
           +Roles, x-permission: role.read, responses: { "200": { descrip
           +tion: OK, content: { application/json: { schema: { type: arra
           +y, items: { $ref: "#/components/schemas/Role" } } } } } } } }
      1015      AdminCreate_Role: { post: { tags: [admin], operationId: c
            reateRole, x-permission: admin.config, requestBody: { require
            d: true, content: { application/json: { schema: { $ref: "#/co
            mponents/schemas/RoleCreate" } } } }, responses: { "201": { d
            escription: Created, content: { application/json: { schema: {
             $ref: "#/components/schemas/Role" } } } } } } }
      1016      AdminGet_Role: { get: { tags: [admin], operationId: getRo
            le, x-permission: admin.config, parameters: [{ $ref: "#/compo
            nents/parameters/PathId" }], responses: { "200": { descriptio
            n: OK, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/Role" } } } } } } }
      1017      AdminUpdate_Role: { patch: { tags: [admin], operationId: 
            updateRole, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], requestBody: { required:
             true, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/RoleUpdate" } } } }, responses: { "200": { des
            cription: OK, content: { application/json: { schema: { $ref: 
            "#/components/schemas/Role" } } } } } } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1013  
      1014      AdminList_Role: { get: { tags: [admin], operationId: list
            Roles, x-permission: role.read, responses: { "200": { descrip
            tion: OK, content: { application/json: { schema: { type: arra
            y, items: { $ref: "#/components/schemas/Role" } } } } } } } }
      1015      AdminCreate_Role: { post: { tags: [admin], operationId: c
            reateRole, x-permission: admin.config, requestBody: { require
            d: true, content: { application/json: { schema: { $ref: "#/co
            mponents/schemas/RoleCreate" } } } }, responses: { "201": { d
            escription: Created, content: { application/json: { schema: {
             $ref: "#/components/schemas/Role" } } } } } } }
      1016 -    AdminGet_Role: { get: { tags: [admin], operationId: getRo
           -le, x-permission: admin.config, parameters: [{ $ref: "#/compo
           -nents/parameters/PathId" }], responses: { "200": { descriptio
           -n: OK, content: { application/json: { schema: { $ref: "#/comp
           -onents/schemas/Role" } } } } } } }                           
      1016 +    AdminGet_Role: { get: { tags: [admin], operationId: getRo
           +le, x-permission: role.read, parameters: [{ $ref: "#/componen
           +ts/parameters/PathId" }], responses: { "200": { description: 
           +OK, content: { application/json: { schema: { $ref: "#/compone
           +nts/schemas/Role" } } } } } } }                              
      1017      AdminUpdate_Role: { patch: { tags: [admin], operationId: 
            updateRole, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], requestBody: { required:
             true, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/RoleUpdate" } } } }, responses: { "200": { des
            cription: OK, content: { application/json: { schema: { $ref: 
            "#/components/schemas/Role" } } } } } } }
      1018      AdminDelete_Role: { delete: { tags: [admin], operationId:
             deleteRole, x-permission: admin.config, description: "No is_
            active column (data-model.md §0.4) — hard delete, RESTRICT-gu
            arded by user_roles/role_permissions.", parameters: [{ $ref: 
            "#/components/parameters/PathId" }], responses: { "204": { de
            scription: Deleted } } } }
      1019  

● Update(contracts\openapi.yaml)  ⎿  Added 1 line, removed 1 line
      1017      AdminUpdate_Role: { patch: { tags: [admin], operationId: 
            updateRole, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], requestBody: { required:
             true, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/RoleUpdate" } } } }, responses: { "200": { des
            cription: OK, content: { application/json: { schema: { $ref: 
            "#/components/schemas/Role" } } } } } } }
      1018      AdminDelete_Role: { delete: { tags: [admin], operationId:
             deleteRole, x-permission: admin.config, description: "No is_
            active column (data-model.md §0.4) — hard delete, RESTRICT-gu
            arded by user_roles/role_permissions.", parameters: [{ $ref: 
            "#/components/parameters/PathId" }], responses: { "204": { de
            scription: Deleted } } } }
      1019  
      1020 -    AdminList_Category: { get: { tags: [admin], operationId: 
           -listCategories, x-permission: admin.config, parameters: [{ $r
           -ef: "#/components/parameters/Limit" }, { $ref: "#/components/
           -parameters/Offset" }], responses: { "200": { description: OK,
           - content: { application/json: { schema: { type: array, items:
           - { $ref: "#/components/schemas/Category" } } } } } } } }     
      1020 +    AdminList_Category: { get: { tags: [admin], operationId: 
           +listCategories, x-permission: category.read, parameters: [{ $
           +ref: "#/components/parameters/Limit" }, { $ref: "#/components
           +/parameters/Offset" }], responses: { "200": { description: OK
           +, content: { application/json: { schema: { type: array, items
           +: { $ref: "#/components/schemas/Category" } } } } } } } }    
      1021      AdminCreate_Category: { post: { tags: [admin], operationI
            d: createCategory, x-permission: admin.config, requestBody: {
             required: true, content: { application/json: { schema: { $re
            f: "#/components/schemas/CategoryCreate" } } } }, responses: 
            { "201": { description: Created, content: { application/json:
             { schema: { $ref: "#/components/schemas/Category" } } } } } 
            } }
      1022      AdminGet_Category: { get: { tags: [admin], operationId: g
            etCategory, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], responses: { "200": { de
            scription: OK, content: { application/json: { schema: { $ref:
             "#/components/schemas/Category" } } } } } } }
      1023      AdminUpdate_Category: { patch: { tags: [admin], operation
            Id: updateCategory, x-permission: admin.config, parameters: [
            { $ref: "#/components/parameters/PathId" }], requestBody: { r
            equired: true, content: { application/json: { schema: { $ref:
             "#/components/schemas/CategoryUpdate" } } } }, responses: { 
            "200": { description: OK, content: { application/json: { sche
            ma: { $ref: "#/components/schemas/Category" } } } } } } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1019  
      1020      AdminList_Category: { get: { tags: [admin], operationId: 
            listCategories, x-permission: category.read, parameters: [{ $
            ref: "#/components/parameters/Limit" }, { $ref: "#/components
            /parameters/Offset" }], responses: { "200": { description: OK
            , content: { application/json: { schema: { type: array, items
            : { $ref: "#/components/schemas/Category" } } } } } } } }
      1021      AdminCreate_Category: { post: { tags: [admin], operationI
            d: createCategory, x-permission: admin.config, requestBody: {
             required: true, content: { application/json: { schema: { $re
            f: "#/components/schemas/CategoryCreate" } } } }, responses: 
            { "201": { description: Created, content: { application/json:
             { schema: { $ref: "#/components/schemas/Category" } } } } } 
            } }
      1022 -    AdminGet_Category: { get: { tags: [admin], operationId: g
           -etCategory, x-permission: admin.config, parameters: [{ $ref: 
           -"#/components/parameters/PathId" }], responses: { "200": { de
           -scription: OK, content: { application/json: { schema: { $ref:
           - "#/components/schemas/Category" } } } } } } }               
      1022 +    AdminGet_Category: { get: { tags: [admin], operationId: g
           +etCategory, x-permission: category.read, parameters: [{ $ref:
           + "#/components/parameters/PathId" }], responses: { "200": { d
           +escription: OK, content: { application/json: { schema: { $ref
           +: "#/components/schemas/Category" } } } } } } }              
      1023      AdminUpdate_Category: { patch: { tags: [admin], operation
            Id: updateCategory, x-permission: admin.config, parameters: [
            { $ref: "#/components/parameters/PathId" }], requestBody: { r
            equired: true, content: { application/json: { schema: { $ref:
             "#/components/schemas/CategoryUpdate" } } } }, responses: { 
            "200": { description: OK, content: { application/json: { sche
            ma: { $ref: "#/components/schemas/Category" } } } } } } }
      1024      AdminDeactivate_Category: { post: { tags: [admin], operat
            ionId: deactivateCategory, x-permission: admin.config, parame
            ters: [{ $ref: "#/components/parameters/PathId" }], responses
            : { "200": { description: OK, content: { application/json: { 
            schema: { $ref: "#/components/schemas/Category" } } } } } } }
      1025  

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1023      AdminUpdate_Category: { patch: { tags: [admin], operation
            Id: updateCategory, x-permission: admin.config, parameters: [
            { $ref: "#/components/parameters/PathId" }], requestBody: { r
            equired: true, content: { application/json: { schema: { $ref:
             "#/components/schemas/CategoryUpdate" } } } }, responses: { 
            "200": { description: OK, content: { application/json: { sche
            ma: { $ref: "#/components/schemas/Category" } } } } } } }
      1024      AdminDeactivate_Category: { post: { tags: [admin], operat
            ionId: deactivateCategory, x-permission: admin.config, parame
            ters: [{ $ref: "#/components/parameters/PathId" }], responses
            : { "200": { description: OK, content: { application/json: { 
            schema: { $ref: "#/components/schemas/Category" } } } } } } }
      1025  
      1026 -    AdminList_Priority: { get: { tags: [admin], operationId: 
           -listPriorities, x-permission: admin.config, responses: { "200
           -": { description: OK, content: { application/json: { schema: 
           -{ type: array, items: { $ref: "#/components/schemas/Priority"
           - } } } } } } } }                                             
      1026 +    AdminList_Priority: { get: { tags: [admin], operationId: 
           +listPriorities, x-permission: priority.read, responses: { "20
           +0": { description: OK, content: { application/json: { schema:
           + { type: array, items: { $ref: "#/components/schemas/Priority
           +" } } } } } } } }                                            
      1027      AdminCreate_Priority: { post: { tags: [admin], operationI
            d: createPriority, x-permission: admin.config, requestBody: {
             required: true, content: { application/json: { schema: { $re
            f: "#/components/schemas/PriorityCreate" } } } }, responses: 
            { "201": { description: Created, content: { application/json:
             { schema: { $ref: "#/components/schemas/Priority" } } } } } 
            } }
      1028      AdminGet_Priority: { get: { tags: [admin], operationId: g
            etPriority, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], responses: { "200": { de
            scription: OK, content: { application/json: { schema: { $ref:
             "#/components/schemas/Priority" } } } } } } }
      1029      AdminUpdate_Priority: { patch: { tags: [admin], operation
            Id: updatePriority, x-permission: admin.config, parameters: [
            { $ref: "#/components/parameters/PathId" }], requestBody: { r
            equired: true, content: { application/json: { schema: { $ref:
             "#/components/schemas/PriorityUpdate" } } } }, responses: { 
            "200": { description: OK, content: { application/json: { sche
            ma: { $ref: "#/components/schemas/Priority" } } } } } } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1025  
      1026      AdminList_Priority: { get: { tags: [admin], operationId: 
            listPriorities, x-permission: priority.read, responses: { "20
            0": { description: OK, content: { application/json: { schema:
             { type: array, items: { $ref: "#/components/schemas/Priority
            " } } } } } } } }
      1027      AdminCreate_Priority: { post: { tags: [admin], operationI
            d: createPriority, x-permission: admin.config, requestBody: {
             required: true, content: { application/json: { schema: { $re
            f: "#/components/schemas/PriorityCreate" } } } }, responses: 
            { "201": { description: Created, content: { application/json:
             { schema: { $ref: "#/components/schemas/Priority" } } } } } 
            } }
      1028 -    AdminGet_Priority: { get: { tags: [admin], operationId: g
           -etPriority, x-permission: admin.config, parameters: [{ $ref: 
           -"#/components/parameters/PathId" }], responses: { "200": { de
           -scription: OK, content: { application/json: { schema: { $ref:
           - "#/components/schemas/Priority" } } } } } } }               
      1028 +    AdminGet_Priority: { get: { tags: [admin], operationId: g
           +etPriority, x-permission: priority.read, parameters: [{ $ref:
           + "#/components/parameters/PathId" }], responses: { "200": { d
           +escription: OK, content: { application/json: { schema: { $ref
           +: "#/components/schemas/Priority" } } } } } } }              
      1029      AdminUpdate_Priority: { patch: { tags: [admin], operation
            Id: updatePriority, x-permission: admin.config, parameters: [
            { $ref: "#/components/parameters/PathId" }], requestBody: { r
            equired: true, content: { application/json: { schema: { $ref:
             "#/components/schemas/PriorityUpdate" } } } }, responses: { 
            "200": { description: OK, content: { application/json: { sche
            ma: { $ref: "#/components/schemas/Priority" } } } } } } }
      1030      AdminDelete_Priority: { delete: { tags: [admin], operatio
            nId: deletePriority, x-permission: admin.config, description:
             "No is_active column — hard delete, RESTRICT-guarded by tick
            ets/sla_policies referencing it.", parameters: [{ $ref: "#/co
            mponents/parameters/PathId" }], responses: { "204": { descrip
            tion: Deleted } } } }
      1031  

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1029      AdminUpdate_Priority: { patch: { tags: [admin], operation
            Id: updatePriority, x-permission: admin.config, parameters: [
            { $ref: "#/components/parameters/PathId" }], requestBody: { r
            equired: true, content: { application/json: { schema: { $ref:
             "#/components/schemas/PriorityUpdate" } } } }, responses: { 
            "200": { description: OK, content: { application/json: { sche
            ma: { $ref: "#/components/schemas/Priority" } } } } } } }
      1030      AdminDelete_Priority: { delete: { tags: [admin], operatio
            nId: deletePriority, x-permission: admin.config, description:
             "No is_active column — hard delete, RESTRICT-guarded by tick
            ets/sla_policies referencing it.", parameters: [{ $ref: "#/co
            mponents/parameters/PathId" }], responses: { "204": { descrip
            tion: Deleted } } } }
      1031  
      1032 -    AdminList_TicketStatus: { get: { tags: [admin], operation
           -Id: listTicketStatuses, x-permission: admin.config, responses
           -: { "200": { description: OK, content: { application/json: { 
           -schema: { type: array, items: { $ref: "#/components/schemas/T
           -icketStatus" } } } } } } } }                                 
      1032 +    AdminList_TicketStatus: { get: { tags: [admin], operation
           +Id: listTicketStatuses, x-permission: ticket_status.read, res
           +ponses: { "200": { description: OK, content: { application/js
           +on: { schema: { type: array, items: { $ref: "#/components/sch
           +emas/TicketStatus" } } } } } } } }                           
      1033      AdminCreate_TicketStatus: { post: { tags: [admin], operat
            ionId: createTicketStatus, x-permission: admin.config, reques
            tBody: { required: true, content: { application/json: { schem
            a: { $ref: "#/components/schemas/TicketStatusCreate" } } } },
             responses: { "201": { description: Created, content: { appli
            cation/json: { schema: { $ref: "#/components/schemas/TicketSt
            atus" } } } } } } }
      1034      AdminGet_TicketStatus: { get: { tags: [admin], operationI
            d: getTicketStatus, x-permission: admin.config, parameters: [
            { $ref: "#/components/parameters/PathId" }], responses: { "20
            0": { description: OK, content: { application/json: { schema:
             { $ref: "#/components/schemas/TicketStatus" } } } } } } }
      1035      AdminUpdate_TicketStatus: { patch: { tags: [admin], opera
            tionId: updateTicketStatus, x-permission: admin.config, param
            eters: [{ $ref: "#/components/parameters/PathId" }], requestB
            ody: { required: true, content: { application/json: { schema:
             { $ref: "#/components/schemas/TicketStatusUpdate" } } } }, r
            esponses: { "200": { description: OK, content: { application/
            json: { schema: { $ref: "#/components/schemas/TicketStatus" }
             } } } } } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1031  
      1032      AdminList_TicketStatus: { get: { tags: [admin], operation
            Id: listTicketStatuses, x-permission: ticket_status.read, res
            ponses: { "200": { description: OK, content: { application/js
            on: { schema: { type: array, items: { $ref: "#/components/sch
            emas/TicketStatus" } } } } } } } }
      1033      AdminCreate_TicketStatus: { post: { tags: [admin], operat
            ionId: createTicketStatus, x-permission: admin.config, reques
            tBody: { required: true, content: { application/json: { schem
            a: { $ref: "#/components/schemas/TicketStatusCreate" } } } },
             responses: { "201": { description: Created, content: { appli
            cation/json: { schema: { $ref: "#/components/schemas/TicketSt
            atus" } } } } } } }
      1034 -    AdminGet_TicketStatus: { get: { tags: [admin], operationI
           -d: getTicketStatus, x-permission: admin.config, parameters: [
           -{ $ref: "#/components/parameters/PathId" }], responses: { "20
           -0": { description: OK, content: { application/json: { schema:
           - { $ref: "#/components/schemas/TicketStatus" } } } } } } }   
      1034 +    AdminGet_TicketStatus: { get: { tags: [admin], operationI
           +d: getTicketStatus, x-permission: ticket_status.read, paramet
           +ers: [{ $ref: "#/components/parameters/PathId" }], responses:
           + { "200": { description: OK, content: { application/json: { s
           +chema: { $ref: "#/components/schemas/TicketStatus" } } } } } 
           +} }                                                          
      1035      AdminUpdate_TicketStatus: { patch: { tags: [admin], opera
            tionId: updateTicketStatus, x-permission: admin.config, param
            eters: [{ $ref: "#/components/parameters/PathId" }], requestB
            ody: { required: true, content: { application/json: { schema:
             { $ref: "#/components/schemas/TicketStatusUpdate" } } } }, r
            esponses: { "200": { description: OK, content: { application/
            json: { schema: { $ref: "#/components/schemas/TicketStatus" }
             } } } } } }
      1036      AdminDelete_TicketStatus: { delete: { tags: [admin], oper
            ationId: deleteTicketStatus, x-permission: admin.config, desc
            ription: "No is_active column — hard delete, RESTRICT-guarded
             by tickets/status_transitions referencing it.", parameters: 
            [{ $ref: "#/components/parameters/PathId" }], responses: { "2
            04": { description: Deleted } } } }
      1037  

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1035      AdminUpdate_TicketStatus: { patch: { tags: [admin], opera
            tionId: updateTicketStatus, x-permission: admin.config, param
            eters: [{ $ref: "#/components/parameters/PathId" }], requestB
            ody: { required: true, content: { application/json: { schema:
             { $ref: "#/components/schemas/TicketStatusUpdate" } } } }, r
            esponses: { "200": { description: OK, content: { application/
            json: { schema: { $ref: "#/components/schemas/TicketStatus" }
             } } } } } }
      1036      AdminDelete_TicketStatus: { delete: { tags: [admin], oper
            ationId: deleteTicketStatus, x-permission: admin.config, desc
            ription: "No is_active column — hard delete, RESTRICT-guarded
             by tickets/status_transitions referencing it.", parameters: 
            [{ $ref: "#/components/parameters/PathId" }], responses: { "2
            04": { description: Deleted } } } }
      1037  
      1038 -    AdminList_StatusTransition: { get: { tags: [admin], opera
           -tionId: listStatusTransitions, x-permission: admin.config, re
           -sponses: { "200": { description: OK, content: { application/j
           -son: { schema: { type: array, items: { $ref: "#/components/sc
           -hemas/StatusTransition" } } } } } } } }                      
      1038 +    AdminList_StatusTransition: { get: { tags: [admin], opera
           +tionId: listStatusTransitions, x-permission: status_transitio
           +n.read, responses: { "200": { description: OK, content: { app
           +lication/json: { schema: { type: array, items: { $ref: "#/com
           +ponents/schemas/StatusTransition" } } } } } } } }            
      1039      AdminCreate_StatusTransition: { post: { tags: [admin], op
            erationId: createStatusTransition, x-permission: admin.config
            , requestBody: { required: true, content: { application/json:
             { schema: { $ref: "#/components/schemas/StatusTransitionCrea
            te" } } } }, responses: { "201": { description: Created, cont
            ent: { application/json: { schema: { $ref: "#/components/sche
            mas/StatusTransition" } } } } } } }
      1040      AdminUpdate_StatusTransition: { patch: { tags: [admin], o
            perationId: updateStatusTransition, x-permission: admin.confi
            g, parameters: [{ $ref: "#/components/parameters/PathId" }], 
            requestBody: { required: true, content: { application/json: {
             schema: { $ref: "#/components/schemas/StatusTransitionUpdate
            " } } } }, responses: { "200": { description: OK, content: { 
            application/json: { schema: { $ref: "#/components/schemas/Sta
            tusTransition" } } } } } } }
      1041  

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1039      AdminCreate_StatusTransition: { post: { tags: [admin], op
            erationId: createStatusTransition, x-permission: admin.config
            , requestBody: { required: true, content: { application/json:
             { schema: { $ref: "#/components/schemas/StatusTransitionCrea
            te" } } } }, responses: { "201": { description: Created, cont
            ent: { application/json: { schema: { $ref: "#/components/sche
            mas/StatusTransition" } } } } } } }
      1040      AdminUpdate_StatusTransition: { patch: { tags: [admin], o
            perationId: updateStatusTransition, x-permission: admin.confi
            g, parameters: [{ $ref: "#/components/parameters/PathId" }], 
            requestBody: { required: true, content: { application/json: {
             schema: { $ref: "#/components/schemas/StatusTransitionUpdate
            " } } } }, responses: { "200": { description: OK, content: { 
            application/json: { schema: { $ref: "#/components/schemas/Sta
            tusTransition" } } } } } } }
      1041  
      1042 -    AdminList_SlaPolicy: { get: { tags: [admin], operationId:
           - listSlaPolicies, x-permission: admin.config, responses: { "2
           -00": { description: OK, content: { application/json: { schema
           -: { type: array, items: { $ref: "#/components/schemas/SlaPoli
           -cy" } } } } } } } }                                          
      1042 +    AdminList_SlaPolicy: { get: { tags: [admin], operationId:
           + listSlaPolicies, x-permission: sla_policy.read, responses: {
           + "200": { description: OK, content: { application/json: { sch
           +ema: { type: array, items: { $ref: "#/components/schemas/SlaP
           +olicy" } } } } } } } }                                       
      1043      AdminCreate_SlaPolicy: { post: { tags: [admin], operation
            Id: createSlaPolicy, x-permission: admin.config, requestBody:
             { required: true, content: { application/json: { schema: { $
            ref: "#/components/schemas/SlaPolicyCreate" } } } }, response
            s: { "201": { description: Created, content: { application/js
            on: { schema: { $ref: "#/components/schemas/SlaPolicy" } } } 
            } } } }
      1044      AdminGet_SlaPolicy: { get: { tags: [admin], operationId: 
            getSlaPolicy, x-permission: admin.config, parameters: [{ $ref
            : "#/components/parameters/PathId" }], responses: { "200": { 
            description: OK, content: { application/json: { schema: { $re
            f: "#/components/schemas/SlaPolicy" } } } } } } }
      1045      AdminUpdate_SlaPolicy: { patch: { tags: [admin], operatio
            nId: updateSlaPolicy, x-permission: admin.config, parameters:
             [{ $ref: "#/components/parameters/PathId" }], requestBody: {
             required: true, content: { application/json: { schema: { $re
            f: "#/components/schemas/SlaPolicyUpdate" } } } }, responses:
             { "200": { description: OK, content: { application/json: { s
            chema: { $ref: "#/components/schemas/SlaPolicy" } } } } } } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1041  
      1042      AdminList_SlaPolicy: { get: { tags: [admin], operationId:
             listSlaPolicies, x-permission: sla_policy.read, responses: {
             "200": { description: OK, content: { application/json: { sch
            ema: { type: array, items: { $ref: "#/components/schemas/SlaP
            olicy" } } } } } } } }
      1043      AdminCreate_SlaPolicy: { post: { tags: [admin], operation
            Id: createSlaPolicy, x-permission: admin.config, requestBody:
             { required: true, content: { application/json: { schema: { $
            ref: "#/components/schemas/SlaPolicyCreate" } } } }, response
            s: { "201": { description: Created, content: { application/js
            on: { schema: { $ref: "#/components/schemas/SlaPolicy" } } } 
            } } } }
      1044 -    AdminGet_SlaPolicy: { get: { tags: [admin], operationId: 
           -getSlaPolicy, x-permission: admin.config, parameters: [{ $ref
           -: "#/components/parameters/PathId" }], responses: { "200": { 
           -description: OK, content: { application/json: { schema: { $re
           -f: "#/components/schemas/SlaPolicy" } } } } } } }            
      1044 +    AdminGet_SlaPolicy: { get: { tags: [admin], operationId: 
           +getSlaPolicy, x-permission: sla_policy.read, parameters: [{ $
           +ref: "#/components/parameters/PathId" }], responses: { "200":
           + { description: OK, content: { application/json: { schema: { 
           +$ref: "#/components/schemas/SlaPolicy" } } } } } } }         
      1045      AdminUpdate_SlaPolicy: { patch: { tags: [admin], operatio
            nId: updateSlaPolicy, x-permission: admin.config, parameters:
             [{ $ref: "#/components/parameters/PathId" }], requestBody: {
             required: true, content: { application/json: { schema: { $re
            f: "#/components/schemas/SlaPolicyUpdate" } } } }, responses:
             { "200": { description: OK, content: { application/json: { s
            chema: { $ref: "#/components/schemas/SlaPolicy" } } } } } } }
      1046      AdminDelete_SlaPolicy: { delete: { tags: [admin], operati
            onId: deleteSlaPolicy, x-permission: admin.config, descriptio
            n: "No is_active column — hard delete, RESTRICT-guarded by ti
            ckets.sla_policy_id referencing it.", parameters: [{ $ref: "#
            /components/parameters/PathId" }], responses: { "204": { desc
            ription: Deleted } } } }
      1047  

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1045      AdminUpdate_SlaPolicy: { patch: { tags: [admin], operatio
            nId: updateSlaPolicy, x-permission: admin.config, parameters:
             [{ $ref: "#/components/parameters/PathId" }], requestBody: {
             required: true, content: { application/json: { schema: { $re
            f: "#/components/schemas/SlaPolicyUpdate" } } } }, responses:
             { "200": { description: OK, content: { application/json: { s
            chema: { $ref: "#/components/schemas/SlaPolicy" } } } } } } }
      1046      AdminDelete_SlaPolicy: { delete: { tags: [admin], operati
            onId: deleteSlaPolicy, x-permission: admin.config, descriptio
            n: "No is_active column — hard delete, RESTRICT-guarded by ti
            ckets.sla_policy_id referencing it.", parameters: [{ $ref: "#
            /components/parameters/PathId" }], responses: { "204": { desc
            ription: Deleted } } } }
      1047  
      1048 -    AdminList_QuickReply: { get: { tags: [admin], operationId
           -: listQuickReplies, x-permission: admin.config, responses: { 
           -"200": { description: OK, content: { application/json: { sche
           -ma: { type: array, items: { $ref: "#/components/schemas/Quick
           -Reply" } } } } } } } }                                       
      1048 +    AdminList_QuickReply: { get: { tags: [admin], operationId
           +: listQuickReplies, x-permission: quick_reply.read, responses
           +: { "200": { description: OK, content: { application/json: { 
           +schema: { type: array, items: { $ref: "#/components/schemas/Q
           +uickReply" } } } } } } } }                                   
      1049      AdminCreate_QuickReply: { post: { tags: [admin], operatio
            nId: createQuickReply, x-permission: admin.config, requestBod
            y: { required: true, content: { application/json: { schema: {
             $ref: "#/components/schemas/QuickReplyCreate" } } } }, respo
            nses: { "201": { description: Created, content: { application
            /json: { schema: { $ref: "#/components/schemas/QuickReply" } 
            } } } } } }
      1050      AdminGet_QuickReply: { get: { tags: [admin], operationId:
             getQuickReply, x-permission: admin.config, parameters: [{ $r
            ef: "#/components/parameters/PathId" }], responses: { "200": 
            { description: OK, content: { application/json: { schema: { $
            ref: "#/components/schemas/QuickReply" } } } } } } }
      1051      AdminUpdate_QuickReply: { patch: { tags: [admin], operati
            onId: updateQuickReply, x-permission: admin.config, parameter
            s: [{ $ref: "#/components/parameters/PathId" }], requestBody:
             { required: true, content: { application/json: { schema: { $
            ref: "#/components/schemas/QuickReplyUpdate" } } } }, respons
            es: { "200": { description: OK, content: { application/json: 
            { schema: { $ref: "#/components/schemas/QuickReply" } } } } }
             } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1047  
      1048      AdminList_QuickReply: { get: { tags: [admin], operationId
            : listQuickReplies, x-permission: quick_reply.read, responses
            : { "200": { description: OK, content: { application/json: { 
            schema: { type: array, items: { $ref: "#/components/schemas/Q
            uickReply" } } } } } } } }
      1049      AdminCreate_QuickReply: { post: { tags: [admin], operatio
            nId: createQuickReply, x-permission: admin.config, requestBod
            y: { required: true, content: { application/json: { schema: {
             $ref: "#/components/schemas/QuickReplyCreate" } } } }, respo
            nses: { "201": { description: Created, content: { application
            /json: { schema: { $ref: "#/components/schemas/QuickReply" } 
            } } } } } }
      1050 -    AdminGet_QuickReply: { get: { tags: [admin], operationId:
           - getQuickReply, x-permission: admin.config, parameters: [{ $r
           -ef: "#/components/parameters/PathId" }], responses: { "200": 
           -{ description: OK, content: { application/json: { schema: { $
           -ref: "#/components/schemas/QuickReply" } } } } } } }         
      1050 +    AdminGet_QuickReply: { get: { tags: [admin], operationId:
           + getQuickReply, x-permission: quick_reply.read, parameters: [
           +{ $ref: "#/components/parameters/PathId" }], responses: { "20
           +0": { description: OK, content: { application/json: { schema:
           + { $ref: "#/components/schemas/QuickReply" } } } } } } }     
      1051      AdminUpdate_QuickReply: { patch: { tags: [admin], operati
            onId: updateQuickReply, x-permission: admin.config, parameter
            s: [{ $ref: "#/components/parameters/PathId" }], requestBody:
             { required: true, content: { application/json: { schema: { $
            ref: "#/components/schemas/QuickReplyUpdate" } } } }, respons
            es: { "200": { description: OK, content: { application/json: 
            { schema: { $ref: "#/components/schemas/QuickReply" } } } } }
             } }
      1052      AdminDelete_QuickReply: { delete: { tags: [admin], operat
            ionId: deleteQuickReply, x-permission: admin.config, descript
            ion: "No is_active column — hard delete.", parameters: [{ $re
            f: "#/components/parameters/PathId" }], responses: { "204": {
             description: Deleted } } } }
      1053  

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1051      AdminUpdate_QuickReply: { patch: { tags: [admin], operati
            onId: updateQuickReply, x-permission: admin.config, parameter
            s: [{ $ref: "#/components/parameters/PathId" }], requestBody:
             { required: true, content: { application/json: { schema: { $
            ref: "#/components/schemas/QuickReplyUpdate" } } } }, respons
            es: { "200": { description: OK, content: { application/json: 
            { schema: { $ref: "#/components/schemas/QuickReply" } } } } }
             } }
      1052      AdminDelete_QuickReply: { delete: { tags: [admin], operat
            ionId: deleteQuickReply, x-permission: admin.config, descript
            ion: "No is_active column — hard delete.", parameters: [{ $re
            f: "#/components/parameters/PathId" }], responses: { "204": {
             description: Deleted } } } }
      1053  
      1054 -    AdminList_Team: { get: { tags: [admin], operationId: list
           -Teams, x-permission: admin.config, responses: { "200": { desc
           -ription: OK, content: { application/json: { schema: { type: a
           -rray, items: { $ref: "#/components/schemas/Team" } } } } } } 
           -} }                                                          
      1054 +    AdminList_Team: { get: { tags: [admin], operationId: list
           +Teams, x-permission: team.read, responses: { "200": { descrip
           +tion: OK, content: { application/json: { schema: { type: arra
           +y, items: { $ref: "#/components/schemas/Team" } } } } } } } }
      1055      AdminCreate_Team: { post: { tags: [admin], operationId: c
            reateTeam, x-permission: admin.config, requestBody: { require
            d: true, content: { application/json: { schema: { $ref: "#/co
            mponents/schemas/TeamCreate" } } } }, responses: { "201": { d
            escription: Created, content: { application/json: { schema: {
             $ref: "#/components/schemas/Team" } } } } } } }
      1056      AdminGet_Team: { get: { tags: [admin], operationId: getTe
            am, x-permission: admin.config, parameters: [{ $ref: "#/compo
            nents/parameters/PathId" }], responses: { "200": { descriptio
            n: OK, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/Team" } } } } } } }
      1057      AdminUpdate_Team: { patch: { tags: [admin], operationId: 
            updateTeam, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], requestBody: { required:
             true, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/TeamUpdate" } } } }, responses: { "200": { des
            cription: OK, content: { application/json: { schema: { $ref: 
            "#/components/schemas/Team" } } } } } } }

● Update(contracts\openapi.yaml)
  ⎿  Added 1 line, removed 1 line
      1053  
      1054      AdminList_Team: { get: { tags: [admin], operationId: list
            Teams, x-permission: team.read, responses: { "200": { descrip
            tion: OK, content: { application/json: { schema: { type: arra
            y, items: { $ref: "#/components/schemas/Team" } } } } } } } }
      1055      AdminCreate_Team: { post: { tags: [admin], operationId: c
            reateTeam, x-permission: admin.config, requestBody: { require
            d: true, content: { application/json: { schema: { $ref: "#/co
            mponents/schemas/TeamCreate" } } } }, responses: { "201": { d
            escription: Created, content: { application/json: { schema: {
             $ref: "#/components/schemas/Team" } } } } } } }
      1056 -    AdminGet_Team: { get: { tags: [admin], operationId: getTe
           -am, x-permission: admin.config, parameters: [{ $ref: "#/compo
           -nents/parameters/PathId" }], responses: { "200": { descriptio
           -n: OK, content: { application/json: { schema: { $ref: "#/comp
           -onents/schemas/Team" } } } } } } }                           
      1056 +    AdminGet_Team: { get: { tags: [admin], operationId: getTe
           +am, x-permission: team.read, parameters: [{ $ref: "#/componen
           +ts/parameters/PathId" }], responses: { "200": { description: 
           +OK, content: { application/json: { schema: { $ref: "#/compone
           +nts/schemas/Team" } } } } } } }                              
      1057      AdminUpdate_Team: { patch: { tags: [admin], operationId: 
            updateTeam, x-permission: admin.config, parameters: [{ $ref: 
            "#/components/parameters/PathId" }], requestBody: { required:
             true, content: { application/json: { schema: { $ref: "#/comp
            onents/schemas/TeamUpdate" } } } }, responses: { "200": { des
            cription: OK, content: { application/json: { schema: { $ref: 
            "#/components/schemas/Team" } } } } } } }
      1058      AdminDelete_Team: { delete: { tags: [admin], operationId:
             deleteTeam, x-permission: admin.config, description: "No is_
            active column — hard delete. team_members cascade with it; ti
            ckets.team_id is SET NULL, not blocked.", parameters: [{ $ref
            : "#/components/parameters/PathId" }], responses: { "204": { 
            description: Deleted } } } }
      1059  

  Ran 2 shell commands (ctrl+o to expand)

● Now updating plan.md's Generic CRUD Pattern section:

  Searched for 1 pattern, read 1 file (ctrl+o to expand)

● Now let's rewrite this section with the read/write permission split:

● Update(plan.md)
  ⎿  Added 24 lines, removed 10 lines
      427  ```python
      428  class AdminCrudService(Generic[ModelT, CreateSchemaT, UpdateSc
           hemaT]):
      429      repository_cls: ClassVar[type[ScopedRepository]]
      430 -    permission: ClassVar[str] = "admin.config"                
      430 +    read_permission: ClassVar[str]              # e.g. "catego
          +ry.read" — granted to agent, lead, admin                      
      431 +    write_permission: ClassVar[str] = "admin.config"   # admin
          +-only, unchanged across every subclass                        
      432      entity_type: ClassVar[str]                 # for the audit
           ed() decorator
      433  
      434      def __init__(self, session: AsyncSession) -> None: ...
      435  
      435 -    @require_permission_via(lambda self: self.permission)   # 
          -see note below                                                
      436 +    @require_permission_via(lambda self: self.read_permission)
          +   # see note below                                           
      437      async def list(self, actor: CurrentActor, filters: Mapping
           [str, Any] | None = None,
      438                      limit: int = 50, offset: int = 0) -> list[
           ModelT]: ...
      439 +    @require_permission_via(lambda self: self.read_permission)
      440      async def get(self, actor: CurrentActor, id: UUID) -> Mode
           lT: ...     # raises NotFoundError
      441 +    @require_permission_via(lambda self: self.write_permission
          +)                                                             
      442      async def create(self, actor: CurrentActor, data: CreateSc
           hemaT) -> ModelT: ...
      443 +    @require_permission_via(lambda self: self.write_permission
          +)                                                             
      444      async def update(self, actor: CurrentActor, id: UUID, data
           : UpdateSchemaT) -> ModelT: ...
      445 +    @require_permission_via(lambda self: self.write_permission
          +)                                                             
      446      async def remove(self, actor: CurrentActor, id: UUID) -> M
           odelT | None:
      447          """Calls repository.deactivate(id) if repository_cls.h
           as_soft_delete else
      448          repository.delete(id) — the branch is on the class att
           ribute, not re-decided per call."""
      449  ```
      450  
      446 -`create`, `update`, and `remove` are each individually wrapped
          - with `@require_permission(...)` and                          
      447 -`@audited(entity_type, action)` at definition time in `admin_c
          -rud_service.py` — `list`/`get` carry                          
      448 -only `require_permission` (reads are not audited, matching `da
          -ta-model.md`'s `audit_logs` schema,                           
      449 -which has no concept of a read event). `require_permission_via
          -` (a two-line helper alongside                                
      450 -`require_permission` in `app/core/permissions.py`) exists sole
          -ly so the permission code can be a                            
      451 -class attribute instead of a literal, since every subclass exc
          -ept `status_transitions`,                                     
      452 -`kb_articles` (`kb_article.create`/`.publish`), and the ticket
          --workflow permissions uses the same                           
      453 -`"admin.config"` string.                                      
      451 +**Read/write permissions are split, not shared** (fixed after 
          +`/speckit-analyze` finding D1: the                            
      452 +original single `permission` attribute gated `list`/`get` behi
          +nd `admin.config`, which only the                             
      453 +`admin` role holds — meaning an Agent could not read categorie
          +s, priorities, ticket statuses, SLA                           
      454 +policies, teams, or quick replies at all, breaking FR-016/FR-0
          +19/FR-029 for its primary actor).                             
      455 +`list`/`get` require `read_permission` (`{entity}.read`, seede
          +d to `agent`, `lead`, *and* `admin` —                         
      456 +`data-model.md` §5); `create`/`update`/`remove` require `write
          +_permission` (`admin.config`,                                 
      457 +`admin`-only, unchanged). Every one of the ten generic-CRUD su
          +bclasses sets `read_permission` to                            
      458 +its own `{entity}.read` code (`branch.read`, `department.read`
          +, `user.read`, `role.read`,                                   
      459 +`category.read`, `priority.read`, `ticket_status.read`, `statu
          +s_transition.read`,                                           
      460 +`sla_policy.read`, `quick_reply.read`, `team.read`); none over
          +rides `write_permission`.                                     
      461  
      462 +`create`, `update`, and `remove` are each additionally wrapped
          + with `@audited(entity_type,                                  
      463 +action)` at definition time in `admin_crud_service.py` — `list
          +`/`get` are not audited (reads have                           
      464 +no concept of a before/after change, matching `data-model.md`'
          +s `audit_logs` schema).                                       
      465 +`require_permission_via` (a two-line helper alongside `require
          +_permission` in                                               
      466 +`app/core/permissions.py`) exists so the permission code can b
          +e a class attribute instead of a                              
      467 +literal.                                                      
      468 +                                                              
      469  **`UserService(AdminCrudService[User, UserCreate, UserUpdate])
           `** overrides `create` only, to hash
      470  `password` into `password_hash` via `passlib`'s Argon2 hasher 
           before delegating to
      471  `super().create()` — everything else (list/get/update/remove, 
           audit, permission) is inherited

  Searched for 1 pattern (ctrl+o to expand)

● Good — all remaining admin.config mentions in plan.md are correct (write-only,
  or explaining the fix). Now let's update data-model.md §5's permission table:

  Searched for 1 pattern, read 1 file (ctrl+o to expand)

● Update(data-model.md)  ⎿  Added 2 lines, removed 1 line
      623  
      624  | Code | Purpose |
      625  |---|---|
      626 -| `admin.config` | All administrative CRUD named in PLAN.md F1
          -0 (branches, departments, users, roles, categories, priorities
          -, statuses, transitions, SLA policies, quick replies) |       
      626 +| `admin.config` | **Write-only** for the ten Generic CRUD Pat
          +tern entities (branches, departments, users, roles, categories
          +, priorities, ticket statuses, status transitions, SLA policie
          +s, quick replies) — `create`/`update`/`delete`/`deactivate`. G
          +ranted to `admin` only. |                                     
      627 +| `branch.read`, `department.read`, `user.read`, `role.read`, 
          +`category.read`, `priority.read`, `ticket_status.read`, `statu
          +s_transition.read`, `sla_policy.read`, `quick_reply.read`, `te
          +am.read` | **Read** for the ten Generic CRUD Pattern entities 
          +(`list`/`get`) — split from `admin.config` after `/speckit-ana
          +lyze` finding D1: an Agent building a ticket-creation form or 
          +reply composer needs to read categories, priorities, teams, an
          +d quick replies, but never held `admin.config`. Granted to `ag
          +ent`, `lead`, and `admin`. |                                  
      628  | `ticket.read`, `ticket.create` | Base CRUD, also the pair F1
           1's acceptance criterion exercises for scoped API keys |
      629  | `ticket.assign` | Assigning/reassigning a ticket (FR-019, F0
           4) |
      630  | `ticket.close` | Closing a ticket |

● Update(data-model.md)
  ⎿  Added 2 lines, removed 2 lines
      623  
      624  | Code | Purpose |
      625  |---|---|
      626 -| `admin.config` | **Write-only** for the ten Generic CRUD Pat
          -tern entities (branches, departments, users, roles, categories
          -, priorities, ticket statuses, status transitions, SLA policie
          -s, quick replies) — `create`/`update`/`delete`/`deactivate`. G
          -ranted to `admin` only. |                                     
      627 -| `branch.read`, `department.read`, `user.read`, `role.read`, 
          -`category.read`, `priority.read`, `ticket_status.read`, `statu
          -s_transition.read`, `sla_policy.read`, `quick_reply.read`, `te
          -am.read` | **Read** for the ten Generic CRUD Pattern entities 
          -(`list`/`get`) — split from `admin.config` after `/speckit-ana
          -lyze` finding D1: an Agent building a ticket-creation form or 
          -reply composer needs to read categories, priorities, teams, an
          -d quick replies, but never held `admin.config`. Granted to `ag
          -ent`, `lead`, and `admin`. |                                  
      626 +| `admin.config` | **Write-only** for the ten Generic CRUD Pat
          +tern entities (branches, departments, users, roles, categories
          +, priorities, ticket statuses, SLA policies, quick replies, te
          +ams) plus `status_transitions` (list/create/update/delete subs
          +et) — `create`/`update`/`delete`/`deactivate` only. Granted to
          + `admin` only. |                                              
      627 +| `branch.read`, `department.read`, `user.read`, `role.read`, 
          +`category.read`, `priority.read`, `ticket_status.read`, `statu
          +s_transition.read`, `sla_policy.read`, `quick_reply.read`, `te
          +am.read` | **Read** for the same eleven entities (`list`/`get`
          +) — split from `admin.config` after `/speckit-analyze` finding
          + D1: an Agent building a ticket-creation form or reply compose
          +r needs to read categories, priorities, teams, and quick repli
          +es, but never held `admin.config`. Granted to `agent`, `lead`,
          + and `admin`. |                                               
      628  | `ticket.read`, `ticket.create` | Base CRUD, also the pair F1
           1's acceptance criterion exercises for scoped API keys |
      629  | `ticket.assign` | Assigning/reassigning a ticket (FR-019, F0
           4) |
      630  | `ticket.close` | Closing a ticket |

● Update(data-model.md)
  ⎿  Added 2 lines, removed 3 lines
      637  | `audit.read` | **Seeded now, granted to `admin` per PLAN.md 
           §7** — Tier D accommodation for `specs/006-audit-log-ui`; noth
           ing checks it yet |
      638  
      639  Full CRUD-permission naming convention (`{entity}.read`/`.crea
           te`/`.update`/`.deactivate`) for
      640 -every other S1/S2/S3 entity not named above (e.g. `kb_article.
          -read`, `kb_article.publish`,                                  
      641 -`quick_reply.read`) is defined once in `plan.md`'s "Generic CR
          -UD Pattern" section rather than                               
      642 -enumerated per entity here.                                   
      640 +every other S1/S2/S3 entity not named above (e.g. `kb_article.
          +read`, `kb_article.publish`) is                               
      641 +defined once in `plan.md`'s "Generic CRUD Pattern" section rat
          +her than enumerated per entity here.                          

  Searched for 1 pattern (ctrl+o to expand)

● Update(tasks.md)
  ⎿  Added 2 lines, removed 2 lines
      108  
      109  - [ ] T046 Create `backend/app/core/security.py` — JWT issue/v
           erify (access 15 min / refresh 7 day), Argon2 password hashing
            via passlib (depends on T004)
      110  - [ ] T047 Complete `backend/app/api/deps.py` — `get_current_a
           ctor()` decodes a bearer JWT into a `CurrentActor` (permission
           s = union of `role_permissions` reachable via every matching `
           user_roles` row for the resolved branch/department) (depends o
           n T046, T032)
      111 -- [ ] T048 Create `backend/app/services/admin_crud_service.py`
          - — generic `AdminCrudService[ModelT, CreateSchemaT, UpdateSche
          -maT]` exactly per `plan.md` §Generic CRUD Pattern (`list`/`get
          -`/`create`/`update`/`remove`, each `create`/`update`/`remove` 
          -wrapped with `require_permission` + `audited`) (depends on T03
          -1, T032, T033)                                                
      112 -- [ ] T049 [P] In `backend/app/services/admin_crud_service.py`
          -, add `BranchCrudService`, `DepartmentCrudService`, `RoleCrudS
          -ervice` thin subclasses (`repository_cls`, `entity_type`, `per
          -mission="admin.config"`) (depends on T048)                    
      111 +- [ ] T048 Create `backend/app/services/admin_crud_service.py`
          + — generic `AdminCrudService[ModelT, CreateSchemaT, UpdateSche
          +maT]` exactly per `plan.md` §Generic CRUD Pattern (`list`/`get
          +` wrapped with `require_permission(read_permission)`; `create`
          +/`update`/`remove` wrapped with `require_permission(write_perm
          +ission)` + `audited`) (depends on T031, T032, T033)           
      112 +- [ ] T049 [P] In `backend/app/services/admin_crud_service.py`
          +, add `BranchCrudService` (`read_permission="branch.read"`), `
          +DepartmentCrudService` (`read_permission="department.read"`), 
          +`RoleCrudService` (`read_permission="role.read"`) thin subclas
          +ses — each sets `repository_cls`, `entity_type`, `read_permiss
          +ion`; `write_permission` inherits `"admin.config"` unchanged (
          +depends on T048)                                              
      113  - [ ] T050 Create `backend/app/services/user_service.py` — `Us
           erService(AdminCrudService[User, UserCreate, UserUpdate])` ove
           rriding `create()` to Argon2-hash `password`→`password_hash` b
           efore delegating to `super().create()`; plus `grant_role(actor
           , user_id, role_id, branch_id, department_id) -> UserRole` (de
           pends on T048, T046)
      114  - [ ] T051 Create `backend/app/services/auth_service.py` — `lo
           gin`, `refresh`, `logout` exactly per `plan.md` §Service Class
           es (depends on T046, T050)
      115  - [ ] T052 [P] Create `backend/app/schemas/auth.py` — `LoginRe
           quest`, `RefreshRequest`, `TokenPair` per `contracts/openapi.y
           aml` (depends on T005)

  Read 1 file (ctrl+o to expand)

● Update(tasks.md)
  ⎿  Added 1 line, removed 1 line
      110  - [ ] T047 Complete `backend/app/api/deps.py` — `get_current_a
           ctor()` decodes a bearer JWT into a `CurrentActor` (permission
           s = union of `role_permissions` reachable via every matching `
           user_roles` row for the resolved branch/department) (depends o
           n T046, T032)
      111  - [ ] T048 Create `backend/app/services/admin_crud_service.py`
            — generic `AdminCrudService[ModelT, CreateSchemaT, UpdateSche
           maT]` exactly per `plan.md` §Generic CRUD Pattern (`list`/`get
           ` wrapped with `require_permission(read_permission)`; `create`
           /`update`/`remove` wrapped with `require_permission(write_perm
           ission)` + `audited`) (depends on T031, T032, T033)
      112  - [ ] T049 [P] In `backend/app/services/admin_crud_service.py`
           , add `BranchCrudService` (`read_permission="branch.read"`), `
           DepartmentCrudService` (`read_permission="department.read"`), 
           `RoleCrudService` (`read_permission="role.read"`) thin subclas
           ses — each sets `repository_cls`, `entity_type`, `read_permiss
           ion`; `write_permission` inherits `"admin.config"` unchanged (
           depends on T048)
      113 -- [ ] T050 Create `backend/app/services/user_service.py` — `Us
          -erService(AdminCrudService[User, UserCreate, UserUpdate])` ove
          -rriding `create()` to Argon2-hash `password`→`password_hash` b
          -efore delegating to `super().create()`; plus `grant_role(actor
          -, user_id, role_id, branch_id, department_id) -> UserRole` (de
          -pends on T048, T046)                                          
      113 +- [ ] T050 Create `backend/app/services/user_service.py` — `Us
          +erService(AdminCrudService[User, UserCreate, UserUpdate])`, `r
          +ead_permission="user.read"`, overriding `create()` to Argon2-h
          +ash `password`→`password_hash` before delegating to `super().c
          +reate()`; plus `grant_role(actor, user_id, role_id, branch_id,
          + department_id) -> UserRole` (depends on T048, T046)          
      114  - [ ] T051 Create `backend/app/services/auth_service.py` — `lo
           gin`, `refresh`, `logout` exactly per `plan.md` §Service Class
           es (depends on T046, T050)
      115  - [ ] T052 [P] Create `backend/app/schemas/auth.py` — `LoginRe
           quest`, `RefreshRequest`, `TokenPair` per `contracts/openapi.y
           aml` (depends on T005)
      116  - [ ] T053 [P] Create `backend/app/schemas/user.py` — `User`, 
           `UserCreate`, `UserUpdate`, `UserRole`, `UserRoleCreate` per `
           contracts/openapi.yaml` (depends on T005)

● Now T068 (Category/Priority/TicketStatus) and the StatusTransition service
  task nearby:

  Read 1 file (ctrl+o to expand)

● Update(tasks.md)
  ⎿  Added 7 lines, removed 6 lines
      151  **Contents (PLAN.md §6)**: F02 — categories, priorities, statu
           ses, transitions, tickets,
      152  timeline, assignment.
      153  
      154 -- [ ] T068 [P] In `backend/app/services/admin_crud_service.py`
          -, add `CategoryCrudService`,                                  
      155 -      `PriorityCrudService`, `TicketStatusCrudService` thin su
          -bclasses (`has_soft_delete=True`                              
      156 -      only for `CategoryCrudService`, matching `data-model.md`
          - §0.4's per-table matrix) (depends                            
      154 +- [ ] T068 [P] In `backend/app/services/admin_crud_service.py`
          +, add `CategoryCrudService`                                   
      155 +      (`read_permission="category.read"`, `has_soft_delete=Tru
          +e`, matching `data-model.md` §0.4's                           
      156 +      per-table matrix), `PriorityCrudService` (`read_permissi
          +on="priority.read"`),                                         
      157 +      `TicketStatusCrudService` (`read_permission="ticket_stat
          +us.read"`) thin subclasses (depends                           
      158        on T048)
      159  - [ ] T069 Create `backend/app/services/status_transition_serv
           ice.py` (admin side) —
      159 -      `list`/`create`/`update`/`delete` only (no single-`get`,
          - matching `contracts/openapi.yaml`)                           
      160 -      over `StatusTransition`, `delete` is a hard `DELETE` (no
          - `is_active` column) (depends on                              
      161 -      T048)                                                   
      160 +      `list` (`require_permission("status_transition.read")`) 
          +plus `create`/`update`/`delete`                               
      161 +      (no single-`get`, matching `contracts/openapi.yaml`) ove
          +r `StatusTransition`, each requiring                          
      162 +      `"admin.config"`; `delete` is a hard `DELETE` (no `is_ac
          +tive` column) (depends on T048)                               
      163  - [ ] T070 [P] Create `backend/app/schemas/ticket_taxonomy.py`
            — `Category`/`CategoryCreate`/
      164        `CategoryUpdate`, `Priority`/`PriorityCreate`/`PriorityU
           pdate`, `TicketStatus`/
      165        `TicketStatusCreate`/`TicketStatusUpdate`, `StatusTransi
           tion`/`StatusTransitionCreate`/

● Now T080 (QuickReply) and T088 (SlaPolicy):

  Read 1 file (ctrl+o to expand)

● Update(tasks.md)
  ⎿  Added 2 lines, removed 2 lines
      220  shell.
      221  
      222  - [ ] T080 [P] In `backend/app/services/admin_crud_service.py`
           , add `QuickReplyCrudService`
      223 -      thin subclass (`has_soft_delete=False` — hard delete, `d
          -ata-model.md` §0.4) (depends on                               
      224 -      T048)                                                   
      223 +      thin subclass (`read_permission="quick_reply.read"`, `ha
          +s_soft_delete=False` — hard                                   
      224 +      delete, `data-model.md` §0.4) (depends on T048)         
      225  - [ ] T081 [P] Create `backend/app/schemas/quick_reply.py` — `
           QuickReply`, `QuickReplyCreate`,
      226        `QuickReplyUpdate` per `contracts/openapi.yaml` (depends
            on T005)
      227  - [ ] T082 Add placeholder-substitution logic (`{{customer_nam
           e}}`, `{{reference_no}}`,

  Read 1 file (ctrl+o to expand)

● Update(tasks.md)
  ⎿  Added 1 line, removed 1 line
      261  round-robin.
      262  
      263  - [ ] T088 [P] In `backend/app/services/admin_crud_service.py`
           , add `SlaPolicyCrudService` thin
      264 -      subclass (`has_soft_delete=False`) (depends on T048)    
      264 +      subclass (`read_permission="sla_policy.read"`, `has_soft
          +_delete=False`) (depends on T048)                             
      265  - [ ] T089 [P] Create `backend/app/schemas/sla_policy.py` — `S
           laPolicy`, `SlaPolicyCreate`,
      266        `SlaPolicyUpdate` per `contracts/openapi.yaml` (depends 
           on T005)
      267  - [ ] T090 Create `backend/app/services/sla_service.py` — `com
           pute_due_dates` (pure function:

● Now updating T136's seed description for the new read-permission grants:

  Searched for 1 pattern, read 1 file (ctrl+o to expand)

● Update(tasks.md)
  ⎿  Added 5 lines, removed 1 line
      461        statuses with the full `status_transitions` table from `
           data-model.md` §4, 3 SLA policies,
      462        8 quick replies, 2 `channel_configs` rows (one per depar
           tment), and the full seeded
      463        permission set from `data-model.md` §5 — **including `au
           dit.read` and `report.cross_branch`
      464 -      granted to the seeded admin**, per PLAN.md §7's explicit
          - instruction. This task's seed                                
      464 +      granted to the seeded admin**, per PLAN.md §7's explicit
          + instruction, **and the eleven                                
      465 +      `{entity}.read` permissions (`branch.read`, `department.
          +read`, `user.read`, `role.read`,                              
      466 +      `category.read`, `priority.read`, `ticket_status.read`, 
          +`status_transition.read`,                                     
      467 +      `sla_policy.read`, `quick_reply.read`, `team.read`) gran
          +ted to every seeded `agent` and                               
      468 +      `lead` user, not just `admin`** (`/speckit-analyze` find
          +ing D1). This task's seed                                     
      469        script writes rows through every model created in Batch 
           4a and touches functionality from
      470        every batch since (customers, tickets, KB articles, SLA 
           policies, channel configs), so it
      471        depends on T030 (full schema) and, transitively, on ever
           y batch's models being complete —

● Now F1 — fixing plan.md's AssignmentService bullet:

  Searched for 1 pattern (ctrl+o to expand)

● Update(plan.md)
  ⎿  Added 1 line, removed 1 line
      516  - `async def change_status(self, actor: CurrentActor, ticket_i
           d: UUID, to_status_id: UUID, reason: str | None) -> Ticket` — 
           the ONLY method in the codebase that queries `status_transitio
           ns`; raises `IllegalTransitionError(current_status_id, permitt
           ed_status_ids)` (→ HTTP 422) when no `(from_status_id, to_stat
           us_id)` row matches the ticket's branch/department (falling ba
           ck to the `department_id IS NULL` default row); raises `Permis
           sionDeniedError` if the matched row's `required_permission` is
            set and absent from `actor.permissions`; raises a validation 
           error if `requires_reason` is true and `reason` is `None`
      517  
      518  **`AssignmentService`**
      519 -- `async def auto_assign_ticket(self, ticket_id: UUID) -> Tick
          -et | None` — round-robins over active `ticket.own`-holding age
          -nts in the ticket's department who are not flagged unavailable
          -; returns `None` (ticket stays unassigned) if none are eligibl
          -e. Called by `categorization_job` after categorization complet
          -es, and directly by `TicketService.create` when the ticket has
          - no `assignee_id`/`team_id` set at creation.                  
      519 +- `async def auto_assign_ticket(self, ticket_id: UUID) -> Tick
          +et | None` — round-robins over active `ticket.own`-holding age
          +nts in the ticket's department who are not flagged unavailable
          +; returns `None` (ticket stays unassigned) if none are eligibl
          +e. Called only by `categorization_job`, after categorization c
          +ompletes — never directly by `TicketService.create` (fixed aft
          +er `/speckit-analyze` finding F1: `TicketCreate` never accepts
          + `assignee_id` in the first place, and a second, synchronous c
          +all site here would risk double-assigning a ticket against the
          + async job's own call).                                       
      520  
      521  **`SlaService`**
      522  - `def compute_due_dates(self, ticket: Ticket, policy: SlaPoli
           cy, branch: Branch) -> SlaDueDates` — pure function; applies `
           sla_paused_ms` and, if `policy.business_hours_only`, `branch.b
           usiness_hours`/`branch.timezone`

● Now C1 — adding FR-016 validation to plan.md's TicketService.create and to
  tasks.md T073:

  Searched for 1 pattern (ctrl+o to expand)
● Update(plan.md)
  ⎿  Added 1 line, removed 1 line
      503  **`TicketService`**
      504  - `async def list(self, actor: CurrentActor, view: TicketView 
           | None, filters: TicketFilters, limit: int, offset: int) -> li
           st[TicketSummary]` — `view` maps to one of F04's five queue de
           finitions via `TicketRepository`
      505  - `async def get(self, actor: CurrentActor, id: UUID) -> Ticke
           t` — includes computed `sla_first_response_due_at`/`sla_resolu
           tion_due_at`/`sla_breach_state` via `SlaService` (never stored
           )
      506 -- `async def create(self, actor: CurrentActor, data: TicketCre
          -ate) -> Ticket` — generates `reference_no` from a DB sequence 
          -(`TKT-{year}-{6-digit}`), resolves `sla_policy_id` via `SlaSer
          -vice.resolve_policy`, writes the `created` event, enqueues `ca
          -tegorization_job` (never awaits it — FR-049)                  
      506 +- `async def create(self, actor: CurrentActor, data: TicketCre
          +ate) -> Ticket` — validates `data.category_id` and `data.prior
          +ity_id` are each currently active, and, if the resolved catego
          +ry/priority is department-scoped (its own `department_id` is n
          +ot `NULL`), that it matches the ticket's `department_id`; reje
          +cts with a localized validation error (422) if either check fa
          +ils (FR-016) — generates `reference_no` from a DB sequence (`T
          +KT-{year}-{6-digit}`), resolves `sla_policy_id` via `SlaServic
          +e.resolve_policy`, writes the `created` event, enqueues `categ
          +orization_job` (never awaits it — FR-049)                     
      507  - `async def update(self, actor: CurrentActor, id: UUID, data:
            TicketUpdate) -> Ticket`
      508  - `async def assign(self, actor: CurrentActor, id: UUID, assig
           nee_id: UUID | None, team_id: UUID | None) -> Ticket`
      509  - `async def add_note(self, actor: CurrentActor, id: UUID, bod
           y: str) -> TicketEvent`

● Now tasks.md T073:

  Searched for 1 pattern, read 1 file (ctrl+o to expand)

● Update(tasks.md)
  ⎿  Added 3 lines, removed 1 line
      174        `breaching_soon` ordered by time-remaining ascending, `r
           ecently_closed`) and the shared
      175        filter set (status/priority/category/assignee/channel/da
           te-range/free-text) — used by both
      176        this batch's `TicketService.list` and Batch 4e's dashboa
           rd (depends on T031, T018)
      177 -- [ ] T073 Create `backend/app/services/ticket_service.py` — `
          -list`, `get`, `create` (DB-sequence                           
      177 +- [ ] T073 Create `backend/app/services/ticket_service.py` — `
          +list`, `get`, `create` (validates                             
      178 +      `category_id`/`priority_id` are each currently active an
          +d, if department-scoped, match the                            
      179 +      ticket's `department_id` — rejects with a localized 422 
          +otherwise, FR-016; DB-sequence                                
      180        `reference_no` generation `TKT-{year}-{6-digit}`, resolv
           es `sla_policy_id` via a stub
      181        `SlaService.resolve_policy` completed in Batch 4f, write
           s the `created` event, enqueues
      182        `categorization_job` without awaiting it), `update`, `as
           sign`, `add_note`, `add_reply`

● Now the two cheap additions. First, batch 4a's grep-based i18n check task,
  inserted as T045a (avoiding renumbering the other 92 tasks and their
  cross-references, same approach used for the spec's FR letter-suffixes):

  Searched for 1 pattern, read 1 file (ctrl+o to expand)

● Update(tasks.md)
  ⎿  Added 8 lines
       91  - [ ] T043 Create `frontend/app/[locale]/layout.tsx` — sets `<
           html dir>` from the active locale via next-intl; the only plac
           e `dir` is set anywhere in the frontend (depends on T041, T042
           )
       92  - [ ] T044 [P] Create `frontend/components/ltr-text.tsx` — `<L
           trText>`, sets `dir="ltr"` locally (constitution Principle III
           ) (depends on T041)
       93  - [ ] T045 Create a placeholder page at `frontend/app/[locale]
           /(agent)/dashboard/page.tsx` containing only a locale switcher
            and one `<LtrText>`-wrapped sample value, to exercise the RTL
            toggle for this batch's gate (depends on T043, T044)
       94 +- [ ] T045a [P] Create `frontend/scripts/check-i18n-literals.s
          +h` — a grep-based check (not an                               
       95 +      ESLint rule) scanning `frontend/app/` and `frontend/comp
          +onents/` for JSX string literals                              
       96 +      (text between tags, and string-valued `label`/`title`/`p
          +laceholder`/`aria-label` props) not                           
       97 +      routed through a `next-intl` translation call (`useTrans
          +lations`/`t(...)`), exiting non-zero                          
       98 +      on any hit; wire it as an npm script (`"check:i18n"`) in
          + `frontend/package.json` (constitution                        
       99 +      Principle I / PLAN.md C1's stated enforcement point — ad
          +ded after `/speckit-analyze` finding                          
      100 +      E1, since PLAN.md C8's equivalent import-lint (T038) alr
          +eady existed but C1's did not)                                
      101 +      (depends on T003, T045)                                 
      102  
      103  **Gate (PLAN.md §6)**: `docker compose up` reaches a healthy s
           tate for every service with zero
      104  restarts; `docker compose exec backend alembic upgrade head` a
           pplies cleanly with no errors;

● Now the quickstart.md addition:

  Read 1 file (ctrl+o to expand)

● Update(quickstart.md)
  ⎿  Added 4 lines
      70  6. Reopen the closed ticket as the `agent` (who does not hold `
          ticket.reopen` by default seed
      71     data) — expect 403 — then as the `lead` (who does) — expect 
          success, with `reopened_count`
      72     incremented (FR-018).
      73 +7. Open the frontend dashboard (`frontend/app/[locale]/(agent)/
         +dashboard/page.tsx`) and the                                   
      74 +   ticket-detail page for the ticket created above, in a browse
         +r resized to a 375px viewport                                  
      75 +   width. **Expected**: both pages remain fully usable — no cli
         +pped controls, no horizontal                                   
      76 +   scroll, no overlapping text — in both English and Arabic (FR
         +-003, SC-007).                                                 
      77  
      78  ## 5. Validate tenant scoping (spec.md Story 12, Platform-Wide 
          Requirements)
      79  

● Let me do a final validation pass across all touched files.

  Ran 1 shell command (ctrl+o to expand)

● All 11 {entity}.read codes are consistent across plan.md, data-model.md, and
  tasks.md. YAML still parses cleanly.

  Remediation applied

  D1 (permission split) — done across all four named artifacts:
  - plan.md: AdminCrudService now has read_permission/write_permission, each of
    the ten thin subclasses documented with its {entity}.read code.
  - contracts/openapi.yaml: all 21 AdminList_*/AdminGet_* operations changed
    from admin.config to their entity's .read permission; write operations
    (create/update/delete/deactivate) untouched.
  - data-model.md §5: split into a write-only admin.config row and a new
    read-permissions row, granted to agent, lead, and admin.
  - tasks.md: T049, T050, T068, T069, T080, T088 (subclass-creation tasks) and
    T136 (seed grants) updated to set/grant the new codes.

  F1 — plan.md's AssignmentService.auto_assign_ticket bullet now states the
  single, correct call site (only categorization_job), with the double-call risk
  noted.

  C1 — FR-016's category/priority active+department validation is now explicit
  in both plan.md's TicketService.create bullet and tasks.md T073.

  Two additions:
  - tasks.md T045a (batch 4a) — a grep-based
    frontend/scripts/check-i18n-literals.sh script wired as an npm script, not
    an ESLint rule, catching un-externalized JSX strings.
  - quickstart.md Step 4.7 — manual 375px-viewport check for the dashboard and
    ticket-detail pages, in both languages.

  One thing noticed but left untouched (out of scope for this remediation, not
  part of D1/F1/C1): there's no task anywhere in tasks.md that actually creates
  a TeamCrudService thin subclass or wires /teams routes —
  team.read/admin.config are now correctly defined for it in the contract and
  data model, but nothing builds it. Let me know if you want that added.

✻ Baked for 10m 7s

❯ Yes, add it. Create the TeamCrudService thin subclass and wire the /teams     
  routes as tasks in                                                            
  the same batch as the other admin-CRUD subclasses, following the identical    
  Generic CRUD                                                                  
  Pattern with team.read / admin.config. Also check whether team_members has    
  any task creating                                                             
  its membership endpoints — a team with no way to add members is the same gap  
  one level down.                                                               
  Name exact file paths as with every other task.                               

  Searched for 4 patterns, read 2 files (ctrl+o to expand)

● Now let's implement. First, plan.md — documenting TeamCrudService + add_member
  right after the UserService precedent:

  Read 1 file (ctrl+o to expand)

● Update(plan.md)
  ⎿  Added 7 lines
      471  `super().create()` — everything else (list/get/update/remove, 
           audit, permission) is inherited
      472  unchanged.
      473  
      474 +**`TeamCrudService(AdminCrudService[Team, TeamCreate, TeamUpda
          +te])`** (`read_permission=                                    
      475 +"team.read"`, `has_soft_delete=False` — hard delete, `data-mod
          +el.md` §0.4) adds exactly one                                 
      476 +bespoke method beyond the inherited CRUD: `async def add_membe
          +r(self, actor: CurrentActor,                                  
      477 +team_id: UUID, user_id: UUID) -> TeamMember`, wrapped with `@r
          +equire_permission("admin.config")`                            
      478 +and `@audited("team_member", "create")` — a team with no way t
          +o add a member is otherwise a dead                            
      479 +end, the same shape of gap D1 fixed one level up (`/speckit-an
          +alyze` finding, follow-up).                                   
      480 +                                                              
      481  Every other S1/S2/S3 entity not covered by the ten above (`kb_
           articles`, `quick_replies` is
      482  already listed) gets its CRUD permission named `{entity}.read`
           /`{entity}.create` following the
      483  same `ticket.read`/`ticket.create` convention documented in `d
           ata-model.md` §5 — `KbService`

● Now tasks.md — inserting T069a/T069b (schema + service) after T069:

● Update(tasks.md)
  ⎿  Added 9 lines
      168        `list` (`require_permission("status_transition.read")`) 
           plus `create`/`update`/`delete`
      169        (no single-`get`, matching `contracts/openapi.yaml`) ove
           r `StatusTransition`, each requiring
      170        `"admin.config"`; `delete` is a hard `DELETE` (no `is_ac
           tive` column) (depends on T048)
      171 +- [ ] T069a [P] Create `backend/app/schemas/team.py` — `Team`,
          + `TeamCreate`, `TeamUpdate`,                                  
      172 +      `TeamMemberCreate` per `contracts/openapi.yaml` (depends
          + on T005)                                                     
      173 +- [ ] T069b In `backend/app/services/admin_crud_service.py`, a
          +dd `TeamCrudService`                                          
      174 +      (`read_permission="team.read"`, `has_soft_delete=False` 
          +— hard delete, matching                                       
      175 +      `data-model.md` §0.4) thin subclass, plus its one bespok
          +e method:                                                     
      176 +      `add_member(actor, team_id, user_id) -> TeamMember`     
      177 +      (`require_permission("admin.config")` + `audited("team_m
          +ember", "create")`) — added after                             
      178 +      `/speckit-analyze` finding D1's follow-up: no task previ
          +ously created this service or its                             
      179 +      `/teams` routes at all (depends on T048, T012)          
      180  - [ ] T070 [P] Create `backend/app/schemas/ticket_taxonomy.py`
            — `Category`/`CategoryCreate`/
      181        `CategoryUpdate`, `Priority`/`PriorityCreate`/`PriorityU
           pdate`, `TicketStatus`/
      182        `TicketStatusCreate`/`TicketStatusUpdate`, `StatusTransi
           tion`/`StatusTransitionCreate`/
● Now inserting T076a after T076, and updating T078's dependency list:

● Update(tasks.md)
  ⎿  Added 5 lines, removed 1 line
      215  - [ ] T076 Create `backend/app/api/routers/admin_config.py` ad
           ditions — `/categories`,
      216        `/priorities`, `/ticket-statuses`, `/status-transitions`
            routes (extends the file from
      217        Batch 4b) (depends on T068, T069, T070)
      218 +- [ ] T076a [P] Create `backend/app/api/routers/admin_config.p
          +y` additions — `/teams`                                       
      219 +      (list/create/get/update/delete) and `/teams/{id}/members
          +` (add member) routes, calling                                
      220 +      `TeamCrudService`'s inherited CRUD methods and its bespo
          +ke `add_member` — validate/delegate/                          
      221 +      serialize only (depends on T069a, T069b)                
      222  - [ ] T077 Create `backend/app/api/routers/tickets.py` — every
            `/tickets*` path in
      223        `contracts/openapi.yaml` except `/tickets/{id}/sla-overr
           ide` (Batch 4f) and the
      224        `/tickets/{id}/ai/*` paths (Batch 4h) — validate/delegat
           e/serialize only (depends on T073,
      225        T074, T071)
      226  - [ ] T078 Wire the Batch 4d router additions into `backend/ap
           p/api/router.py` (depends on T076,
      223 -      T077)                                                   
      227 +      T076a, T077)                                            
      228  - [ ] T079 [P] Create `frontend/app/[locale]/(agent)/tickets/[
           id]/page.tsx` — ticket detail,
      229        status-change control (surfacing the localized illegal-t
           ransition error), assignment,
      230        timeline, notes/replies, attachments, inline customer co
           ntext (FR-028) (depends on T043,

  Read 1 file (ctrl+o to expand)

● Update(tasks.md)
  ⎿  Added 5 lines, removed 3 lines
      170        `"admin.config"`; `delete` is a hard `DELETE` (no `is_ac
           tive` column) (depends on T048)
      171  - [ ] T069a [P] Create `backend/app/schemas/team.py` — `Team`,
            `TeamCreate`, `TeamUpdate`,
      172        `TeamMemberCreate` per `contracts/openapi.yaml` (depends
            on T005)
      173 -- [ ] T069b In `backend/app/services/admin_crud_service.py`, a
          -dd `TeamCrudService`                                          
      174 -      (`read_permission="team.read"`, `has_soft_delete=False` 
          -— hard delete, matching                                       
      175 -      `data-model.md` §0.4) thin subclass, plus its one bespok
          -e method:                                                     
      173 +- [ ] T069b In `backend/app/services/admin_crud_service.py`, a
          +dd `TeamCrudService` thin subclass                            
      174 +      — sets `repository_cls` (`ScopedRepository[Team]`, `scop
          +ing_mode=S1_FULL`), `entity_type`,                            
      175 +      `read_permission="team.read"`, `has_soft_delete=False` (
          +hard delete, matching                                         
      176 +      `data-model.md` §0.4; `write_permission` inherits `"admi
          +n.config"` unchanged) — plus its one                          
      177 +      bespoke method:                                         
      178        `add_member(actor, team_id, user_id) -> TeamMember`
      179        (`require_permission("admin.config")` + `audited("team_m
           ember", "create")`) — added after
      180        `/speckit-analyze` finding D1's follow-up: no task previ
           ously created this service or its

  Ran 1 shell command (ctrl+o to expand)

● 141 tasks now (137 + 4 new), all correctly inserted. Let's do a final sanity
  check that everything cross-references correctly.

  Searched for 1 pattern (ctrl+o to expand)

● All wired consistently — defined once, referenced correctly by dependents
  (T076a→T069a/T069b, T078→T076a).

  Done

  - plan.md: TeamCrudService(AdminCrudService[Team, TeamCreate, TeamUpdate])
    documented alongside UserService — read_permission="team.read", hard delete
    (no is_active), plus its one bespoke method, add_member.
  - tasks.md, batch 4d (alongside the other admin-CRUD subclass tasks,
    T068/T069):
    - T069a: backend/app/schemas/team.py — Team, TeamCreate, TeamUpdate,
      TeamMemberCreate.
    - T069b: TeamCrudService in backend/app/services/admin_crud_service.py,
      including add_member(actor, team_id, user_id) -> TeamMember.
    - T076a: /teams CRUD routes and /teams/{id}/members wired into
      backend/app/api/routers/admin_config.py.
    - T078 updated to depend on T076a too.

  On your specific question — yes, team_members had the same gap: POST 
  /teams/{id}/members already existed in contracts/openapi.yaml, but no task
  implemented it (nothing created admin_config.py's /teams routes at all).
  T069b/T076a now cover exactly that endpoint. I did not add a members-list or
  remove-member endpoint — neither exists in the contract, and adding one would
  be new functionality beyond closing the identified gap.

✻ Cogitated for 4m 7s

※ recap: You're building the spec-driven CRM plan for CRM-AZM-Solution and just finished patching gaps in tasks.md (permission split, team CRUD, i18n check, viewport check). Next: review the updated tasks.md/plan.md, then move on to /speckit-implement when ready.